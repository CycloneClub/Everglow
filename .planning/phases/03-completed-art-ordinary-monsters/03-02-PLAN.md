---
phase: 03-completed-art-ordinary-monsters
plan: 02
type: execute
wave: 2
depends_on: [03-01]
files_modified:
  - Sources/Modules/Yggdrasil/KelpCurtain/NPCs/GuppyConch.cs
  - Sources/Modules/Yggdrasil/KelpCurtain/NPCs/VerdantRods.cs
  - .planning/phases/03-completed-art-ordinary-monsters/03-BIOLOGY.json
  - .planning/phases/03-completed-art-ordinary-monsters/03-BIOLOGY.md
  - .planning/phases/03-completed-art-ordinary-monsters/03-DEVIATIONS.md
autonomous: true
requirements: [BIO-01, BIO-03, BIO-06, QUAL-03]
user_setup: []
estimate:
  tokens: 58000
  raw_tokens: 58000
  tasks: 3
  confidence: low

must_haves:
  truths:
    - "格普螺 (`GuppyConch`) spawns only inside the Yggdrasil Subworld, crawls slowly on land, never targets the player, deals only contact damage, retracts into its shell when hit (defense 10 -> 20, incoming damage scaled to 70%), and drops 格普螺外壳 at 11% from an item implemented in Phase 1 (D-28/D-31/D-32/D-36/D-37)."
    - "叶飞棍 (`VerdantRods`) spawns only inside the Yggdrasil Subworld, flies with an eight-frame rod AI mirroring the existing `DarkGlimmeringRods` precedent, is neutral (it circles the player and never melees on purpose), takes suffocation damage while submerged but escapes the water, and applies 中毒 for 10 s to 50% of the players it damages (D-28/D-31/D-32)."
    - "Both creatures' designed drops that do not exist in the repository (软体甲壳碎片 for 格普螺; 飞棍毛发 and 毒腺 for 叶飞棍) are absent from the drop tables and recorded as precise blockers, so the mod still builds and loads (D-39)."
    - "Neither class contains `Main.LocalPlayer`, and every dust/visual block in either class is wrapped in `if (!Main.dedServ)` (D-35, QUAL-03)."
    - "`03-BIOLOGY.json` and `03-BIOLOGY.md` are updated consistently for both rows — `code_complete:true` together with the row's `internal_name` populated in the same edit, `frame_count` from the sprite geometry, blockers restated for the absent drops — without changing the frozen counts, the five `phase:3` ids, the six-id `texture_complete` set or the nine-id `design_art` set (D-24)."
    - statement: "In a tModLoader client the guppy conch hides in its shell after a hit and the leaf flying rod drifts around the player without ever chasing them."
      verification: backstop
  artifacts:
    - "Sources/Modules/Yggdrasil/KelpCurtain/NPCs/GuppyConch.cs"
    - "Sources/Modules/Yggdrasil/KelpCurtain/NPCs/VerdantRods.cs"
    - "updated .planning/phases/03-completed-art-ordinary-monsters/03-BIOLOGY.json and 03-BIOLOGY.md"
    - "updated .planning/phases/03-completed-art-ordinary-monsters/03-DEVIATIONS.md"
  key_links:
    - "GuppyConch.SetStaticDefaults -> NPCSpawnManager.RegisterNPC(Type) -> EditSpawnPool keeps the type only inside YggdrasilWorld"
    - "GuppyConch.SpawnChance -> SubworldSystem.IsActive<YggdrasilWorld>() + KelpCurtainBiome.IsKelpCurtainLayer(spawnInfo.player) -> 0f outside the subworld, and an evaluable gate on a dedicated server (BIO-06)"
    - "GuppyConch.ModifyIncomingHit -> shelled state -> 70% incoming damage and NPC.defense 20 -> NPC.netUpdate on every transition"
    - "GuppyConch.ModifyNPCLoot -> ItemDropRule.Common(ModContent.ItemType<GuppyShell>(), 9, 1, 1) -> a Phase 1 item"
    - "VerdantRods.AI -> NPC.noGravity hover with a target position and sine wobble (DarkGlimmeringRods precedent) -> NPC.wet suffocation branch"
    - "VerdantRods.OnHitPlayer -> 50% BuffID.Poisoned for 600 ticks, applied once on the client that took the hit (Player.AddBuff performs the hurt-path buff sync), matching the LargeBloodLanternGhost / LeafcutterAnt precedent"
    - "03-BIOLOGY.json rows -> 03-BIOLOGY.md mirror -> scripts/check-biology.ps1 parity invariant"
  prohibitions:
    - "Never create, move, rename or modify any `.png`; `GuppyConch.png` and `VerdantRods.png` already exist and the classes must sit beside them."
    - "Never add a region subfolder under `NPCs/`; the default texture path must keep resolving to the approved art."
    - "Never write a drop rule for 软体甲壳碎片, 飞棍毛发 or 毒腺 — those classes do not exist and a type reference to them is a compile error (D-37/D-39)."
    - "Never introduce a generic creature base class or interface, and never a parallel spawn system (D-31/D-33)."
    - "Never run the localization exporter and never create or edit an HJSON key (D-20)."
    - "Never gate `SpawnChance` on a client-only camera or screen value, and never call `KelpCurtainBiome.IsBiomeActive` from it: `ModNPC.SpawnChance` runs on the server, where that predicate's camera term is zero, so such a gate silently returns `0f` on a dedicated server. Use the server-safe `KelpCurtainBiome.IsKelpCurtainLayer(spawnInfo.player)` added by plan 03-01."
    - "Never wrap `OnHitPlayer`'s debuff in `Main.netMode != NetmodeID.MultiplayerClient`: tML documents that hook as running on the local client only, so the guard makes the poison dead code in multiplayer."
    - "Never change the frozen `counts`, `phase3_tranche` or `texture_complete` set in `03-BIOLOGY.json`."
  assumptions:
    - "格普螺 is not capturable (only 水蛞蝓, 装甲虾 and 爆弹水母 are 可被捕捉), so it gets no `catchItem`, no `Main.npcCatchable` and no critter item."
    - "11% and 33% drop chances are written as `ItemDropRule.Common` denominators 9 and 3 (D-38)."
    - "叶飞棍 spawns in both 亡碧湖 and 森雨幽谷 per the design row, but no regional biome predicate exists yet, so the gate uses the layer-level server-safe `KelpCurtainBiome.IsKelpCurtainLayer` and the refinement is a recorded blocker."
---

# Phase 3 Plan 02 — 格普螺 and 叶飞棍: Shell-Up Neutral Snail and Neutral Flying Rod

<objective>
Expand the proven chain from plan 03-01 to the two remaining simple repository-art creatures of the tranche: a 300-life neutral giant snail that hides in its shell when struck, and a 100-life neutral flying rod that drifts around the player and can be driven into water.

Purpose: both rows already have approved art in the repository (D-41), both have a complete design row (D-28 full implementation), and both exercise behaviour the phase has not yet proven: a stateful defensive reaction driven by incoming hits (格普螺), and a `noGravity` hover AI with an environmental hazard (叶飞棍). Their drops split cleanly into "exists" (格普螺外壳, wired) and "does not exist" (软体甲壳碎片, 飞棍毛发, 毒腺, blocked), which is the D-37/D-39 boundary the phase's later waves depend on.

D-28 (full implementation), D-31 (vanilla-first AI, local `AI()` where no vanilla style fits), D-32/D-33 (subworld-only spawn through the existing manager), D-34 (conservative defaults recorded), D-35 (`!Main.dedServ`), D-36 (direct drop table), D-37/D-39 (only implemented items in a table) and D-38 (chance defaults recorded) are implemented here.

Output: `GuppyConch.cs`, `VerdantRods.cs`, and updated `03-BIOLOGY.json`, `03-BIOLOGY.md` and `03-DEVIATIONS.md`.
</objective>

<execution_context>
@C:/Users/book/.config/opencode/gsd-core/workflows/execute-plan.md
@C:/Users/book/.config/opencode/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/phases/03-completed-art-ordinary-monsters/03-CONTEXT.md
@.planning/phases/03-completed-art-ordinary-monsters/03-RESEARCH.md
@.planning/phases/03-completed-art-ordinary-monsters/03-01-PLAN.md
@.planning/phases/03-completed-art-ordinary-monsters/03-BIOLOGY.json
@.planning/phases/03-completed-art-ordinary-monsters/03-DEVIATIONS.md
@.planning/phases/01-item-inventory-completed-art-items/evidence/biology.xml
@AGENTS.md
@Sources/Modules/Yggdrasil/AGENTS.md
</context>

## Design Values For This Plan

Source: committed snapshot `.planning/phases/01-item-inventory-completed-art-items/evidence/biology.xml` (read-only, D-25).

| Design row | Heading block id | Stats table id | Behavior `<p>` id | Drop `<p>` id |
| --- | --- | --- | --- | --- |
| 格普螺 | `K2TldVE5woR5xQxjJA8cGswqnnd` | `IZAjdqbGQoNuYzxnEJGcpTEEnQc` | `FsnSd54BHoDUP9xGftGceO7Ynmg` | `IqEbdZxOKo0CoHxSqmgcen4anCg` |
| 叶飞棍 | `RPs8db7IMof8ilxWLPNccOtPnBe` | `EU6pdMiwLoZxqNxi0KDcnceonBb` | `CFCDdFU78ozdhSxpAQsc64btnyd` | `H2dQdrngYoGevPxYaDlc5oganOg` |

**格普螺 (design, verbatim intent):** 中立生物, never initiates an attack on any creature; a green-fleshed giant snail named after the warlock 格普; crawls slowly on the ground; retracts into its shell after being hit; still deals contact damage. Stats: 类型 普通, 生命 300, 伤害 25, 防御 10 (正常) / 20 (缩壳), 击退抗性 85, 减伤 15 (正常) / 30 (缩壳), 免疫 困惑, 钱币（铜） 400 (`4银`; `NPC.value` is in copper coins, so the design value transposes 1:1 and is **not** `40000`), 稀有度 (cell empty). Drops: 0~2 软体甲壳碎片 (**absent**), 11% 1 格普螺外壳 (**`GuppyShell`, implemented in Phase 1**).

**叶飞棍 (design, verbatim intent):** a land creature that also spawns in 森雨幽谷; a green flying rod with a leaf-like appearance; AI similar to the most basic flying rod; neutral — does not attack the player, but circles nearby; occasionally skims water when a water source is near; suffocates in water like a vanilla land creature but can fly out; after damaging the player there is a 50% chance to inflict 中毒 for 10 s. Stats: 类型 普通, 生命 100, 伤害 30, 防御 4, 击退抗性 20, 免疫 中毒, 钱币（铜） 200 (`2银`; copper, transposed 1:1 — **not** `20000`), 稀有度 (cell empty). Drops: 33% 飞棍毛发 (**absent**), 11% 毒腺 (**absent**).

**类型 vs 稀有度 (applies to both rows).** The leading design column is `类型` (both rows write 普通) and the trailing `稀有度` cell is **empty**; `NPC.rare = ItemRarityID.White` is therefore a conservative default recorded in `03-DEVIATIONS.md` §4, not a design-sourced value.

## Repository Art (frozen by plan 03-01, D-41)

- `Sources/Modules/Yggdrasil/KelpCurtain/NPCs/GuppyConch.png` — 114x58, git-tracked.
- `Sources/Modules/Yggdrasil/KelpCurtain/NPCs/VerdantRods.png` — 54x432, git-tracked; 432 / 54 = 8, so an eight-frame vertical strip, matching the existing `DarkGlimmeringRods` eight-frame precedent.

Both classes must be created in `Sources/Modules/Yggdrasil/KelpCurtain/NPCs/` with the asset's basename, so tML's default texture resolution finds the art with no `Texture` override and no asset move. Derive `Main.npcFrameCount[NPC.type]` from the sprite's integer frame geometry and write the same number into the matrix row's `frame_count` (if 114x58 is not integer-divisible, use 1 and record it in `03-DEVIATIONS.md`).

## Artifacts This Plan Produces

- `Sources/Modules/Yggdrasil/KelpCurtain/NPCs/GuppyConch.cs` — `Everglow.Yggdrasil.KelpCurtain.NPCs.GuppyConch`
- `Sources/Modules/Yggdrasil/KelpCurtain/NPCs/VerdantRods.cs` — `Everglow.Yggdrasil.KelpCurtain.NPCs.VerdantRods`
- Updated `.planning/phases/03-completed-art-ordinary-monsters/03-BIOLOGY.json`, `03-BIOLOGY.md`, `03-DEVIATIONS.md`

<tasks>

<task type="auto">
  <name>Task 1: 格普螺 — shell-up neutral giant snail with its Phase 1 accessory drop</name>
  <reversibility rating="costly">D-28: the internal name `GuppyConch` and its state names become compatibility-sensitive once committed.</reversibility>
  <files>Sources/Modules/Yggdrasil/KelpCurtain/NPCs/GuppyConch.cs</files>
  <precondition>The Wave 1 gate is green: `powershell -NoProfile -ExecutionPolicy Bypass -File ".planning/phases/03-completed-art-ordinary-monsters/scripts/check-biology.ps1"` exits 0 (plan 03-01).</precondition>
  <read_first>
    - `Sources/Modules/Yggdrasil/KelpCurtain/NPCs/MossyThornTurtle.cs` (written in plan 03-01 — mirror its registration, spawn gate, dedserv handling and loot shape exactly).
    - `Sources/Modules/Yggdrasil/KelpCurtain/NPCs/RiverSlug.cs` (the local-`AI()` crawler precedent: private state `enum` plus named wrapper properties over `NPC.ai[]`/`NPC.localAI[]`, and the authoritative-random-write pattern).
    - `Sources/Modules/Yggdrasil/Common/NPCSpawnManager.cs` and `Sources/Modules/Yggdrasil/KelpCurtain/KelpCurtainBiome.cs` lines 35-48 (the spawn pool and the client-facing biome predicate); the server-safe `public static bool IsKelpCurtainLayer(Player player)` added below `IsBiomeActive` by plan 03-01 is the predicate `SpawnChance` must use — reuse it, do not duplicate it and do not call `IsBiomeActive` from a spawn hook.
    - `Sources/Modules/Yggdrasil/KelpCurtain/Items/Accessories/GuppyShell.cs` (the drop item's class name and namespace).
    - `Sources/Modules/Yggdrasil/YggdrasilTown/NPCs/CyanOreBeetle.cs` line 15 (`NPCID.Sets.SpecificDebuffImmunity[Type][BuffID.Confused] = true;` — this row's 免疫 cell is 困惑).
    - `.planning/phases/03-completed-art-ordinary-monsters/03-BIOLOGY.json` (the row `bio-valley-of-lush-and-moist-guppy-conch`).
    - `.planning/phases/03-completed-art-ordinary-monsters/03-RESEARCH.md` §Pattern 4 (server-authoritative AI with `ai[]` wrappers and `netUpdate`) and §Pitfall 6.
  </read_first>
  <action>
    Create `Sources/Modules/Yggdrasil/KelpCurtain/NPCs/GuppyConch.cs` for the design row 格普螺 as a full implementation (D-28): namespace `Everglow.Yggdrasil.KelpCurtain.NPCs`, file-scoped namespace, tabs, Allman braces, LF, UTF-8 without BOM, trailing newline, with a class XML doc comment naming the design row, its region, its heading block id `K2TldVE5woR5xQxjJA8cGswqnnd`, and that the file placement beside `GuppyConch.png` is what resolves the approved art.

    - `public override string LocalizationCategory => Everglow.Commons.Utilities.LocalizationUtils.Categories.NPCs;` — no HJSON key is created (D-20).
    - `SetStaticDefaults`: set `Main.npcFrameCount[NPC.type]` from the sprite's integer frame geometry (record the value); `NPCSpawnManager.RegisterNPC(Type);`; `NPCID.Sets.SpecificDebuffImmunity[Type][BuffID.Confused] = true;` (design 免疫 困惑).
    - `SetDefaults`: this creature has no vanilla equivalent — 缩壳 on being hit is not a vanilla `aiStyle` — so set `NPC.aiStyle = -1;` and write a small local `AI()` (D-31 permits local `AI()` when no vanilla style fits; note that judgement in a comment). Set `NPC.width`/`NPC.height` from the sprite size, `NPC.lifeMax = 300; NPC.life = 300; NPC.damage = 25; NPC.defense = 10; NPC.knockBackResist = 0.15f;` (击退抗性 85% maps to `1 - 0.85`), `NPC.value = 400;` (the design's 钱币（铜） cell is `4银` = **400 copper**, and `NPC.value` is documented in copper coins, so the value transposes 1:1 — not `40000`), `NPC.rare = ItemRarityID.White;` (a conservative default: this row's 稀有度 cell is empty and 普通 is its 类型 cell — record it in `03-DEVIATIONS.md` §4), `NPC.friendly = false;`, `NPC.HitSound`, `NPC.DeathSound`, and `SpawnModBiomes = [ModContent.GetInstance<KelpCurtainBiome>().Type];`. Deliberately set `NPC.catchItem = 0;` and do not set `Main.npcCatchable` or `NPCID.Sets.CountsAsCritter`: this creature is not 可被捕捉.
    - Local crawl state: a private `enum GuppyConchState { Crawling, Shelled }` plus named wrapper properties over `NPC.ai[0]` (state) and `NPC.localAI[0]` (shell timer), never bare numeric indexes. `AI()`: `NPC.TargetClosest()` only for facing/`spriteDirection`, never to attack (the design says it never initiates an attack); crawl with `NPC.velocity.X = CrawlSpeed * NPC.direction;` under normal gravity; reverse `NPC.direction` at a wall or when no floor tile exists ahead of the leading edge; stay below `CrawlSpeed` (a small value such as 0.4f) to read as a slow snail.
    - Shell state: when the creature takes a hit, enter `Shelled` with `ShellTimer` set to 120 ticks (a conservative default, recorded); on entry set `NPC.defense = 20;` and `NPC.velocity = Vector2.Zero;`, on exit set `NPC.defense = 10;`; only the server performs the transition (`Main.netMode != NetmodeID.MultiplayerClient`) and each transition sets `NPC.netUpdate = true`. While `Shelled`, the crawl velocity stays zero and the timer decrements on the server.
    - `public override void ModifyIncomingHit(ref NPC.HitModifiers modifiers)`: while `Shelled`, apply `modifiers.FinalDamage *= 0.70f;` — the design's 30% 减伤 (缩壳); while `Crawling`, apply `modifiers.FinalDamage *= 0.85f;` — the design's 15% 减伤. Add a comment naming the design cells and their 正常/缩壳 split. Do not scale `modifiers.Defense`; the defense switch is done on `NPC.defense` at the state transition so the value is server-authoritative.
    - `ModifyNPCLoot(NPCLoot npcLoot)`: add exactly one rule, `npcLoot.Add(ItemDropRule.Common(ModContent.ItemType<GuppyShell>(), 9, 1, 1));` (11% for 1 格普螺外壳, denominator 9). Add a short comment recording that the design's other drop, 0~2 软体甲壳碎片, has no repository item and is therefore not written (D-39) — the blocker text is recorded in the matrix, not invented as a type reference.
    - `HitEffect(NPC.HitInfo hit)`: on death emit a small dust burst inside `if (!Main.dedServ)`, reusing an existing KelpCurtain dust (`Dusts.LichenSlime` is already used by `RiverSlug`); create no new dust class.
    - `SpawnChance(NPCSpawnInfo spawnInfo)`: return `0f` unless `SubworldSystem.IsActive<YggdrasilWorld>()` **and** `KelpCurtainBiome.IsKelpCurtainLayer(spawnInfo.player)` — the server-safe layer predicate from plan 03-01. Do **not** call `KelpCurtainBiome.IsBiomeActive` here: `ModNPC.SpawnChance` is documented as running in single player or **on the server only**, and that predicate's camera term is zero on a dedicated server, so it would return `0f` there and this creature could never spawn. Use `spawnInfo.player` and never `Main.LocalPlayer` (server-only hook). Then require the design's context: this is a land crawler, so return `0f` when the spawn tile is under water and when `spawnInfo.Water` is set. Return a conservative weight in the 0.5f–2f band and record it as an assumption (D-34); do not gate on region X/Y ranges — no 森雨幽谷 predicate exists yet and that gap is a recorded blocker.
    - Add no `Texture` override, no handwritten asset path, no base class or interface, and no `.png`.
  </action>
  <verify>
    <automated>dotnet build /p:Configuration=Release /p:WarningLevel=0; if ($?) { powershell -NoProfile -ExecutionPolicy Bypass -File ".planning/phases/03-completed-art-ordinary-monsters/scripts/check-biology.ps1" }</automated>
    <fails_when>non-zero exit — an `error CS` in the Release build (for example `GuppyShell` or a dust type not resolvable), or `check-biology.ps1` printing `FAIL(` or not printing `OK(0): phase3 tranche = 5 / 5 (rows=31)`.</fails_when>
    <human-check>
      <test>In a tModLoader client, find a guppy conch in the Kelp Curtain and hit it once, then walk into it.</test>
      <expected>The conch crawls slowly, never turns to attack, deals contact damage when walked into, and on being hit stops moving and stays retracted for about two seconds before resuming the crawl; subsequent hits during the retracted window hurt noticeably less.</expected>
      <why_human>The shell-state feel, the defense switch and the contact damage are runtime gameplay properties; the D-21 batch in plan 03-04 records the run.</why_human>
    </human-check>
  </verify>
  <acceptance_criteria>
    - `Sources/Modules/Yggdrasil/KelpCurtain/NPCs/GuppyConch.cs` exists beside the tracked `GuppyConch.png` and contains `NPCSpawnManager.RegisterNPC`, `IsKelpCurtainLayer(spawnInfo.player)`, `SubworldSystem.IsActive<YggdrasilWorld>`, `NPC.lifeMax = 300`, `NPC.value = 400;` (with the terminating semicolon, so a stray `40000` cannot satisfy it by substring), `SpecificDebuffImmunity[Type][BuffID.Confused]`, `ModifyIncomingHit` with both the `0.70f` and `0.85f` scales, `ModifyNPCLoot` with `ItemDropRule.Common(ModContent.ItemType<GuppyShell>(), 9, 1, 1)`, and `Main.dedServ`.
    - The file contains no `Main.LocalPlayer`, no `Texture` override, no `catchItem` assignment other than `0`, no reference to 软体甲壳碎片 or any other unimplemented type, and no bare numeric `NPC.ai[]` index outside its enum wrapper.
    - `dotnet build /p:Configuration=Release /p:WarningLevel=0` exits 0 and `check-biology.ps1` prints `OK(0): phase3 tranche = 5 / 5 (rows=31)`; its informational line still reads `OK: implemented classes = 2 / 5` because this task creates the class but Task 3 is what flips the row's `code_complete`/`internal_name`, and gate invariant 5 keeps the two in step.
    - No `.png` was added, moved or modified.
  </acceptance_criteria>
  <done>格普螺 is a full, subworld-isolated, shell-up neutral crawler whose only implemented drop (格普螺外壳) is wired and whose absent drop carries a recorded blocker.</done>
  <commit>feat(03-02): implement GuppyConch shell-up neutral crawler with GuppyShell drop</commit>
</task>

<task type="auto">
  <name>Task 2: 叶飞棍 — neutral eight-frame flying rod with water suffocation and 50% poison</name>
  <reversibility rating="costly">D-28: the internal name `VerdantRods` and its AI state layout become compatibility-sensitive once committed.</reversibility>
  <files>Sources/Modules/Yggdrasil/KelpCurtain/NPCs/VerdantRods.cs</files>
  <precondition>`Sources/Modules/Yggdrasil/KelpCurtain/NPCs/GuppyConch.cs` exists on disk and `dotnet build /p:Configuration=Release /p:WarningLevel=0` exited 0 (Task 1).</precondition>
  <read_first>
    - `Sources/Modules/Yggdrasil/YggdrasilTown/NPCs/DarkGlimmeringRods.cs` (the in-repo flying-rod precedent: eight frames, `aiStyle = -1`, `NPC.noGravity = true`, `OnSpawn` scale, the `TargetPos` + sine-wobble hover AI, and the `NPCSpawnManager.RegisterNPC` registration).
    - `Sources/Modules/Yggdrasil/KelpCurtain/NPCs/GuppyConch.cs` (written in Task 1 — mirror its spawn gate, `!Main.dedServ` and registration shape).
    - `Sources/Modules/Yggdrasil/KelpCurtain/NPCs/MossyThornTurtle.cs` (written in plan 03-01 — the `ModifyNPCLoot` shape).
    - `Sources/Modules/Yggdrasil/Common/NPCSpawnManager.cs` and `Sources/Modules/Yggdrasil/KelpCurtain/KelpCurtainBiome.cs` lines 35-48 (the spawn pool, the client-facing predicate, and the server-safe `IsKelpCurtainLayer` added by plan 03-01 that `SpawnChance` must use).
    - `Sources/Modules/Yggdrasil/KelpCurtain/Projectiles/Melee/AcroporaSpear_proj.cs` line 401 (`target.AddBuff(BuffID.Poisoned, 600);` — the module's poison-on-hit shape).
    - `.planning/phases/03-completed-art-ordinary-monsters/03-BIOLOGY.json` (the row `bio-death-jade-lake-verdant-rods`).
  </read_first>
  <action>
    Create `Sources/Modules/Yggdrasil/KelpCurtain/NPCs/VerdantRods.cs` for the design row 叶飞棍 as a full implementation (D-28): same namespace, formatting and comment conventions as Task 1; the class XML doc comment names the design row, its regions (亡碧湖 and 森雨幽谷), its heading block id `RPs8db7IMof8ilxWLPNccOtPnBe`, and that the file placement beside `VerdantRods.png` resolves the approved art.

    - `LocalizationCategory => Everglow.Commons.Utilities.LocalizationUtils.Categories.NPCs;` — no HJSON key (D-20).
    - `SetStaticDefaults`: `Main.npcFrameCount[NPC.type] = 8;` (432 / 54, the `DarkGlimmeringRods` precedent), `NPCSpawnManager.RegisterNPC(Type);`, `NPCID.Sets.SpecificDebuffImmunity[Type][BuffID.Poisoned] = true;` (design 免疫 中毒).
    - `SetDefaults`: `NPC.aiStyle = -1;`, `NPC.width = 54; NPC.height = 54;` (one frame), `NPC.lifeMax = 100; NPC.life = 100; NPC.damage = 30; NPC.defense = 4; NPC.knockBackResist = 0.8f;` (击退抗性 20% maps to `1 - 0.20`), `NPC.value = 200;` (the design's 钱币（铜） cell is `2银` = **200 copper**, and `NPC.value` is documented in copper coins, so the value transposes 1:1 — not `20000`), `NPC.rare = ItemRarityID.White;` (a conservative default: this row's 稀有度 cell is empty and 普通 is its 类型 cell — record it in `03-DEVIATIONS.md` §4), `NPC.noGravity = true;`, `NPC.friendly = false;`, `NPC.HitSound`, `NPC.DeathSound`, `NPC.catchItem = 0;` with no `Main.npcCatchable` and no `NPCID.Sets.CountsAsCritter`, and `SpawnModBiomes = [ModContent.GetInstance<KelpCurtainBiome>().Type];`.
    - Hover AI mirroring `DarkGlimmeringRods`: a public `Vector2 TargetPos` field set in `OnSpawn` to a point near the creature, re-aimed at the player's centre only when the design's "circles the player" behaviour applies (the design is neutral — it never melees on purpose, so do not add a `TargetClosest`-driven attack); damp velocity each tick and steer toward `TargetPos` plus a sine wobble; set `NPC.rotation` from the horizontal velocity as `DarkGlimmeringRods` does; animate `NPC.frame`/`NPC.frameCounter` across the eight frames.
    - Water behaviour: while the creature is submerged (`NPC.wet` and the tile at `NPC.Center` has `LiquidAmount > 0`), the server reduces `NPC.life` by a small amount every 30 ticks (a conservative default, recorded) and sets `NPC.netUpdate = true`, so the design's "suffocates in water like a vanilla land creature" is represented without inventing a debuff; the AI also biases `TargetPos` upward while submerged so it flies out, matching "can fly out normally". Guard the life write with `Main.netMode != NetmodeID.MultiplayerClient` and never reduce below 1 life.
    - Optional flavour: when a water source is within a few tiles, occasionally dip `TargetPos` to the liquid surface ("偶尔会点水"). Keep this purely cosmetic and inside the AI; if you implement it, keep it server-safe (no client-only state writes).
    - `OnHitPlayer(Player target, Player.HurtInfo hurtInfo)`: apply `if (Main.rand.NextBool(2)) { target.AddBuff(BuffID.Poisoned, 600); }` — the design's 50% / 10 s. Do **not** wrap this in `if (Main.netMode != NetmodeID.MultiplayerClient)`: tML documents `ModNPC.OnHitPlayer` as "Called on the local client only", so a netmode guard would make the poison dead code in multiplayer — the documented 50% poison would then never apply to a player. The hook runs once, on the client that owns the player being hit (the same locality contract plan 03-01 applies to `OnHitByItem`), and `Player.AddBuff` performs its own hurt-path client→server buff sync, exactly as the repo precedents do: `LargeBloodLanternGhost.OnHitPlayer` applies its debuff unguarded, and `LeafcutterAnt.OnHitPlayer` applies `target.AddBuff(BuffID.Poisoned, 600);` unguarded. Record this locality choice in `03-DEVIATIONS.md` §4.
    - `ModifyNPCLoot(NPCLoot npcLoot)`: write the override with an **empty body** and a comment naming both of the design's drops (33% 飞棍毛发, 11% 毒腺) and stating that neither item exists in the repository, so no rule is written and the mod still builds and loads (D-37/D-39). Do not reference either type.
    - `HitEffect(NPC.HitInfo hit)`: on death emit a small dust burst inside `if (!Main.dedServ)`, reusing an existing KelpCurtain dust; create no new dust class.
    - `SpawnChance(NPCSpawnInfo spawnInfo)`: return `0f` unless `SubworldSystem.IsActive<YggdrasilWorld>()` **and** `KelpCurtainBiome.IsKelpCurtainLayer(spawnInfo.player)` (the server-safe predicate from plan 03-01; never `KelpCurtainBiome.IsBiomeActive`, whose camera term is zero on a dedicated server and would block every spawn there); use `spawnInfo.player` and never `Main.LocalPlayer`. Require an open-air position (do not gate on water); return a conservative weight in the 0.5f–2f band recorded as an assumption (D-34). Do not gate on region X/Y ranges — no 森雨幽谷 predicate exists yet; that gap is a recorded blocker.
    - Add no `Texture` override, no handwritten asset path, no base class or interface, and no `.png`.
  </action>
  <verify>
    <automated>dotnet build /p:Configuration=Release /p:WarningLevel=0; if ($?) { powershell -NoProfile -ExecutionPolicy Bypass -File ".planning/phases/03-completed-art-ordinary-monsters/scripts/check-biology.ps1" }</automated>
    <fails_when>non-zero exit — an `error CS` in the Release build, or `check-biology.ps1` printing `FAIL(` or not printing `OK(0): phase3 tranche = 5 / 5 (rows=31)`.</fails_when>
    <human-check>
      <test>In a tModLoader client, meet a leaf flying rod in the Kelp Curtain, let it touch you several times, then drive it into deep water.</test>
      <expected>The rod drifts in a wobbling path, circles without chasing, sometimes inflicts the poisoned debuff on contact, loses health while fully submerged and flies back out.</expected>
      <why_human>The hover feel, the poison rate and the water suffocation are runtime gameplay properties; the D-21 batch in plan 03-04 records the run.</why_human>
    </human-check>
  </verify>
  <acceptance_criteria>
    - `Sources/Modules/Yggdrasil/KelpCurtain/NPCs/VerdantRods.cs` exists beside the tracked `VerdantRods.png` and contains `Main.npcFrameCount[NPC.type] = 8`, `NPCSpawnManager.RegisterNPC`, `IsKelpCurtainLayer(spawnInfo.player)`, `SubworldSystem.IsActive<YggdrasilWorld>`, `NPC.lifeMax = 100`, `NPC.value = 200;` (with the terminating semicolon, so a stray `20000` cannot satisfy it by substring), `NPC.noGravity = true`, `SpecificDebuffImmunity[Type][BuffID.Poisoned]`, `OnHitPlayer` with `BuffID.Poisoned` and a 600-tick duration *outside* any multiplayer-client netmode guard, `ModifyNPCLoot`, and `Main.dedServ`.
    - The file contains no `Main.LocalPlayer`, no `Texture` override, no reference to 飞棍毛发 or 毒腺, and no bare numeric `NPC.ai[]` index outside its enum wrapper.
    - `dotnet build /p:Configuration=Release /p:WarningLevel=0` exits 0 and `check-biology.ps1` prints `OK(0): phase3 tranche = 5 / 5 (rows=31)`; the informational `OK: implemented classes = 2 / 5` line is still expected here because Task 3 flips this row's `code_complete`/`internal_name`.
    - No `.png` was added, moved or modified.
  </acceptance_criteria>
  <done>叶飞棍 is a full, subworld-isolated neutral flying rod with the eight-frame hover AI, water suffocation, and the documented 50% poison, with an explicitly empty and commented loot table.</done>
  <commit>feat(03-02): implement VerdantRods neutral flying rod with water suffocation and poison</commit>
</task>

<task type="auto">
  <name>Task 3: Reconcile both matrix rows, mirror and ledger; close the wave gate</name>
  <reversibility rating="costly">D-24: the matrix is the shared contract for Phase 4 and Phase 8; a status or blocker written here is consumed downstream.</reversibility>
  <files>.planning/phases/03-completed-art-ordinary-monsters/03-BIOLOGY.json, .planning/phases/03-completed-art-ordinary-monsters/03-BIOLOGY.md, .planning/phases/03-completed-art-ordinary-monsters/03-DEVIATIONS.md</files>
  <precondition>`GuppyConch.cs` and `VerdantRods.cs` both exist and the Release build is green (Tasks 1–2).</precondition>
  <read_first>
    - `.planning/phases/03-completed-art-ordinary-monsters/03-BIOLOGY.json` (the two rows `bio-valley-of-lush-and-moist-guppy-conch` and `bio-death-jade-lake-verdant-rods`).
    - `.planning/phases/03-completed-art-ordinary-monsters/03-DEVIATIONS.md` (the eight sections written in plan 03-01; append rather than restructure).
    - `.planning/phases/03-completed-art-ordinary-monsters/scripts/check-biology.ps1` (invariants 4, 5, 7, 8, 11 and the BOM invariant 13 the update must keep green; invariant 5's biconditional is what makes the row flip and its `internal_name` a single atomic edit).
    - `.planning/phases/03-completed-art-ordinary-monsters/03-BIOLOGY.md` (the 31-row mirror whose cells must stay byte-identical to the JSON).
  </read_first>
  <action>
    In `03-BIOLOGY.json`, update the two rows implemented by this plan without touching the frozen `counts`, `phase3_tranche`, `texture_complete` or `design_art` values. Each row is flipped to `code_complete: true` **and** given its `internal_name` in the same edit — the gate's invariant 5 makes that biconditional binding (a `phase == 3` row with `code_complete: true` must carry a non-empty `internal_name` whose `<short name>.cs` exists on disk, and a row with `code_complete: false` must carry an empty one), so a row flipped without its `internal_name` filled, or an `internal_name` filled before its class exists, fails the gate rather than passing silently:
    - `bio-valley-of-lush-and-moist-guppy-conch`: `code_complete: true`; `internal_name: "Everglow.Yggdrasil.KelpCurtain.NPCs.GuppyConch"`; `frame_count` set to the value the class actually uses; `mapping_confidence: "high"`; blockers replaced with the drop blocker `"drop blocked (absent item): design 0~2 软体甲壳碎片 has no repository ModItem; the 11% 格普螺外壳 rule is wired to GuppyShell (Phase 1)"` plus the retained marker `"localization deferred (D-20); runtime verification outstanding (D-21)"`.
    - `bio-death-jade-lake-verdant-rods`: `code_complete: true`; `internal_name: "Everglow.Yggdrasil.KelpCurtain.NPCs.VerdantRods"`; `frame_count: 8`; `mapping_confidence: "assumed"`; blockers replaced with `"drop blocked (absent items): design 33% 飞棍毛发 and 11% 毒腺 have no repository ModItem, so ModifyNPCLoot is deliberately empty (D-39)"` plus the retained localization/runtime marker.
    Regenerate `03-BIOLOGY.md` from the updated JSON so every id, status and blocker cell stays identical between the two files, and keep the row count at exactly 31.

    Append to `03-DEVIATIONS.md` (do not restructure the existing eight sections):
    - under §4 Conservative defaults: 格普螺's shell timer (120 ticks) and spawn weight, 叶飞棍's spawn weight, the water-suffocation tick interval (30), the `1 - percent/100` knockback conversions for both rows, **the `NPC.value` copper transpose (格普螺 `4银` -> `400`, 叶飞棍 `2银` -> `200`; `钱币（铜）` and `NPC.value` are both copper, so no conversion is applied and the 100x readings `40000` / `20000` are explicitly rejected)**, **the `NPC.rare = ItemRarityID.White` choice (the design's 稀有度 cell is empty for both rows, so 普通 is that row's 类型 cell and not a rarity value)**, and the `OnHitPlayer` locality note (tML does not run that hook on the server, so the 50% poison is applied once on the client that owns the hit player and `Player.AddBuff` syncs it; a `Main.netMode != NetmodeID.MultiplayerClient` guard would have made it dead code in multiplayer);
    - under §5 Drop availability: the two rows' drop disposition (one wired rule each for 格普螺; an empty table for 叶飞棍) with the exact absent item names;
    - under §7 Localization: extend the deferral record to both rows;
    - under §8 Runtime verification: add both rows to the D-21 client check list;
    - a new §9 Region scope note only if the two rows' spawn gating differs from the plan-03-01 note (both use the server-safe layer predicate `KelpCurtainBiome.IsKelpCurtainLayer`, never the client-camera `IsBiomeActive`; 叶飞棍's design also names 森雨幽谷, which is the same missing-predicate gap).

    Record the `aiStyle` decision for 格普螺 under §4 as well: no vanilla `aiStyle` provides a hit-triggered shell-retract state, so a local `AI()` was written, which D-31 permits.

    Do not create or modify any `.png`, do not edit an HJSON file, and do not re-run `scripts/parse-design-xml.ps1`.
  </action>
  <verify>
    <automated>dotnet build /p:Configuration=Release /p:WarningLevel=0; if ($?) { powershell -NoProfile -ExecutionPolicy Bypass -File ".planning/phases/03-completed-art-ordinary-monsters/scripts/check-biology.ps1" }</automated>
    <fails_when>non-zero exit — an `error CS`, or `check-biology.ps1` printing `FAIL(` (for example a Markdown parity failure, a row count other than 31, a `phase == 3` row whose `code_complete` and `internal_name` disagree, a `code_complete` row whose class file is missing, or the BOM invariant reporting a file) instead of `OK(0): phase3 tranche = 5 / 5 (rows=31)`.</fails_when>
  </verify>
  <acceptance_criteria>
    - Both rows read `code_complete: true` with a non-empty `internal_name` (`...NPCs.GuppyConch`, `...NPCs.VerdantRods`) that resolves to the class file created in Tasks 1-2, and `03-BIOLOGY.json` still has 31 rows, the same five `phase3_tranche` ids, the same six-id `texture_complete` set and the same nine-id `design_art` set.
    - `03-BIOLOGY.md` still has 31 rows whose ids, statuses and blocker cells match the JSON.
    - `03-DEVIATIONS.md` records both rows' conservative defaults (including the copper `NPC.value` transpose, the empty-稀有度 conservative `NPC.rare` default and the `OnHitPlayer` poison-locality note), drop disposition, localization deferral, runtime-verification entry and the local-`AI()` justification for 格普螺.
    - `check-biology.ps1` exits 0 and prints `OK(0): phase3 tranche = 5 / 5 (rows=31)` followed by `OK: implemented classes = 4 / 5` (`RiverSlug`, `MossyThornTurtle` and this plan's two new classes; only 巨树人 is still `code_complete: false` with an empty `internal_name` for plan 03-03); `dotnet build /p:Configuration=Release /p:WarningLevel=0` exits 0.
    - No `.png`, HJSON file or Feishu document was created or modified.
  </acceptance_criteria>
  <done>Both new creatures are recorded as implemented with their precise drop blockers, the mirror and ledger agree with the JSON, and the wave gate is green.</done>
  <commit>docs(03-02): reconcile guppy conch and leaf flying rod rows with blockers</commit>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Creature class -> spawn system | Both creatures register with `NPCSpawnManager` and gate their own `SpawnChance` on `SubworldSystem.IsActive<YggdrasilWorld>()` plus the server-safe `KelpCurtainBiome.IsKelpCurtainLayer(spawnInfo.player)`. |
| NPC -> player damage | Contact damage and the water suffocation drain are computed server-side; the 50% poison roll and its `AddBuff` run once on the local client that owns the hit player, through tML's documented `OnHitPlayer` locality, with `Player.AddBuff` performing the buff sync. |
| NPC -> loot table | Each drop rule's item type must exist on disk; absent designed drops are comments and matrix blockers, never type references. |
| Class file -> mod loader texture resolution | Both classes sit beside their tracked `.png` with no `Texture` override and no handwritten path. |
| Matrix JSON -> mirror and gate | The gate resolves classes from the working tree and re-asserts row counts, tranche membership and per-row status. |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-03-09 | Elevation of Privilege | Main-world leakage of 格普螺 or 叶飞棍, or a spawn gate that never fires on a dedicated server (BIO-06) | high | mitigate | Both `SpawnChance` overrides return `0f` unless `SubworldSystem.IsActive<YggdrasilWorld>()` **and** the server-safe `KelpCurtainBiome.IsKelpCurtainLayer(spawnInfo.player)` (the client-camera-based `IsBiomeActive` would have returned `0f` on a dedicated server and blocked the whole tranche); gate invariant 7 rejects a Phase 3 class that declares `SpawnChance` without both tokens; the D-21 run checks an ordinary world and a dedicated-server launch. |
| T-03-10 | Tampering | Client-authoritative shell state or suffocation damage | medium | mitigate | The shell transition, the shell timer and the water life drain run under `Main.netMode != NetmodeID.MultiplayerClient` with `NPC.netUpdate = true` on state changes; `SpawnChance` uses `spawnInfo.player`. The poison is deliberately *not* netmode-guarded: `OnHitPlayer` is a local-client hook, so the 50% roll and `target.AddBuff` run once on the hit player's own client and `Player.AddBuff` performs the buff sync (matching `LeafcutterAnt.OnHitPlayer`), rather than being suppressed into dead code. |
| T-03-11 | Denial of Service | Compile break from a drop rule naming an absent item | high | mitigate | 叶飞棍's drop table is deliberately empty with a comment; 格普螺 references only `GuppyShell`; gate invariant 8 resolves every `ModContent.ItemType<X>()` in a Phase 3 class against the on-disk item tree; the Release build proves resolution. |
| T-03-12 | Denial of Service | Dedicated-server crash from graphics access | medium | mitigate | Every dust block in both classes sits inside `if (!Main.dedServ)`; gate invariant 7 enforces it. |
| T-03-13 | Tampering | Silent drop omission presented as completion | high | mitigate | Both rows carry an explicit drop blocker in the matrix and in `03-DEVIATIONS.md` §5; the ledger names the exact absent items. |
| T-03-14 | Repudiation | Matrix/mirror divergence after the row update | medium | mitigate | The mirror is regenerated from the JSON and gate invariant 11 re-asserts id-set and per-row status parity on every run. |
| T-03-15 | Tampering | Hand-created localization keys | high | mitigate | The exporter is not run and no HJSON is edited; the D-20 deferral is extended to both rows. |
| T-03-SC | Tampering | Package supply chain | low | accept | No package is installed by this plan (03-RESEARCH §Package Legitimacy Audit: not applicable). |
</threat_model>

<verification>
- `dotnet build /p:Configuration=Release /p:WarningLevel=0` exits 0 with no `error CS`.
- `scripts/check-biology.ps1` exits 0 and prints `OK(0): phase3 tranche = 5 / 5 (rows=31)` after Tasks 1, 2 and 3.
- Neither class file contains `Main.LocalPlayer`, a `Texture` override, a handwritten asset path, or a reference to an unimplemented item type.
- Both `SpawnChance` overrides reference `SubworldSystem.IsActive<YggdrasilWorld>()` **and** the server-safe `KelpCurtainBiome.IsKelpCurtainLayer(spawnInfo.player)` and use `spawnInfo.player`, so the gate is evaluable on a dedicated server (never the client-camera predicate).
- `NPC.value` is the design's copper amount in both classes (`4银` -> `400`, `2银` -> `200`), not a 100x reading; `格普螺`'s `OnHitPlayer`-free defensive behaviour and `叶飞棍`'s poison are applied/rolled exactly once, and the poison is not inside a multiplayer-client netmode guard.
- `03-BIOLOGY.json` / `03-BIOLOGY.md` still hold 31 rows with unchanged frozen counts, tranche ids, `texture_complete` set and `design_art` set; the two rows are `code_complete: true` with a populated `internal_name` resolving to their class file and with their drop blockers; the two `code_complete: false` tranche rows that remain carry an empty `internal_name`.
- No `.png` or other binary asset was added, moved, renamed or modified; no HJSON was edited.
</verification>

<success_criteria>
- 格普螺 and 叶飞棍 are full implementations (D-28) with their documented stats, behaviour and spawn isolation, reachable only inside the Yggdrasil Subworld.
- 格普螺's Phase 1 accessory drop is wired; every absent designed drop carries a precise blocker instead of a type reference (D-37/D-39).
- The matrix, mirror and ledger stay consistent and the wave gate is green.
</success_criteria>

<output>
Create `.planning/phases/03-completed-art-ordinary-monsters/03-02-SUMMARY.md` when done.
</output>
