---
phase: 03-completed-art-ordinary-monsters
plan: 04
type: execute
wave: 4
depends_on: [03-03]
files_modified:
  - .planning/phases/03-completed-art-ordinary-monsters/03-BIOLOGY.json
  - .planning/phases/03-completed-art-ordinary-monsters/03-BIOLOGY.md
  - .planning/phases/03-completed-art-ordinary-monsters/03-DEVIATIONS.md
  - .planning/phases/03-completed-art-ordinary-monsters/03-UAT.md
autonomous: true
requirements: [BIO-01, BIO-02, BIO-03, BIO-06, QUAL-03, QUAL-04]
user_setup: []
estimate:
  tokens: 48000
  raw_tokens: 48000
  tasks: 2
  confidence: low

must_haves:
  truths:
    - "All five `phase:3` rows read `code_complete: true` with a populated `internal_name` resolving to an on-disk class file, and `scripts/check-biology.ps1 -RequireAll` exits 0 — every tranche creature either has its class on disk or is recorded as the pre-existing `RiverSlug` implementation (D-24, QUAL-04)."
    - "Every tranche row's blockers state its real disposition: which designed drops are wired to Phase 1 items, which drops do not exist, which effect parameters are conservative defaults, which spawn predicate is missing, and that localization is deferred (D-20) and runtime verification outstanding (D-21)."
    - "`03-DEVIATIONS.md` is the single consolidated ledger for the phase, carries a coverage claim over all five rows, and records the deferred/unfinished-art/out-of-phase residue without mutating the Feishu source (QUAL-05, QUAL-04)."
    - "The full offline gate chain is green in one run: the Phase 3 gate, the Phase 1 gates and the Phase 2 gate, followed by `dotnet build /p:Configuration=Release /p:WarningLevel=0`, plus the AGENTS.md byte-level UTF-8 BOM check run in the AGENTS.md shape (the phase-scoped BOM invariant 13 is the binding equivalent on this branch)."
    - "`03-UAT.md` records the D-21 client bundle — the per-row spawn, behaviour, combat, drop and dedicated-server checks a live tModLoader client must still perform — so the phase does not present an unverified tranche as verified (D-21)."
    - statement: "In a tModLoader client every one of the five tranche creatures can be encountered in the Kelp Curtain and none of them appears in a normal world."
      verification: backstop
  artifacts:
    - "updated .planning/phases/03-completed-art-ordinary-monsters/03-BIOLOGY.json and 03-BIOLOGY.md (all five tranche rows implemented)"
    - "consolidated .planning/phases/03-completed-art-ordinary-monsters/03-DEVIATIONS.md"
    - ".planning/phases/03-completed-art-ordinary-monsters/03-UAT.md"
  key_links:
    - "03-BIOLOGY.json -> scripts/check-biology.ps1 -RequireAll -> every phase:3 row implemented"
    - "check-biology.ps1 -> Phase 1 gates (check-inventory-reconciliation, check-carryover, check-tranche-A, check-tranche-B, check-armofgianttree-charge) -> Phase 2 gate (check-phase2) -> one green chain"
    - "03-BIOLOGY.json -> 03-BIOLOGY.md -> 03-DEVIATIONS.md coverage claim (row-for-row agreement)"
    - "03-UAT.md -> the five tranche rows' spawn / behaviour / drop / dedicated-server client checks"
  prohibitions:
    - "Never create, move, rename or modify any `.png` or other binary/art asset."
    - "Never edit a Phase 1 or Phase 2 artifact or gate script to make its gate pass; a stale out-of-phase baseline is reported and recorded, never silently bypassed or re-anchored without a written justification."
    - "Never run the in-game localization exporter and never create or edit an HJSON key (D-20)."
    - "Never re-fetch or mutate the Feishu biology document, and never re-run `scripts/parse-design-xml.ps1` (D-25)."
    - "Never promote an absent designed drop into item scope; Phase 3 consumes Phases 1–2 and introduces no item work (D-37/D-43)."
  assumptions:
    - "The Phase 1 and Phase 2 gates are expected to stay green because Phase 3 touches no item, tile, wall, buff or localization artifact; if one fails, the failure is triaged before any ledger entry is written."
    - "The D-21 client bundle is recorded, not executed, because no live tModLoader client session is part of this plan (the Phase 1/2 end-of-phase UAT precedent)."
---

# Phase 3 Plan 04 — Tranche Close-Out: Consolidated Ledger, Full Gate Chain, Build and UAT Bundle

<objective>
Close the phase: reconcile every tranche row against the code that now exists, consolidate the deviation and blocker ledger into one auditable artifact, run the complete offline verification chain plus the Release build, and record the client-side UAT bundle the tranche still needs.

Purpose: this is the phase gate. Plans 03-01…03-03 each proved and recorded one slice; this plan makes the whole phase auditable as a unit, in the shape Phase 4 and Phase 8 will consume. It deliberately adds no gameplay scope — it is the reconciliation, ledger, gate-chain and reporting task that the roadmap's `Phase 3` verification needs (`Completed-art biology matrix; client spawn/combat/capture checks; drop-source checks against Phases 1–2; dedicated-server context checks; dotnet build`).

D-21 (runtime verification is required in addition to the offline gates and the Release build), D-22 (code-complete + art-incomplete marking; the row stays unchecked), D-24 (the matrix is the machine source of truth for Phases 3–4), D-27 (the gate must run green before and after each wave) and QUAL-04 (the per-creature comparison record) are implemented here.

Output: the finalized `03-BIOLOGY.json` / `03-BIOLOGY.md`, the consolidated `03-DEVIATIONS.md`, and `03-UAT.md`.
</objective>

<execution_context>
@C:/Users/book/.config/opencode/gsd-core/workflows/execute-plan.md
@C:/Users/book/.config/opencode/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/STATE.md
@.planning/phases/03-completed-art-ordinary-monsters/03-CONTEXT.md
@.planning/phases/03-completed-art-ordinary-monsters/03-BIOLOGY.json
@.planning/phases/03-completed-art-ordinary-monsters/03-DEVIATIONS.md
@.planning/phases/02-remaining-items-unfinished-art-materials/02-DEVIATIONS.md
@.planning/phases/02-remaining-items-unfinished-art-materials/02-VERIFICATION.md
@.planning/phases/02-remaining-items-unfinished-art-materials/02-UAT.md
@.planning/phases/01-item-inventory-completed-art-items/01-DEVIATIONS.md
@AGENTS.md
</context>

## The Five Tranche Rows At Close-Out

| Row id | Design row | Class | Drops wired | Drops absent |
| --- | --- | --- | --- | --- |
| `bio-death-jade-lake-river-slug` | 水蛞蝓 | `RiverSlug` (pre-existing) | none (design names none) | none |
| `bio-death-jade-lake-verdant-rods` | 叶飞棍 | `VerdantRods` | none | 飞棍毛发, 毒腺 |
| `bio-spiny-moss-court-giant-tree-man` | 巨树人 | `GiantDandelion` | `ArmOfGiantTree`, `HardenedWitherbarkHeart`, `BoulderCatapult` | 枯木碎块 |
| `bio-valley-of-lush-and-moist-mossy-thorn-turtle` | 荆棘苔龟 | `MossyThornTurtle` | `ThornTurtleShell` | none |
| `bio-valley-of-lush-and-moist-guppy-conch` | 格普螺 | `GuppyConch` | `GuppyShell` | 软体甲壳碎片 |

## Artifacts This Plan Produces

- Updated `.planning/phases/03-completed-art-ordinary-monsters/03-BIOLOGY.json` and `03-BIOLOGY.md`
- Consolidated `.planning/phases/03-completed-art-ordinary-monsters/03-DEVIATIONS.md`
- New `.planning/phases/03-completed-art-ordinary-monsters/03-UAT.md`

<tasks>

<task type="auto">
  <name>Task 1: Reconcile all five tranche rows and consolidate the deviation ledger</name>
  <reversibility rating="costly">D-24: this is the frozen Phase 3–4 creature record; Phase 4 and Phase 8 read it as the source of truth.</reversibility>
  <files>.planning/phases/03-completed-art-ordinary-monsters/03-BIOLOGY.json, .planning/phases/03-completed-art-ordinary-monsters/03-BIOLOGY.md, .planning/phases/03-completed-art-ordinary-monsters/03-DEVIATIONS.md</files>
  <precondition>The Wave 3 gate is green and `MossyThornTurtle.cs`, `GuppyConch.cs`, `VerdantRods.cs`, `GiantDandelion.cs` all exist on disk (plans 03-01…03-03).</precondition>
  <read_first>
    - `.planning/phases/03-completed-art-ordinary-monsters/03-BIOLOGY.json` (all 31 rows; the five `phase:3` rows in particular).
    - `.planning/phases/03-completed-art-ordinary-monsters/03-DEVIATIONS.md` (the eight sections from plan 03-01, the 03-02 additions and the 03-03 additions — this task consolidates them into one ledger, mirroring how 02-05 consolidated 02-01…02-04).
    - `.planning/phases/02-remaining-items-unfinished-art-materials/02-DEVIATIONS.md` (the consolidated-ledger shape: header, coverage claim, §1 Scope and Allocation, §2 Blockers table generated mechanically from the machine source of truth, then the deferred/localization sections).
    - `.planning/phases/03-completed-art-ordinary-monsters/scripts/check-biology.ps1` (invariants 2–6, 10, 11 and the BOM invariant 13 that `-RequireAll` must satisfy).
  </read_first>
  <action>
    Finalize `03-BIOLOGY.json`: set `code_complete: true` on all five `phase:3` rows (the first three were flipped during their waves; `RiverSlug` is already true) and confirm that each carries the non-empty `internal_name` its owning wave wrote alongside the flip (`…NPCs.RiverSlug`, `…NPCs.MossyThornTurtle`, `…NPCs.GuppyConch`, `…NPCs.VerdantRods`, `…NPCs.GiantDandelion`) — gate invariant 5 makes `code_complete` and a resolvable `internal_name` a biconditional, so a row that reaches this plan with `code_complete: true` but an empty or stale `internal_name` must be repaired here rather than left for `-RequireAll` to fail on. Replace each row's blocker list with its final disposition, keeping the frozen `counts`, `phase3_tranche`, `texture_complete` and `design_art` values unchanged:
    - `bio-death-jade-lake-river-slug`: the pre-existing-class blocker, restated as `"pre-existing accepted implementation (D-23): class not modified by Phase 3; subworld isolation relies on SpawnModBiomes + KelpCurtainBiome and its HitEffect dust is not Main.dedServ-guarded (gate allowlist)"` plus the runtime/localization marker.
    - the other four rows: their wave-specific drop/effect/spawn blockers as written in plans 03-01…03-03, each followed by `"localization deferred (D-20); runtime verification outstanding (D-21)"`.

    Then regenerate `03-BIOLOGY.md` from the finalized JSON so that the row count (31), the id order, the per-row `status` and the blocker cell text are identical between the two files.

    Consolidate `03-DEVIATIONS.md` into the phase's single ledger, mirroring `02-DEVIATIONS.md`'s structure: keep a header naming the source snapshot, the tranche rule and the machine source of truth, then a **Coverage claim** sentence stating that this ledger covers all five Phase 3 tranche rows and that a tranche row absent from the ledger is a defect. Keep the existing sections in place (Tranche rule; Repository-art vs design-art discrepancy; Mapping assumptions; Conservative defaults; Drop availability; Spawn-region gap; Localization; Runtime verification; Attack projectiles), and add:
      - a **Blockers** table with one row per blocker across the five tranche rows, columns `row id | design name | blocker kind (drop|effect|spawn|artwork|runtime|localization) | exact blocker text | why`, where each **exact blocker text** cell is byte-identical to the corresponding element of that row's `blockers` array in `03-BIOLOGY.json` (generate the table mechanically from the JSON, as plan 02-05 did);
      - a **Phase 3 close-out** block stating the frozen counts (`rows 31`, `phase3 5`, `phase4 23`, `phase7 3`, `deferred 2`), the five implemented tranche rows, the six design-art-only creatures deferred to Phase 4, the one boss row that also has repository art, and the explicit statement that **no item scope was promoted into this phase** (D-37/D-43) and that Phase 3's drop obligations are satisfied by wiring five Phase 1 item types and recording five absent materials as blockers;
      - a **Decision disposition** short table recording which CONTEXT decisions applied and which did not: D-28 applied to all five rows; **D-29 is not triggered in this phase** because every repository-art creature has a defined behaviour row, so no identity-only shell class was needed (the empty-design-section Phase 4 rows are the shell candidates and are labelled `D-29 shell` in the matrix); D-30 applied only to 巨树人's missing Spiny Moss Court swamp predicate; D-31's vanilla-`aiStyle` preference was honoured only by 荆棘苔龟 (`CloneDefaults(NPCID.GiantTortoise)`), with a local `AI()` justified and recorded for 格普螺, 叶飞棍 and 巨树人;
      - a **Deferred** table mirroring the Phase 1/2 registry: unfinished-art creatures (Phase 4), boss/special encounters (Phase 7), the two hardmode-deferred designs (枯萎之种, 枯木人卫士), localization (D-20) and live runtime verification (D-21).

    Run `scripts/check-biology.ps1 -RequireAll` and confirm it exits 0 with the tranche fully implemented.

    Do not create or modify any `.png`, do not edit an HJSON file, and do not edit any Phase 1 or Phase 2 artifact.
  </action>
  <verify>
    <automated>powershell -NoProfile -ExecutionPolicy Bypass -File ".planning/phases/03-completed-art-ordinary-monsters/scripts/check-biology.ps1" -RequireAll</automated>
    <fails_when>non-zero exit — the gate printing `FAIL(`, exiting 2, not printing `OK(0): phase3 tranche = 5 / 5 (rows=31)` followed by `OK: implemented classes = 5 / 5`, reporting a require-all failure for a `phase == 3` row that is not `code_complete`, or reporting a BOM (invariant 13).</fails_when>
  </verify>
  <acceptance_criteria>
    - All five `phase:3` rows read `code_complete: true` with a non-empty `internal_name` resolving to an on-disk class file; `03-BIOLOGY.json` still has 31 rows, the same `phase3_tranche`, the same six-id `texture_complete` set and the same nine-id `design_art` set.
    - `03-BIOLOGY.md` has 31 rows whose ids, statuses and blocker cells match the JSON exactly.
    - `03-DEVIATIONS.md` carries the coverage claim, the mechanically generated Blockers table whose text is identical to the JSON, the Phase 3 close-out counts, and the Deferred table.
    - `check-biology.ps1 -RequireAll` exits 0 and prints `OK(0): phase3 tranche = 5 / 5 (rows=31)` and `OK: implemented classes = 5 / 5`.
    - No `.png`, HJSON file, Feishu document or Phase 1/2 artifact was created or modified.
  </acceptance_criteria>
  <done>Every tranche row is reconciled with its implemented class and its final blocker text, and the phase has one consolidated, machine-consistent ledger with a coverage claim.</done>
  <commit>docs(03-04): reconcile tranche rows and consolidate the phase leader</commit>
</task>

<task type="auto">
  <name>Task 2: Full offline gate chain, Release build and the D-21 client UAT bundle</name>
  <reversibility rating="reversible">Reporting task: it records evidence and adds no gameplay state.</reversibility>
  <files>.planning/phases/03-completed-art-ordinary-monsters/03-UAT.md, .planning/phases/03-completed-art-ordinary-monsters/03-DEVIATIONS.md</files>
  <precondition>Task 1 is complete and `check-biology.ps1 -RequireAll` is green.</precondition>
  <read_first>
    - `.planning/phases/02-remaining-items-unfinished-art-materials/02-UAT.md` (the end-of-phase client-bundle format this phase mirrors).
    - `.planning/phases/02-remaining-items-unfinished-art-materials/02-VERIFICATION.md` (the phase verification-report format, including how outstanding runtime checks are stated).
    - `.planning/phases/03-completed-art-ordinary-monsters/03-DEVIATIONS.md` (the consolidated ledger from Task 1; §8 is where the chain results and any triage are recorded).
    - `.planning/phases/03-completed-art-ordinary-monsters/03-RESEARCH.md` §Validation Architecture (the phase requirement-to-command map) and §Security Domain.
    - `.planning/phases/01-item-inventory-completed-art-items/scripts/check-inventory-reconciliation.ps1`, `check-carryover.ps1`, `check-tranche-A.ps1`, `check-tranche-B.ps1`, `check-armofgianttree-charge.ps1` and `.planning/phases/02-remaining-items-unfinished-art-materials/scripts/check-phase2.ps1` (the earlier gates to re-run unchanged).
    - `AGENTS.md` §Tests and Verification (the byte-level UTF-8 BOM check snippet, its `git merge-base HEAD origin/master` anchor, and the Phase 1 `01-07-SUMMARY.md` deviation 2 / Phase 2 `02-05-SUMMARY.md` re-anchor precedent that makes that anchor non-isolating on this long-lived branch).
  </read_first>
  <action>
    Run the complete offline chain in one command and record the verbatim result lines: `dotnet build /p:Configuration=Release /p:WarningLevel=0`, then `check-biology.ps1 -RequireAll`, then the Phase 1 gates (`check-inventory-reconciliation.ps1`, `check-carryover.ps1`, `check-tranche-A.ps1`, `check-tranche-B.ps1`, `check-armofgianttree-charge.ps1`) and the Phase 2 gate (`check-phase2.ps1`), each with `powershell -NoProfile -ExecutionPolicy Bypass -File`.

    Triage rule for an earlier-phase failure: Phase 3 adds no item, tile, wall, buff or localization artifact, so an earlier gate is expected to stay green. If one fails, first determine whether Phase 3 caused it (for example a gate that scans the whole working tree for added binaries or BOMs). A Phase 3-caused failure is fixed inside this phase's own files. A failure caused by a stale out-of-phase baseline anchor is reported to the user with the exact failing line and the reason the baseline is stale, and is recorded in `03-DEVIATIONS.md` §8 as an outstanding item; it is never fixed by editing the earlier phase's gate or artifacts (the 02-05 re-anchoring precedent needed an explicit written justification).

    Then run the AGENTS.md byte-level UTF-8 BOM check exactly in the shape AGENTS.md writes it: resolve `$base = git merge-base HEAD origin/master`, assemble `$files` from `git -c core.quotepath=false diff --name-only --diff-filter=ACMRTUXB $base --` plus `git -c core.quotepath=false ls-files --others --exclude-standard`, and fail on any file whose first three bytes read through `[IO.File]::ReadAllBytes` are `0xEF 0xBB 0xBF`. Record its result line (`UTF-8 BOM check passed (N files).`) verbatim in `03-DEVIATIONS.md` §8 and state the anchor it used. Two facts about that anchor belong in the same §8 entry: (a) it is the whole branch measured against a distant pre-phase ancestor (at planning time 1360 changed/untracked files, 0 BOM), so it is **advisory and non-isolating** — the binding, phase-scoped BOM gate is invariant 13 inside `check-biology.ps1`, which the chain already ran first over the phase's own change set; and (b) if the `origin/master`-anchored run ever reports a BOM in a path outside Phase 3's change set, that is a stale-anchor finding to record with the offending path — nothing is edited to silence it, following Phase 1 `01-07-SUMMARY.md` deviation 2 and Phase 2 `02-05-SUMMARY.md`'s re-anchor.

    Create `.planning/phases/03-completed-art-ordinary-monsters/03-UAT.md` as the D-21 client bundle, mirroring `02-UAT.md`'s format. It records, per tranche row, the client checks that cannot be observed offline:
      - spawn: the creature appears in the Kelp Curtain in its designed context (荆棘苔龟 and 格普螺 on land; 叶飞棍 in open air; 巨树人 on land in the Spiny Moss Court area; 水蛞蝓 on the surface);
      - isolation: none of the five appears in an ordinary world after several minutes of idling there (BIO-06);
      - behaviour: the row-specific behaviour read from `03-BIOLOGY.json`'s design section (荆棘苔龟 spin with its 防御 10/20 and 伤害 50/75 spin-state switch plus melee reflect; 格普螺 slow crawl + shell retract with reduced incoming damage; 叶飞棍 hover, water suffocation and 50% poison; 巨树人 three-range state machine, ground shockwave, thrown boulder, post-smash vulnerability; 水蛞蝓 crawl, capture and fishing-power bait);
      - combat: contact damage values and the immunity cells behave as documented;
      - drops: killing the creature can produce the wired Phase 1 items (荆棘龟壳, 格普螺外壳, 巨树之臂, 硬化枯木心脏, 巨石弹射装置) at the documented chances;
      - dedicated server and multiplayer: the tranche spawns and fights on a dedicated server, and the two attack projectiles are not duplicated or client-spawned (QUAL-03);
      - localization: the display names fall back cleanly and are recorded as deferred (D-20), with no key fabricated.
    Mark every entry as not yet executed, and state in the file's header that the bundle is recorded but not run, so the tranche is not presented as client-verified.

    Append the chain results and the triage outcome to `03-DEVIATIONS.md` §8, and add the phase close-out statement to the Deferred table if any item remains open.

    Do not create or modify any `.png`, do not edit an HJSON file, and do not launch the game as part of this task.
  </action>
  <verify>
    <automated>dotnet build /p:Configuration=Release /p:WarningLevel=0; if ($?) { powershell -NoProfile -ExecutionPolicy Bypass -File ".planning/phases/03-completed-art-ordinary-monsters/scripts/check-biology.ps1" -RequireAll }; if ($?) { powershell -NoProfile -ExecutionPolicy Bypass -File ".planning/phases/01-item-inventory-completed-art-items/scripts/check-inventory-reconciliation.ps1" }; if ($?) { powershell -NoProfile -ExecutionPolicy Bypass -File ".planning/phases/01-item-inventory-completed-art-items/scripts/check-carryover.ps1" }; if ($?) { powershell -NoProfile -ExecutionPolicy Bypass -File ".planning/phases/01-item-inventory-completed-art-items/scripts/check-tranche-A.ps1" }; if ($?) { powershell -NoProfile -ExecutionPolicy Bypass -File ".planning/phases/01-item-inventory-completed-art-items/scripts/check-tranche-B.ps1" }; if ($?) { powershell -NoProfile -ExecutionPolicy Bypass -File ".planning/phases/01-item-inventory-completed-art-items/scripts/check-armofgianttree-charge.ps1" }; if ($?) { powershell -NoProfile -ExecutionPolicy Bypass -File ".planning/phases/02-remaining-items-unfinished-art-materials/scripts/check-phase2.ps1" }; if ($?) { $base = git merge-base HEAD origin/master; if (-not $?) { throw 'git merge-base failed' }; $diffNames = git -c core.quotepath=false diff --name-only --diff-filter=ACMRTUXB $base --; if (-not $?) { throw 'git diff failed' }; $otherNames = git -c core.quotepath=false ls-files --others --exclude-standard; if (-not $?) { throw 'git ls-files failed' }; $files = @($diffNames + $otherNames) | Sort-Object -Unique; $bom = @($files | Where-Object { if (-not (Test-Path -LiteralPath $_ -PathType Leaf)) { return $false }; $b = [IO.File]::ReadAllBytes($_); $b.Length -ge 3 -and $b[0] -eq 0xEF -and $b[1] -eq 0xBB -and $b[2] -eq 0xBF }); if ($bom) { throw "UTF-8 BOM: $($bom -join ', ')" }; Write-Output "UTF-8 BOM check passed ($($files.Count) files)."; Write-Output 'verify chain OK(0)' }</automated>
    <fails_when>non-zero exit at any link in the chain — an `error CS` in the Release build, or any gate printing `FAIL(` / exiting 2 / not printing its success line (the Phase 3 gate's success line is `OK(0): phase3 tranche = 5 / 5 (rows=31)` followed by `OK: implemented classes = 5 / 5`), or the AGENTS.md byte-level BOM block throwing `UTF-8 BOM:`, unless the exact failure is triaged as a stale out-of-phase baseline and recorded in `03-DEVIATIONS.md` §8 with the failing line quoted.</fails_when>
    <human-check>
      <test>Run the `.planning/phases/03-completed-art-ordinary-monsters/03-UAT.md` bundle in a tModLoader client, including one dedicated-server launch and one ordinary-world idle run.</test>
      <expected>Every tranche creature spawns and behaves as recorded, none appears in the ordinary world, the wired drops are obtainable, and no missing-texture or disabled-mod entry appears in the tML log.</expected>
      <why_human>Spawn isolation, AI feel, drop acquisition and dedicated-server behaviour are runtime properties; no offline script observes them (D-21).</why_human>
    </human-check>
  </verify>
  <acceptance_criteria>
    - The full chain ran in one command and every link exited 0, or the single exception is quoted with its failing line and justification in `03-DEVIATIONS.md` §8.
    - The AGENTS.md byte-level UTF-8 BOM check ran in the AGENTS.md shape (`git merge-base HEAD origin/master` anchor plus the `ReadAllBytes` / `0xEF 0xBB 0xBF` filter), reported no BOM-prefixed file, and its result line and anchor are recorded in `03-DEVIATIONS.md` §8 alongside the note that the phase-scoped invariant 13 is the binding BOM gate on this branch.
    - `03-UAT.md` exists with per-row spawn, isolation, behaviour, combat, drop, dedicated-server/multiplayer and localization entries, every one marked not yet executed, and a header stating the bundle is recorded but not run.
    - `03-DEVIATIONS.md` §8 contains the chain result lines and the triage outcome; the Deferred table lists any remaining open item.
    - No `.png`, HJSON file or Feishu document was created or modified, and the game was not launched by this task.
  </acceptance_criteria>
  <done>The complete offline chain (Release build plus the Phase 1, 2 and 3 gates) is green in one run and recorded, and the outstanding client-side tranche verification is captured in a per-row UAT bundle instead of being presented as done.</done>
  <commit>docs(03-04): full phase gate chain, Release build and D-21 client UAT bundle</commit>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Matrix JSON -> ledger and mirror | The ledger's blocker text is generated mechanically from the JSON so the two cannot disagree. |
| Phase 3 changes -> earlier phases' gates | Phase 3 adds only creature classes and planning artifacts, so the Phase 1/2 gates must stay green; a stale baseline is reported, never bypassed. |
| Recorded verification -> presented verification | The UAT bundle is explicitly marked not executed so unrun client checks are not mistaken for verified behaviour. |
| Repository -> source snapshot | The Feishu document stays read-only; the phase writes no source status back (that is Phase 8). |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-03-23 | Repudiation | A tranche row presented as complete without an executed client check | high | mitigate | `03-UAT.md` records every runtime check as not yet executed and states that in its header; the matrix keeps every row `status: "unchecked"` (no colour) until Phase 8's source synchronization; the ledger's coverage claim names the five rows so an omission is detectable. |
| T-03-24 | Tampering | Editing a Phase 1/2 gate or artifact to force a green chain | high | mitigate | Prohibition block plus an explicit triage rule: a Phase 3-caused failure is fixed in Phase 3's own files, a stale baseline is reported and recorded in §8, and no earlier-phase file is in this plan's `files_modified`. |
| T-03-25 | Tampering | Ledger/JSON divergence after consolidation | medium | mitigate | The Blockers table is generated mechanically from `03-BIOLOGY.json` and gate invariant 11 re-asserts id-set and per-row status parity between JSON and Markdown on every run. |
| T-03-26 | Tampering | Item scope silently promoted into the monster phase | high | mitigate | The close-out block states explicitly that no item scope was promoted and that the five absent materials stay blockers (D-37/D-43); gate invariant 8 rejects any drop rule whose item type is not on disk. |
| T-03-27 | Denial of Service | A dropped or truncated gate command hiding a failure | low | mitigate | The chain is a single `if ($?)`-chained command whose exit status is the phase's gate; each link's success line is quoted in §8. |
| T-03-SC | Tampering | Package supply chain | low | accept | No package is installed by this plan (03-RESEARCH §Package Legitimacy Audit: not applicable). |
</threat_model>

<verification>
- `dotnet build /p:Configuration=Release /p:WarningLevel=0` exits 0 with no `error CS`.
- `check-biology.ps1 -RequireAll` exits 0 and prints `OK(0): phase3 tranche = 5 / 5 (rows=31)` and `OK: implemented classes = 5 / 5`, with its phase-scoped BOM invariant (13) green.
- The AGENTS.md byte-level UTF-8 BOM check runs in the AGENTS.md shape against `git merge-base HEAD origin/master`, reports no BOM-prefixed file, and its result line, file count and anchor are recorded in `03-DEVIATIONS.md` §8 together with the statement that invariant 13 is the binding phase-scoped equivalent on this branch.
- The Phase 1 gates (`check-inventory-reconciliation`, `check-carryover`, `check-tranche-A`, `check-tranche-B`, `check-armofgianttree-charge`) and the Phase 2 gate (`check-phase2`) all exit 0, or their single exception is quoted and justified in `03-DEVIATIONS.md` §8.
- All five `phase:3` rows read `code_complete: true` with a populated `internal_name` resolving to an on-disk class file and with final blocker text; the JSON still holds 31 rows and the frozen tranche ids, `texture_complete` set and `design_art` set.
- `03-BIOLOGY.md` mirrors the JSON row-for-row; `03-DEVIATIONS.md` holds the coverage claim, the mechanical Blockers table, the close-out counts and the Deferred table.
- `03-UAT.md` exists and marks every client check not yet executed.
- No `.png` or other binary asset was added, moved, renamed or modified; no HJSON was edited; the Feishu document is untouched; no Phase 1/2 artifact was edited.
</verification>

<success_criteria>
- The Phase 3 tranche is fully reconciled: five rows implemented, five Phase 1 item types wired, five absent materials blocked, and every effect/spawn gap recorded (D-24, D-37/D-43, QUAL-04).
- One consolidated ledger and one mirror agree byte-for-byte with the machine source of truth, and the phase's deferred residue is explicit.
- The complete offline chain and the Release build are green in a single run and recorded.
- The out-of-reach client and dedicated-server checks are captured as an explicit, unexecuted UAT bundle rather than assumed (D-21).
</success_criteria>

<output>
Create `.planning/phases/03-completed-art-ordinary-monsters/03-04-SUMMARY.md` when done.
</output>
