---
phase: 04-remaining-ordinary-monsters
plan: 09
type: execute
wave: 4
depends_on: [04-02, 04-03, 04-04, 04-05, 04-06, 04-07, 04-08]
files_modified:
  - .planning/phases/03-completed-art-ordinary-monsters/03-BIOLOGY.json
  - .planning/phases/03-completed-art-ordinary-monsters/03-BIOLOGY.md
  - .planning/phases/04-remaining-ordinary-monsters/04-DEVIATIONS.md
  - .planning/phases/04-remaining-ordinary-monsters/04-UAT.md
  - .planning/phases/04-remaining-ordinary-monsters/04-VALIDATION.md
autonomous: true
requirements: [BIO-01, BIO-02, BIO-03]
user_setup: []
estimate:
  tokens: 52000
  raw_tokens: 52000
  tasks: 2
  confidence: low

must_haves:
  truths:
    - "All 21 in-scope `phase:4` rows read `code_complete: true` with a populated `internal_name` resolving to an on-disk class under `Sources/Modules/Yggdrasil/KelpCurtain/NPCs`, and `scripts/check-biology.ps1 -RequireAll` exits 0 — every in-scope creature has its code on disk and the matrix is never ahead of the code it describes (D-24, QUAL-04)."
    - "Every in-scope row's blockers state its real disposition: the missing artwork with the `Commons.ModAsset.White_Mod` marker, the behaviour-undefined marker on the three D-45 shells, the absent designed drops and the absent capture items, the unimplemented morale/command, disguise and egg systems, the missing region predicates, and the canonical `localization deferred (D-20); runtime verification outstanding (D-21)` tail (D-45/D-46/D-48/D-52/D-58)."
    - "The frozen `counts` block (rows 31 / phase3 5 / phase4 23 / phase7 3 / deferred 2 / texture_complete_true 6 / design_art_true 9), the 5-id `phase3_tranche` set, the 6-id `texture_complete` set and the 9-id `design_art` set are unchanged, the two hardmode rows stay `deferred: true`, and every row's `status` stays `unchecked` (no Feishu writes; Phase 8 owns synchronization)."
    - "`04-DEVIATIONS.md` becomes the phase's single consolidated ledger: the open sections are completed with a mechanically generated Blockers table whose every `exact blocker text` cell is byte-identical to the corresponding element of `03-BIOLOGY.json`, a Phase 4 close-out counts block, a decision disposition table and a deferred registry."
    - "The full offline chain is green in one run: the Release build, the Phase 4 gate with `-RequireAll`, the Phase 3 gate with `-RequireAll`, the five Phase 1 gates, the Phase 2 gate and `dotnet test --filter \"FullyQualifiedName~Yggdrasil\"`, plus the AGENTS.md byte-level UTF-8 BOM check in the AGENTS.md shape (the phase-scoped invariant 13 of the Phase 4 gate is the binding equivalent on this branch)."
    - "`04-UAT.md` records the D-21 client bundle — per-creature spawn, isolation, behaviour, combat, drop and dedicated-server checks that a live tModLoader client must still perform — with every entry marked not executed, so the phase does not present an unverified tranche as verified (D-21)."
    - statement: "In a tModLoader client all 21 in-scope creatures can be encountered in the Kelp Curtain in their designed bands and none of them appears in a normal world."
      verification: backstop
  artifacts:
    - "updated .planning/phases/03-completed-art-ordinary-monsters/03-BIOLOGY.json and 03-BIOLOGY.md (all 21 in-scope rows reconciled)"
    - "consolidated .planning/phases/04-remaining-ordinary-monsters/04-DEVIATIONS.md"
    - ".planning/phases/04-remaining-ordinary-monsters/04-UAT.md"
  key_links:
    - "03-BIOLOGY.json -> scripts/check-biology.ps1 -RequireAll -> every in-scope row implemented with a resolving internal_name"
    - "03-BIOLOGY.json -> 03-BIOLOGY.md -> row-for-row id and status parity (the Phase 4 gate's invariant 11)"
    - "04-DEVIATIONS.md Blockers table -> the JSON `blockers` arrays -> byte-identical text, so the ledger cannot drift from the matrix"
    - "the Phase 4 gate -> the Phase 3 gate -> the five Phase 1 gates -> the Phase 2 gate -> the Yggdrasil MSTest link -> one green chain"
    - "04-UAT.md -> the 21 in-scope rows' spawn / isolation / behaviour / drop / dedicated-server checks"
  prohibitions:
    - "Never create, move, rename or modify any `.png`/`.obj`/`.xnb` or other binary/art asset (D-51, AGENTS.md)."
    - "Never edit the Phase 3 gate `.planning/phases/03-completed-art-ordinary-monsters/scripts/check-biology.ps1` (OQ3), and never edit a Phase 1 or Phase 2 artifact or gate script to make its gate pass."
    - "Never change the frozen `counts`, `phase3_tranche`, `texture_complete` or `design_art` values, never promote a Phase 4 row to `green`/`yellow`, and never write a Feishu status (D-22/D-25; that is Phase 8)."
    - "Never promote an absent designed drop into item scope; Phase 4 consumes Phases 1–2 and introduces no item work (D-57/D-58)."
    - "Never run the in-game localization exporter and never create or edit an HJSON key (D-20)."
    - "Never re-fetch or mutate the Feishu biology document, and never re-run `scripts/parse-design-xml.ps1` (D-25)."
  assumptions:
    - "The older-phase gates are expected to stay green because Phase 4 adds only creature classes, projectiles and planning artifacts; if one fails, the failure is triaged before any ledger entry is written (the Phase 3 close-out rule)."
    - "The D-21 client bundle is recorded, not executed, because no live tModLoader client session is part of this plan (the Phase 1/2/3 end-of-phase UAT precedent)."
    - "The 26 classes across the 21 design rows are the `internal_name`-plus-siblings set recorded in `04-DEVIATIONS.md` §2; the matrix names one class per row and the ledger names the siblings."
---

# Phase 4 Plan 09 — Phase Close-Out: Matrix Reconciliation, Consolidated Ledger, Full Chain and the D-21 Client Bundle

## User Story

**As a** Kelp Curtain owner, **I want** the phase to end with every creature's record matching the code that exists, one auditable ledger of what was and was not built, and a single green verification run, **so that** I can say exactly what shipped and what is still owed.

> Planner-derived from the ROADMAP Phase 4 `Goal` line; the roadmap goal is not authored in user-story form and this restates it without adding scope (the Phase 1/2/3 plan precedent).

<objective>
Close the phase: reconcile all 21 in-scope rows against the classes that now exist, consolidate the deviation and blocker ledger into one auditable artifact, run the complete offline chain plus the Release build, and record the client-side UAT bundle the tranche still needs.

Purpose: this is the phase gate. Waves 1–3 each created classes but deliberately never touched the shared matrix, so the matrix is still describing 20 rows as unimplemented; this plan is where the machine record catches up, in one place, with the `-RequireAll` gate as the proof. It also deliberately adds no gameplay scope: it is the reconciliation, ledger, chain and reporting work the roadmap's Phase 4 verification needs (`Remaining biology matrix; client spawn/behavior/combat checks; drop and progression integration checks; dedicated-server safety; dotnet build; unresolved-source audit`).

D-21 (runtime verification required in addition to the offline gates and the Release build), D-22/D-24/D-25 (marking rules, the shared matrix, the read-only snapshot) and QUAL-04 (the per-creature comparison record) are implemented here.

Output: the finalized `03-BIOLOGY.json` / `03-BIOLOGY.md`, the consolidated `04-DEVIATIONS.md`, and `04-UAT.md`.
</objective>

<execution_context>
@C:/Users/book/.config/opencode/gsd-core/workflows/execute-plan.md
@C:/Users/book/.config/opencode/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/STATE.md
@.planning/phases/04-remaining-ordinary-monsters/04-CONTEXT.md
@.planning/phases/04-remaining-ordinary-monsters/04-RESEARCH.md
@.planning/phases/04-remaining-ordinary-monsters/04-VALIDATION.md
@.planning/phases/04-remaining-ordinary-monsters/04-DEVIATIONS.md
@.planning/phases/04-remaining-ordinary-monsters/scripts/check-biology.ps1
@.planning/phases/03-completed-art-ordinary-monsters/03-BIOLOGY.json
@.planning/phases/03-completed-art-ordinary-monsters/03-DEVIATIONS.md
@.planning/phases/03-completed-art-ordinary-monsters/03-UAT.md
@.planning/phases/02-remaining-items-unfinished-art-materials/02-DEVIATIONS.md
@.planning/phases/02-remaining-items-unfinished-art-materials/02-UAT.md
@AGENTS.md
</context>

## The 21 Rows At Close-Out

| # | Row id | Design row | `internal_name` | Sibling classes | Wired drops | Blocked drops |
| --- | --- | --- | --- | --- | --- | --- |
| 1 | `bio-death-jade-lake-fluorescent-hydra` | 荧光水螅 | `…NPCs.DeathJadeLake.FluorescentHydra` | — | none | none (D-45 shell) |
| 2 | `bio-death-jade-lake-giant-tiger-shrimp` | 巨型虎虾 | `…DeathJadeLake.GiantTigerShrimp` | — | none | none (D-45 shell) |
| 3 | `bio-death-jade-lake-water-strider` | 水黾 | `…DeathJadeLake.WaterStrider` | — | none | none |
| 4 | `bio-death-jade-lake-toxic-toad` | 剧毒蟾蜍 | `…DeathJadeLake.ToxicToad` | — | none | 毒腺 (33%), 牛黄 (1%/2%) |
| 5 | `bio-death-jade-lake-glow-salamander` | 幽光蝾螈 | `…DeathJadeLake.GlowSalamander` | — | none | 毒腺, 牛黄 |
| 6 | `bio-death-jade-lake-armored-shrimp` | 装甲虾 | `…DeathJadeLake.ArmoredShrimp` | — | none | 软体甲壳碎片 (1); capture item |
| 7 | `bio-death-jade-lake-bomb-jellyfish` | 爆弹水母 | `…DeathJadeLake.BombJellyfish` | `LargeBombJellyfish` | none | capture item |
| 8 | `bio-death-jade-lake-sailfin-snakehead` | 帆鳍鳢 | `…DeathJadeLake.SailfinSnakehead` | — | none | 亡碧膏 (33%) |
| 9 | `bio-death-jade-lake-radiolarian` | 放射虫 | `…DeathJadeLake.Radiolarian` | — | `RadialCarapace` (6.7%) | 软体甲壳碎片 (2–5), 亡碧膏 (1–2) |
| 10 | `bio-death-jade-lake-algae-octopus` | 覆藻章鱼 | `…DeathJadeLake.AlgaeOctopus` | — | none | 软体甲壳碎片 (1), 亡碧膏 (1) |
| 11 | `bio-death-jade-lake-large-algae-octopus` | 大型覆藻章鱼 | `…DeathJadeLake.LargeAlgaeOctopus` | — | none | 软体甲壳碎片 (2–4), 亡碧膏 (3–6), 武器/饰品 TBD |
| 12 | `bio-death-jade-lake-jade-anglerfish` | 碧灵鮟鱇 | `…DeathJadeLake.JadeSpiritAnglerfish` | — | `MeatLantern` (25%), `Photophore` (12.5%) | none |
| 13 | `bio-death-jade-lake-cannon-barnacle` | 炮弹藤壶 | `…DeathJadeLake.CannonBarnacle` | — | none | none (D-45 shell) |
| 14 | `bio-spiny-moss-court-withered-soldier` | 枯木活化士兵 | `…SpinyMossCourt.AnimatedWitherbarkSoldier` | `AnimatedWitherbarkSoldierRanged`, `AnimatedWitherbarkSoldierSpell`, `AnimatedWitherbarkHound` | `ActivatedDogStaff` (9%, 犬) | 枯木碎块 (1–2 / 犬 1), 干涸心脏 (morale-gated 50%) |
| 15 | `bio-spiny-moss-court-court-commander` | 王庭号令者 | `…SpinyMossCourt.CourtCommander` | — | none | 枯木碎块 (2–4), 干涸心脏 (50%) |
| 16 | `bio-spiny-moss-court-brodie-flydragon` | 布罗迪蝇蜓 | `…SpinyMossCourt.BrodieFlydragon` | `SmallBrodieFlydragon` | none | none |
| 17 | `bio-valley-of-lush-and-moist-red-needle-caterpillar` | 红针洋辣子 | `…ValleyOfLushAndMoist.RedNeedleCaterpillar` | — | `CaterpillarJuice` (100%, 1–2) | none |
| 18 | `bio-valley-of-lush-and-moist-assassin-raspberry` | 阿萨辛覆盘子 | `…ValleyOfLushAndMoist.AssassinRaspberry` | — | none | none |
| 19 | `bio-valley-of-lush-and-moist-serpent-moss` | 蛇行苔 | `…ValleyOfLushAndMoist.SerpentMoss` | — | none | none |
| 20 | `bio-valley-of-lush-and-moist-small-guppy-conch` | 小格普螺 | `…ValleyOfLushAndMoist.SmallGuppyConch` | — | none | capture item |
| 21 | `bio-valley-of-lush-and-moist-large-mossy-thorn-turtle` | 大型荆棘苔龟 | `…ValleyOfLushAndMoist.LargeMossyThornTurtle` | — | none | design drop list TBD |

`…` expands to `Everglow.Yggdrasil.KelpCurtain.`.

## Artifacts This Plan Produces

- Updated `.planning/phases/03-completed-art-ordinary-monsters/03-BIOLOGY.json` and `03-BIOLOGY.md` (21 in-scope rows reconciled)
- Consolidated `.planning/phases/04-remaining-ordinary-monsters/04-DEVIATIONS.md`
- New `.planning/phases/04-remaining-ordinary-monsters/04-UAT.md`

<tasks>

<task type="auto">
  <name>Task 1: Reconcile all 21 in-scope rows and consolidate the deviation and blocker ledger</name>
  <reversibility rating="costly">D-24: this is the shared Phase 3–4 creature record; it is the input to Phase 8's source synchronization and to Phase 5–6's regional work.</reversibility>
  <files>.planning/phases/03-completed-art-ordinary-monsters/03-BIOLOGY.json, .planning/phases/03-completed-art-ordinary-monsters/03-BIOLOGY.md, .planning/phases/04-remaining-ordinary-monsters/04-DEVIATIONS.md</files>
  <precondition>The Wave 3 gate is green and every class named in this plan's §The 21 Rows At Close-Out exists on disk (plans 04-02…04-08).</precondition>
  <read_first>
    - `.planning/phases/03-completed-art-ordinary-monsters/03-BIOLOGY.json` (all 31 rows; the 21 `phase:4` rows and their current blockers; the frozen `counts`, `phase3_tranche`, `texture_complete` and `design_art` values).
    - `.planning/phases/04-remaining-ordinary-monsters/04-DEVIATIONS.md` (the ledger opened by plan 04-01, with §11 reserved for this task's blocker register).
    - `.planning/phases/03-completed-art-ordinary-monsters/03-DEVIATIONS.md` (§10's Blockers table is the mechanical from-JSON shape to mirror, and §11–§14 are the close-out/counts/disposition/deferred shapes to mirror).
    - `.planning/phases/02-remaining-items-unfinished-art-materials/02-DEVIATIONS.md` (the consolidated-ledger shape: header, coverage claim, then the mechanically generated Blockers table).
    - `.planning/phases/04-remaining-ordinary-monsters/scripts/check-biology.ps1` (invariants 2–7, 10 and 11 — especially the matrix-wide `internal_name`-resolves requirement and the artwork-blocker requirement that `-RequireAll` must satisfy).
  </read_first>
  <action>
    Finalize `03-BIOLOGY.json` by editing the 21 in-scope `phase:4` rows only. For each row set `code_complete: true` and populate `internal_name` with the full namespace-qualified class name from this plan's §The 21 Rows At Close-Out, then replace its `blockers` array with the row's final disposition. Every one of the 21 arrays must keep the D-48 artwork element `artwork: approved texture (贴图) missing from repository; class uses Commons.ModAsset.White_Mod (D-48)` and end with the canonical tail `localization deferred (D-20); runtime verification outstanding (D-21)`; between them place, as separate elements: the D-45 marker `behavior undefined in the design row (D-29/D-45 shell)` on rows 1, 2 and 13; one `drop: <design drop name> not implemented (item scope; D-58)` element per blocked drop listed in this plan's table; one `capture: catch item not implemented (item scope; D-58)` element on 装甲虾, 爆弹水母 and 小格普螺; the morale/command-system element on the two Spiny Moss Court soldier rows and a `summoned-soldier morale bonus not applied` element on 王庭号令者 (`system: Spiny Moss Court morale/command system not implemented (D-46)`); the Valley egg-system element on 布罗迪蝇蜓 (`system: Valley egg-laying system not implemented (D-46)`); the disguised-hazard element on the two Valley hazard rows (`system: Valley disguised-hazard visual system not implemented (D-46)`); the region element naming 森雨幽谷 on 覆藻章鱼 and 刺苔庭园/森雨幽谷 on 布罗迪蝇蜓 (`region: 森雨幽谷 / 刺苔庭园 spawn predicate not implemented (Phases 5-6; D-52)`); the lingering-render element on 覆藻章鱼 and 大型覆藻章鱼 (`effect: ink-cloud lingering rendering not implemented (OQ4/D-46)`); the effect blocker elements for the 水黾 surface tuning and the 大型荆棘苔龟 state-3 presentation only if `04-DEVIATIONS.md` §7 records them; and, on 红针洋辣子, the OQ1 element `drop: CaterpillarJuice wired from YggdrasilTown (pre-existing implemented item; OQ1, D-57)`. Name the sibling classes inside the owning row's blockers (the matrix schema carries one `internal_name` per row), so the record of `LargeBombJellyfish`, `SmallBrodieFlydragon`, `AnimatedWitherbarkSoldierRanged`, `AnimatedWitherbarkSoldierSpell` and `AnimatedWitherbarkHound` is not lost. Do not change any row's `status` (every row stays `unchecked`), `design_art`, `texture_complete`, `phase`, `deferred`, `deferred_reason`, `feishu` block ids or `repo_asset`; do not change the frozen `counts`, `phase3_tranche`, `source`, `tranche_rule` or `naming_rule`; and do not touch the five `phase:3` rows, the three `phase:7` rows or the two deferred rows.

    Regenerate `03-BIOLOGY.md` from the finalized JSON so its 31 rows, ids, order, per-row `status` and blocker cell text are identical to the JSON.

    Consolidate `04-DEVIATIONS.md`: keep the header, the coverage claim and §1–§13 exactly as plan 04-01 wrote them (extending any section whose facts changed during waves 2–3), and complete it with:
    - **§11 Blockers** — a table with one row per blocker across the 21 in-scope rows, with columns `row id | design name | blocker kind (artwork|behavior|drop|capture|system|region|effect|runtime|localization) | exact blocker text | why`, where every `exact blocker text` cell is byte-identical to the corresponding element of that row's `blockers` array in `03-BIOLOGY.json`. **Generate this table mechanically from the JSON** (a short script that reads the JSON with `[IO.File]::ReadAllText` and writes the rows is acceptable and preferred), so the ledger cannot drift from the matrix (the Phase 3 T-03-25 mitigation).
    - **§12 Phase 4 close-out** — the frozen counts (`rows 31`, `phase3 5`, `phase4 23`, `phase7 3`, `deferred 2`), the 21 implemented in-scope rows, the 26 classes those rows map to, the two deferred hardmode rows, the four wired Phase 1 drop types (`ThornTurtleShell` is Phase 3's; Phase 4's are `RadialCarapace`, `MeatLantern`, `Photophore`, `ActivatedDogStaff`) plus the cross-namespace `CaterpillarJuice`, the complete absent-drop register (毒腺, 牛黄, 软体甲壳碎片, 亡碧膏, 枯木碎块, 干涸心脏, and 大型荆棘苔龟's TBD list), the three absent capture items, the three unimplemented systems (morale/command, Valley disguised hazards, Valley egg laying), the region-predicate gap, the projectile set this phase built and the effect blockers it recorded, and the explicit statement that **no item scope was promoted into this phase** (D-57/D-58) and that Phase 4's obligations are satisfied by referencing Phase 1–2 implementations.
    - **§13 Decision disposition** — a short table recording which CONTEXT decisions applied and which did not: D-44 applied (the 21-row set); D-45 applied to three rows (the identity shells); D-46 applied to five rows (the two Spiny Moss Court rows, the two Valley hazard rows and the flydragon); D-47 applied to the soldier group and the mini-boss (full implementations) with the mini-boss call recorded as full; D-48/D-49/D-50/D-51 applied to every class; D-52 applied with the region gap recorded; D-53 applied (no class depends on a Phase 5–6 tile); D-54/D-59 applied with every default entered in §10; D-55 applied; D-56/D-57/D-58 applied with the OQ1 exception documented in §6.1; D-60 informational.
    - **§14 Deferred registry** — a table mirroring the Phase 1/2/3 registries: the two hardmode designs (枯萎之种, 枯木人卫士), boss/special encounters (Klein Snake, Giant Winged Dragon, 吸血魔毯), region-level spawn predicates (Phases 5–6), the three unimplemented systems, the absent drop materials and capture items (item scope), localization (D-20) and live runtime verification (D-21).
    - **§15 Client verification** — a one-line pointer to `04-UAT.md`, which Task 2 creates.

    Run `scripts/check-biology.ps1 -RequireAll` and confirm it exits 0 with all 21 rows reconciled. Then run `.planning/phases/03-completed-art-ordinary-monsters/scripts/check-biology.ps1 -RequireAll` and confirm it still exits 0 with `OK(0): phase3 tranche = 5 / 5 (rows=31)` and `OK: implemented classes = 5 / 5` (the Phase 3 chain must still reproduce — OQ3).

    Do not create or modify any `.png`, do not edit an HJSON file, and do not edit any Phase 1 or Phase 2 artifact or the Phase 3 gate script.
  </action>
  <verify>
    <automated>powershell -NoProfile -ExecutionPolicy Bypass -File ".planning/phases/04-remaining-ordinary-monsters/scripts/check-biology.ps1" -RequireAll; if ($?) { powershell -NoProfile -ExecutionPolicy Bypass -File ".planning/phases/03-completed-art-ordinary-monsters/scripts/check-biology.ps1" -RequireAll }</automated>
    <fails_when>non-zero exit — the Phase 4 gate printing `FAIL(`, exiting 2, reporting `UTF-8 BOM:`, reporting a require-all failure for an in-scope row that is not `code_complete`, or not printing `OK(0): phase4 in-scope set = 21 rows (rows=31)` followed by `OK: reconciled rows = 21 / 21`; or the Phase 3 gate printing `FAIL(` or not printing `OK(0): phase3 tranche = 5 / 5 (rows=31)` and `OK: implemented classes = 5 / 5`.</fails_when>
  </verify>
  <acceptance_criteria>
    - All 21 in-scope `phase:4` rows read `code_complete: true` with a non-empty `internal_name` resolving to an on-disk class under `Sources/Modules/Yggdrasil/KelpCurtain/NPCs`; `03-BIOLOGY.json` still has 31 rows, the unchanged `counts` block, the same 5-id `phase3_tranche`, the same 6-id `texture_complete` set and the same 9-id `design_art` set; the two hardmode rows remain `deferred: true` with a non-empty `deferred_reason`; every row's `status` is still `unchecked`.
    - Every one of the 21 rows carries an `artwork` blocker containing `Commons.ModAsset.White_Mod` and the `localization deferred (D-20); runtime verification outstanding (D-21)` tail, plus its own disposition elements; the three D-45 shell rows carry the behaviour-undefined marker.
    - `03-BIOLOGY.md` has 31 rows whose ids, statuses and blocker cells match the JSON exactly.
    - `04-DEVIATIONS.md` carries the coverage claim, the mechanically generated §11 Blockers table whose text is identical to the JSON, the §12 close-out counts with the explicit no-item-scope-promoted statement, the §13 decision disposition table and the §14 deferred registry.
    - `check-biology.ps1 -RequireAll` (Phase 4) exits 0 and prints `OK(0): phase4 in-scope set = 21 rows (rows=31)` and `OK: reconciled rows = 21 / 21`; the Phase 3 gate `-RequireAll` exits 0 and prints its two Phase 3 lines; `.planning/phases/03-completed-art-ordinary-monsters/scripts/check-biology.ps1` is unmodified.
    - No `.png`, HJSON file, Feishu document or Phase 1/2 artifact was created or modified.
  </acceptance_criteria>
  <done>Every in-scope row is reconciled with its implemented class and its final blocker text, and the phase has one consolidated, machine-consistent ledger with a coverage claim and a close-out block.</done>
  <commit>docs(04-09): reconcile the 21 in-scope rows and consolidate the phase ledger</commit>
</task>

<task type="auto">
  <name>Task 2: Full offline gate chain, Release build and the D-21 client UAT bundle</name>
  <reversibility rating="reversible">Reporting task: it records evidence and adds no gameplay state.</reversibility>
  <files>.planning/phases/04-remaining-ordinary-monsters/04-UAT.md, .planning/phases/04-remaining-ordinary-monsters/04-DEVIATIONS.md, .planning/phases/04-remaining-ordinary-monsters/04-VALIDATION.md</files>
  <precondition>Task 1 is complete and both `check-biology.ps1 -RequireAll` runs are green.</precondition>
  <read_first>
    - `.planning/phases/03-completed-art-ordinary-monsters/03-UAT.md` (the client-bundle format this phase mirrors, including the header stating the bundle is recorded but not run).
    - `.planning/phases/02-remaining-items-unfinished-art-materials/02-UAT.md` and `02-VERIFICATION.md` (the earlier formats).
    - `.planning/phases/04-remaining-ordinary-monsters/04-DEVIATIONS.md` (§11–§14 from Task 1; §12 is where the chain results and any triage are recorded — note that plan 04-01 reserved §11 for blockers, so the chain results belong in §12 or a §12.1 subsection).
    - `.planning/phases/04-remaining-ordinary-monsters/04-VALIDATION.md` §Test Infrastructure (the full suite command this plan must run and satisfy).
    - `.planning/phases/01-item-inventory-completed-art-items/scripts/check-inventory-reconciliation.ps1`, `check-carryover.ps1`, `check-tranche-A.ps1`, `check-tranche-B.ps1`, `check-armofgianttree-charge.ps1`, `.planning/phases/02-remaining-items-unfinished-art-materials/scripts/check-phase2.ps1` and `.planning/phases/03-completed-art-ordinary-monsters/scripts/check-biology.ps1` (the earlier gates to re-run unchanged).
    - `AGENTS.md` §Tests and Verification (the byte-level UTF-8 BOM snippet, its `git merge-base HEAD origin/master` anchor, and the Phase 1 `01-07-SUMMARY.md` deviation 2 / Phase 2 `02-05-SUMMARY.md` / Phase 3 `03-DEVIATIONS.md` §8.2 precedents that make that anchor non-isolating on this long-lived branch).
  </read_first>
  <action>
    Run the complete offline chain in one command and record the verbatim result lines: `dotnet build /p:Configuration=Release /p:WarningLevel=0`, then the Phase 4 gate `scripts/check-biology.ps1 -RequireAll`, then the Phase 3 gate `../03-completed-art-ordinary-monsters/scripts/check-biology.ps1 -RequireAll`, then the Phase 1 gates (`check-inventory-reconciliation.ps1`, `check-carryover.ps1`, `check-tranche-A.ps1`, `check-tranche-B.ps1`, `check-armofgianttree-charge.ps1`), the Phase 2 gate (`check-phase2.ps1`), and finally `dotnet test --filter "FullyQualifiedName~Yggdrasil"` — the MSTest link `04-VALIDATION.md` lists as part of the full suite command, which this plan must therefore also run and record so the plan and the validation strategy agree. Each PowerShell gate runs with `powershell -NoProfile -ExecutionPolicy Bypass -File`.

    Triage rule for an earlier-phase failure: Phase 4 adds no item, tile, wall, buff or localization artifact, so an earlier gate is expected to stay green. If one fails, first determine whether Phase 4 caused it (for example a gate that scans the whole working tree for added binaries or BOMs, or the Phase 3 gate's matrix-wide `internal_name`-resolves invariant, which Phase 4's new `internal_name` values must satisfy). A Phase 4-caused failure is fixed inside Phase 4's own files. A failure caused by a stale out-of-phase baseline anchor is reported to the user with the exact failing line and the reason the baseline is stale, and is recorded in `04-DEVIATIONS.md` §12 as an outstanding item; it is never fixed by editing the earlier phase's gate or artifacts.

    Then run the AGENTS.md byte-level UTF-8 BOM check exactly in the shape AGENTS.md writes it: resolve `$base = git merge-base HEAD origin/master`, assemble `$files` from `git -c core.quotepath=false diff --name-only --diff-filter=ACMRTUXB $base --` plus `git -c core.quotepath=false ls-files --others --exclude-standard`, and fail on any file whose first three bytes read through `[IO.File]::ReadAllBytes` are `0xEF 0xBB 0xBF`. Because those `git` calls are fallible stages inside the block, check `$LASTEXITCODE` explicitly after each of them (not just `$?`) so a git failure aborts with a named message instead of being silently treated as an empty change set, and assemble the deduplicated file set and the BOM list with plain `foreach` loops (no shell pipeline after a `git` call). Record its result line (`UTF-8 BOM check passed (N files).`) verbatim in `04-DEVIATIONS.md` §12 and state the anchor it used. Two facts belong in the same entry: (a) it measures the whole branch against a distant pre-phase ancestor, so it is **advisory and non-isolating** — the binding, phase-scoped BOM gate is invariant 13 inside the Phase 4 gate, which the chain already ran first over the phase's own change set (the `03-DEVIATIONS.md` §8.2 precedent); and (b) if it ever reports a BOM in a path outside Phase 4's change set, that is a stale-anchor finding to record with the offending path, and nothing is edited to silence it.

    Create `.planning/phases/04-remaining-ordinary-monsters/04-UAT.md` as the D-21 client bundle, mirroring `03-UAT.md`'s format. It records, per in-scope row, the client checks that cannot be observed offline:
      - **spawn and band**: the creature appears in the Kelp Curtain in its designed context — the surface for 水黾; dry land for 剧毒蟾蜍, 装甲虾 and the Spiny Moss Court rows; underwater for 幽光蝾螈, 爆弹水母, 帆鳍鳢, 放射虫 and the two octopuses; the water bottom for 碧灵鮟鱇 and 大型覆藻章鱼; open air for 布罗迪蝇蜓; and the Valley land band for the three hazard rows, 小格普螺 and 大型荆棘苔龟.
      - **isolation (BIO-06)**: none of the 21 appears in an ordinary world after several minutes of idling there, and none spawns outside its band inside Yggdrasil.
      - **behaviour**: the row-specific behaviour from `03-BIOLOGY.json` — the water dashes and land/water recovery of 水黾; 剧毒蟾蜍's poison bubble and its 75%/25% contact split and death cloud; 幽光蝾螈's moisture cycle (60 s / 10 s / 30-frame suffocation) and its 蟾蜍 avoidance; 装甲虾's 2–5 group; 爆弹水母's hover and 30/50 death blast; 帆鳍鳢's neutral cruise versus charge; 放射虫's continuous fire and its 4-tile/300-frame dash; the two octopuses' stealth, Slow, ink Darkness and 大型覆藻章鱼's grab, 180-frame four-cloud wave and low-life flee; 碧灵鮟鱇's stealth lamp and 60-damage reveal dash; the three shells' loadability; the four soldier variants' four attack patterns, the 120-frame boulder pickup, the 200-frame spacing and the hound charge; 王庭号令者's staff raise, 1–3 summon and 10-tile spacing; both flydragon sizes; 红针洋辣子's 180-frame 4–6 spike volley and its poison split; 阿萨辛覆盘子's 4–8 tile window and its defence 20/4 switch with its restore on retract; 蛇行苔's 2-tile bind and 60-frame cadence; 小格普螺's crawl; and 大型荆棘苔龟's full four-state cycle including a **target lost mid-flight** case and a **melee reflect while retracted** case.
      - **combat**: contact and projectile damage values and the immunity cells behave as documented, including the retracted 999 defence and the reflect clamp.
      - **drops**: killing the creature can produce the wired Phase 1 items at the documented chances — `RadialCarapace`, `MeatLantern`, `Photophore`, `ActivatedDogStaff` (hound only) and `CaterpillarJuice` — and no absent material drops.
      - **dedicated server and multiplayer (QUAL-03)**: the tranche spawns and fights on a dedicated server; no new projectile is duplicated or client-spawned; the reflect is applied exactly once; and no client-only work runs on the server.
      - **localization**: the display names fall back cleanly and are recorded as deferred (D-20), with no key fabricated.
    Mark every entry as not yet executed, and state in the file's header that the bundle is recorded but not run, so the tranche is never presented as client-verified.

    Append the chain results and the triage outcome to `04-DEVIATIONS.md` §12 (or a §12.1 subsection), and complete the §14 deferred registry if anything remains open.

    Do not create or modify any `.png`, do not edit an HJSON file, and do not launch the game as part of this task.
  </action>
  <verify>
    <automated>dotnet build /p:Configuration=Release /p:WarningLevel=0; if ($?) { powershell -NoProfile -ExecutionPolicy Bypass -File ".planning/phases/04-remaining-ordinary-monsters/scripts/check-biology.ps1" -RequireAll }; if ($?) { powershell -NoProfile -ExecutionPolicy Bypass -File ".planning/phases/03-completed-art-ordinary-monsters/scripts/check-biology.ps1" -RequireAll }; if ($?) { powershell -NoProfile -ExecutionPolicy Bypass -File ".planning/phases/01-item-inventory-completed-art-items/scripts/check-inventory-reconciliation.ps1" }; if ($?) { powershell -NoProfile -ExecutionPolicy Bypass -File ".planning/phases/01-item-inventory-completed-art-items/scripts/check-carryover.ps1" }; if ($?) { powershell -NoProfile -ExecutionPolicy Bypass -File ".planning/phases/01-item-inventory-completed-art-items/scripts/check-tranche-A.ps1" }; if ($?) { powershell -NoProfile -ExecutionPolicy Bypass -File ".planning/phases/01-item-inventory-completed-art-items/scripts/check-tranche-B.ps1" }; if ($?) { powershell -NoProfile -ExecutionPolicy Bypass -File ".planning/phases/01-item-inventory-completed-art-items/scripts/check-armofgianttree-charge.ps1" }; if ($?) { powershell -NoProfile -ExecutionPolicy Bypass -File ".planning/phases/02-remaining-items-unfinished-art-materials/scripts/check-phase2.ps1" }; if ($?) { dotnet test --filter "FullyQualifiedName~Yggdrasil" }; if ($?) { $p = ".planning/phases/04-remaining-ordinary-monsters/04-UAT.md"; if (-not (Test-Path -LiteralPath $p -PathType Leaf)) { throw "04-UAT.md missing" }; $t = [IO.File]::ReadAllText($p); foreach ($token in @('not executed','isolation','dedicated','localization','JadeSpiritAnglerfish','LargeMossyThornTurtle','RedNeedleCaterpillar','AnimatedWitherbarkSoldier','CourtCommander','AlgaeOctopus','ToxicToad','ArmoredShrimp','BombJellyfish','SailfinSnakehead','Radiolarian','WaterStrider','GlowSalamander','SmallGuppyConch','AssassinRaspberry','SerpentMoss','BrodieFlydragon','FluorescentHydra','GiantTigerShrimp','CannonBarnacle')) { if (-not $t.Contains($token)) { throw "04-UAT.md missing token: $token" } }; $b = [IO.File]::ReadAllBytes($p); if ($b.Length -ge 3 -and $b[0] -eq 0xEF -and $b[1] -eq 0xBB -and $b[2] -eq 0xBF) { throw "UTF-8 BOM in 04-UAT.md" }; "OK: 04-UAT.md present, token-complete, BOM-free" }</automated>
    <fails_when>non-zero exit at any link in the chain — an `error CS` in the Release build, a failing `dotnet test --filter "FullyQualifiedName~Yggdrasil"`, any gate printing `FAIL(` / exiting 2 / not printing its success line (the Phase 4 gate's lines are `OK(0): phase4 in-scope set = 21 rows (rows=31)` and `OK: reconciled rows = 21 / 21`; the Phase 3 gate's are `OK(0): phase3 tranche = 5 / 5 (rows=31)` and `OK: implemented classes = 5 / 5`), a `04-UAT.md missing` / `04-UAT.md missing token: …` / `UTF-8 BOM in 04-UAT.md` throw, or the AGENTS.md BOM block reporting a BOM in a file outside Phase 4's change set, unless the exact failure is triaged as a stale out-of-phase baseline and recorded in `04-DEVIATIONS.md` §12 with the failing line quoted.</fails_when>
    <human-check>
      <test>Run the `.planning/phases/04-remaining-ordinary-monsters/04-UAT.md` bundle in a tModLoader client, including one dedicated-server launch and one ordinary-world idle run, and including the mini-boss mid-flight target-loss case.</test>
      <expected>Every in-scope creature spawns in its designed band and behaves as recorded, none appears in the ordinary world, the five wired drop types are obtainable and no absent material drops, the reflect is applied once while the shell is closed, and no missing-texture or disabled-mod entry appears in the tML log for any of the 26 new classes.</expected>
      <why_human>Spawn isolation, AI feel, the moisture and bind cycles, the state machine and drop acquisition are runtime properties; no offline script observes them (D-21).</why_human>
    </human-check>
  </verify>
  <acceptance_criteria>
    - The full chain ran in one command and every link exited 0, or the single exception is quoted with its failing line and justification in `04-DEVIATIONS.md` §12.
    - The AGENTS.md byte-level UTF-8 BOM check ran in the AGENTS.md shape (`git merge-base HEAD origin/master` anchor plus the `ReadAllBytes` / `0xEF 0xBB 0xBF` filter), reported no BOM-prefixed file, and its result line and anchor are recorded in `04-DEVIATIONS.md` §12 alongside the note that the Phase 4 gate's invariant 13 is the binding phase-scoped equivalent on this branch.
    - `04-UAT.md` exists with the header stating the bundle is recorded but not run, and carries per-row spawn, isolation, behaviour, combat, drop, dedicated-server/multiplayer and localization entries for all 21 rows, every one marked not yet executed; it contains every class-name token enumerated in this task's verify.
    - `04-DEVIATIONS.md` §12 contains the chain result lines and the triage outcome; §14 lists any remaining open item.
    - No `.png`, HJSON file or Feishu document was created or modified, and the game was not launched by this task.
  </acceptance_criteria>
  <done>The complete offline chain (Release build plus the Phase 1, 2, 3 and 4 gates and the Yggdrasil MSTest link) is green in one run and recorded, and the outstanding client-side verification of the 21-row tranche is captured in a per-row UAT bundle instead of being presented as done.</done>
  <commit>docs(04-09): full phase gate chain, Release build and the D-21 client UAT bundle</commit>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Matrix JSON -> ledger and mirror | The ledger's blocker text is generated mechanically from the JSON so the two cannot disagree. |
| Phase 4 changes -> earlier phases' gates | Phase 4 adds only creature classes, projectiles and planning artifacts, so the Phase 1/2/3 gates must stay green; a stale baseline is reported, never bypassed. |
| Recorded verification -> presented verification | The UAT bundle is explicitly marked not executed so unrun client checks are not mistaken for verified behaviour. |
| Repository -> source snapshot | The Feishu document stays read-only and no status is written back; Phase 8 owns synchronization. |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-04-55 | Repudiation | Rows presented as complete without an executed client check | high | mitigate | `04-UAT.md` records every runtime check as not yet executed and states that in its header; the matrix keeps every row `status: "unchecked"` (no colour) until Phase 8; §12's close-out counts name the 21 rows so an omission is detectable. |
| T-04-56 | Tampering | Editing a Phase 1/2/3 gate or artifact to force a green chain | high | mitigate | Prohibition block plus the triage rule: a Phase 4-caused failure is fixed in Phase 4's own files, a stale baseline is reported and recorded in §12, and no earlier-phase file is in this plan's `files_modified`. |
| T-04-57 | Tampering | Ledger/JSON divergence after consolidation | medium | mitigate | The §11 Blockers table is generated mechanically from `03-BIOLOGY.json`, and the Phase 4 gate's invariant 11 re-asserts id-set and per-row status parity between the JSON and the Markdown mirror on every run. |
| T-04-58 | Tampering | The defence-999 or reflect hazard shipping unverified | high | mitigate | `04-UAT.md` includes an explicit mini-boss mid-flight target-loss case and a retracted-reflect case; §12 names the state machine as an open runtime item (T-04-48/T-04-51). |
| T-04-59 | Tampering | Item scope silently promoted into the monster phase | high | mitigate | The §12 close-out block states explicitly that no item scope was promoted and lists the wired Phase 1–2 types and the absent-drop register; the gate rejects any drop rule whose item type is not on disk (D-57/D-58). |
| T-04-60 | Denial of Service | A dropped or truncated gate command hiding a failure | low | mitigate | The chain is a single `if ($?)`-chained command whose exit status is the phase's gate; each link's success line is quoted in §12. |
| T-04-SC | Tampering | Package supply chain | low | accept | No package is installed by this plan (04-RESEARCH §Package Legitimacy Audit: not applicable). |
</threat_model>

<verification>
- `dotnet build /p:Configuration=Release /p:WarningLevel=0` exits 0 with no `error CS` and packages `Everglow.tmod`.
- The Phase 4 gate `-RequireAll` exits 0 and prints `OK(0): phase4 in-scope set = 21 rows (rows=31)`, `OK: reconciled rows = 21 / 21`, `OK: guarded classes = 37` (the 26 region-folder classes plus the 11 `$phaseProjectiles` entries of invariant 7) and its BOM line, with invariant 13 (the phase-scoped BOM guard) green.
- The Phase 3 gate `-RequireAll` exits 0 and prints `OK(0): phase3 tranche = 5 / 5 (rows=31)` and `OK: implemented classes = 5 / 5`.
- The AGENTS.md byte-level UTF-8 BOM check runs in the AGENTS.md shape against `git merge-base HEAD origin/master`, reports no BOM-prefixed file, and its result line, file count and anchor are recorded in `04-DEVIATIONS.md` §12 with the statement that invariant 13 is the binding phase-scoped equivalent on this branch.
- The five Phase 1 gates, the Phase 2 gate and `dotnet test --filter "FullyQualifiedName~Yggdrasil"` all exit 0, or their single exception is quoted and justified in `04-DEVIATIONS.md` §12.
- The BOM block's three git stages each check `$LASTEXITCODE` explicitly and no `git` call is followed by a shell pipeline, so a git failure aborts with a named message rather than passing as an empty change set.
- All 21 in-scope rows read `code_complete: true` with a populated `internal_name` resolving to an on-disk class and with final blocker text; the JSON still holds 31 rows and the frozen `counts` / `phase3_tranche` / `texture_complete` / `design_art` values; the two hardmode rows are still deferred; every `status` is still `unchecked`.
- `03-BIOLOGY.md` mirrors the JSON row-for-row; `04-DEVIATIONS.md` holds the coverage claim, the mechanical §11 Blockers table, the §12 close-out counts and chain results, the §13 disposition table and the §14 deferred registry.
- `04-UAT.md` exists with per-row entries for all 21 rows, every one marked not yet executed.
- No `.png` or other binary asset was added, moved, renamed or modified; no HJSON was edited; the Feishu document is untouched; no Phase 1/2 artifact and not the Phase 3 gate script was edited.
</verification>

<success_criteria>
- The 21-row Phase 4 tranche is fully reconciled: 21 rows implemented across 26 classes, four Phase 1 drop types plus one cross-namespace pre-existing material wired, and every absent drop, capture item, unimplemented system, region predicate and effect gap recorded (D-24, D-57/D-58, QUAL-04).
- One consolidated ledger and one mirror agree byte-for-byte with the machine source of truth, and the phase's deferred residue is explicit (QUAL-05).
- The complete offline chain and the Release build are green in a single run and recorded.
- The out-of-reach client and dedicated-server checks, including the mini-boss state-machine edge cases, are captured as an explicit unexecuted UAT bundle rather than assumed (D-21).
- BIO-01, BIO-02 and BIO-03 are complete on the code side; their requirement rows and Phase 8's source synchronization remain for the final phase, and nothing here writes a Feishu status (D-25).
</success_criteria>

<output>
Create `.planning/phases/04-remaining-ordinary-monsters/04-09-SUMMARY.md` when done.
</output>
