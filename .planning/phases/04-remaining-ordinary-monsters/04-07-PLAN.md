---
phase: 04-remaining-ordinary-monsters
plan: 07
type: execute
wave: 3
depends_on: [04-01]
files_modified:
  - Sources/Modules/Yggdrasil/KelpCurtain/NPCs/ValleyOfLushAndMoist/RedNeedleCaterpillar.cs
  - Sources/Modules/Yggdrasil/KelpCurtain/NPCs/ValleyOfLushAndMoist/AssassinRaspberry.cs
  - Sources/Modules/Yggdrasil/KelpCurtain/NPCs/ValleyOfLushAndMoist/SerpentMoss.cs
  - Sources/Modules/Yggdrasil/KelpCurtain/Projectiles/Enemies/RedNeedleCaterpillar_Spike.cs
  - Sources/Modules/Yggdrasil/KelpCurtain/Projectiles/Enemies/AssassinRaspberry_Spike.cs
autonomous: true
requirements: [BIO-03]
user_setup: []
estimate:
  tokens: 60000
  raw_tokens: 60000
  tasks: 3
  confidence: low

must_haves:
  truths:
    - "郤｢髓域ｴ玖ｾ｣蟄・(`RedNeedleCaterpillar`) reuses the repository's segmented-worm template `Everglow.Commons.Templates.Enemies.Caterpillar` and mirrors `Sources/Modules/Yggdrasil/YggdrasilTown/NPCs/BarkSpicyCaterpillar.cs`, with the design's 60 life / 20 contact damage / 4 defence / -0.12 knockback resistance progression, 荳ｭ豈・immunity, 80 copper and the 4窶・ spike volley fired every 180 frames when the player is at least 4 tiles away (D-53, OQ4)."
    - "Its contact and spike hits apply the design's 25% 20-second / 37.5% 10-second 荳ｭ豈・split, and its loot table wires the pre-existing implemented `CaterpillarJuice` (denominator 1, quantity 1窶・) exactly as `BarkSpicyCaterpillar` does 窶・the OQ1 cross-namespace decision recorded in `04-DEVIATIONS.md` ﾂｧ6.1 (D-57/D-59)."
    - "髦ｿ關ｨ霎幄ｦ・尨蟄・(`AssassinRaspberry`) is a stationary hostile that stays buried beyond 8 tiles, extends when the player is inside 8 tiles, scatters 4窶・ `AssassinRaspberry_Spike` ground spikes toward the player when the player is between 4 and 8 tiles, cannot attack below 4 tiles but does not retract, and carries defence 20 while passive and 4 while attacking with knockback immunity and 荳ｭ豈・蝗ｰ諠・immunity (D-46)."
    - "陋・｡瑚渠 (`SerpentMoss`) spawns disguised as an environmental plant, binds a player who comes within 2 tiles, then every 60 frames applies 60 ticks of `BuffID.Webbed` and 15 damage, with a 33% chance of 15 seconds of `BuffID.Poisoned` after each damage tick, and it is knockback-immune and immune to 荳ｭ豈・蝗ｰ諠・(D-46)."
    - "All three classes override `Texture => Commons.ModAsset.White_Mod`, register through `NPCSpawnManager.RegisterNPC`, gate `SpawnChance` on `SubworldSystem.IsActive<YggdrasilWorld>()` plus `KelpCurtainBiome.IsKelpCurtainLayer(spawnInfo.Player)` plus a dry-land condition, keep graphics behind `!Main.dedServ`, and write authoritative state under `Main.netMode != NetmodeID.MultiplayerClient` with `NPC.netUpdate` (D-48/D-52/D-55)."
    - "The design's 莨ｪ陬・(disguise) presentation for 髦ｿ關ｨ霎幄ｦ・尨蟄・and 陋・｡瑚渠 belongs to a Valley disguised-hazard visual system that does not exist, so it is recorded as a precise D-46 blocker naming the missing system and the affected files; the trigger, bind, damage and spike behaviour it hides behind is built (D-46)."
  artifacts:
    - "Sources/Modules/Yggdrasil/KelpCurtain/NPCs/ValleyOfLushAndMoist/RedNeedleCaterpillar.cs"
    - "Sources/Modules/Yggdrasil/KelpCurtain/NPCs/ValleyOfLushAndMoist/AssassinRaspberry.cs"
    - "Sources/Modules/Yggdrasil/KelpCurtain/NPCs/ValleyOfLushAndMoist/SerpentMoss.cs"
    - "Sources/Modules/Yggdrasil/KelpCurtain/Projectiles/Enemies/RedNeedleCaterpillar_Spike.cs"
    - "Sources/Modules/Yggdrasil/KelpCurtain/Projectiles/Enemies/AssassinRaspberry_Spike.cs"
  key_links:
    - "RedNeedleCaterpillar : Everglow.Commons.Templates.Enemies.Caterpillar -> the repository's segmented-worm AI -> 謗芽誠蜷梧醍坩蛻ｺ豈幄勠 is satisfied by reusing the same drop as BarkSpicyCaterpillar"
    - "RedNeedleCaterpillar.ModifyNPCLoot -> ItemDropRule.Common(ModContent.ItemType<CaterpillarJuice>(), 1, 1, 2) -> the pre-existing YggdrasilTown material (OQ1)"
    - "AssassinRaspberry state enum -> the 4窶・ tile window -> 4窶・ AssassinRaspberry_Spike spawns"
    - "SerpentMoss bind -> BuffID.Webbed + 15 damage every 60 frames -> the design's 譚溽ｼ壼ｹｶ謖∫ｻｭ騾謌蝉ｼ､螳ｳ"
  prohibitions:
    - "Never create, generate, move or modify a `.png`/`.obj`/`.xnb` or any other binary/art asset (D-48/D-51); in particular do not add a gore sprite, so `RedNeedleCaterpillar` must NOT override `PreKill` the way `BarkSpicyCaterpillar` does, because that override loads `BarkSpicyCaterpillar_gore*` assets."
    - "Never edit `Sources/Modules/Yggdrasil/YggdrasilTown/NPCs/BarkSpicyCaterpillar.cs`, `Sources/Everglow.Function/Templates/Enemies/Caterpillar.cs`, or `CaterpillarJuice`: the template and the item are reused read-only (D-57)."
    - "Never write a `ModContent.ItemType<X>()` for an `X` without a `.cs` on disk (D-58)."
    - "Never approximate the Valley disguised-hazard system or a 譽ｮ髮ｨ蟷ｽ隹ｷ/蛻ｺ闍泌ｺｭ蝗ｭ region predicate; both are recorded blockers (D-46/D-52)."
    - "Never store a bind timer on the `Player`; the per-NPC cadence lives in `NPC.localAI[]` (Pitfall 6)."
    - "Never edit `.planning/phases/03-completed-art-ordinary-monsters/03-BIOLOGY.json`, `03-BIOLOGY.md` or any earlier phase's artifact from this plan."
  assumptions:
    - "OQ1 is resolved as WIRE: 郤｢髓域ｴ玖ｾ｣蟄・s design text 謗芽誠蜷梧醍坩蛻ｺ豈幄勠 is satisfied by the pre-existing implemented `CaterpillarJuice` from `Sources/Modules/Yggdrasil/YggdrasilTown/Items/Materials/CaterpillarJuice.cs`, mirroring `BarkSpicyCaterpillar.cs`'s `ItemDropRule.Common(ModContent.ItemType<CaterpillarJuice>(), 1, 1, 2)`; this references existing content and creates no new item scope, so it satisfies D-57, and the Phase 4 gate's item index covers the whole `Sources/Modules/Yggdrasil` tree because of it (04-01 ﾂｧ6.1)."
    - "The design's 蜃ｻ騾謚玲ｧ progression `-12/-8/4` maps to `NPC.knockBackResist = -0.12f` normally, `-0.08f` in expert mode and `0.04f` in master mode, exactly as `BarkSpicyCaterpillar.cs` already does; a negative resistance is therefore valid in this repository and needs no clamp (this resolves research assumption A8)."
    - "`[NoGameModeScale]` from `Everglow.Commons.Mechanics.Miscs` is applied because the design supplies per-difficulty values and the template's own scaling would double-apply (the `BarkSpicyCaterpillar` precedent)."
    - "The design's 譚溽ｼ・maps to the vanilla `BuffID.Webbed`, verified present in this tML build (04-01 ﾂｧ10)."
---

# Phase 4 Plan 07 窶・Valley of Lush and Moist: Hazards & the Needle Caterpillar (郤｢髓域ｴ玖ｾ｣蟄・ 髦ｿ關ｨ霎幄ｦ・尨蟄・ 陋・｡瑚渠)

## User Story

**As a** Kelp Curtain player, **I want to** be stung by a caterpillar that shoots needles, ambushed by a raspberry that was pretending to be scenery, and grabbed by moss that was pretending to be a plant, **so that** the Valley punishes me for walking through it without looking.

> Planner-derived from the ROADMAP Phase 4 `Goal` line; the roadmap goal is not authored in user-story form and this restates it without adding scope (the Phase 1/2/3 plan precedent).

<objective>
Implement the Valley of Lush and Moist's three ordinary hazards: the needle-firing caterpillar that reuses the repository's existing segmented-worm AI, the buried raspberry that ambushes at a precise distance window, and the binding moss, together with the two spike projectiles.

Purpose: this plan carries the phase's one genuine cross-module reuse. 郤｢髓域ｴ玖ｾ｣蟄・s design says its AI is 蜷梧醍坩蛻ｺ豈幄勠 and its drop is 謗芽誠蜷梧醍坩蛻ｺ豈幄勠, and the repository already holds both 窶・an abstract coroutine-driven `Caterpillar` template in `Everglow.Function/Templates/Enemies` and the implemented `CaterpillarJuice` material 窶・so the honest implementation reuses them rather than re-deriving a segmented worm or inventing a drop. That reuse is also why the gate's item index must span the whole `Sources/Modules/Yggdrasil` tree (OQ1). The other two rows are the phase's D-46 disguise cases: their ambush *mechanics* are fully specified and are built, while the 莨ｪ陬・visual system that hides them is missing and is recorded as a blocker rather than faked with an alpha trick that would be indistinguishable from a bug.

D-46 (build the independently-completable part, record the missing system), D-53 (no dependency on unbuilt terrain), D-55 (server authority plus `!Main.dedServ`), D-57/D-59 (reference implemented items; follow the design's chances) and OQ1's resolution are implemented here.

Output: `RedNeedleCaterpillar.cs`, `AssassinRaspberry.cs` and `SerpentMoss.cs` under `Sources/Modules/Yggdrasil/KelpCurtain/NPCs/ValleyOfLushAndMoist/`, plus `RedNeedleCaterpillar_Spike.cs` and `AssassinRaspberry_Spike.cs` under `Sources/Modules/Yggdrasil/KelpCurtain/Projectiles/Enemies/`.
</objective>

<execution_context>
@C:/Users/book/.config/opencode/gsd-core/workflows/execute-plan.md
@C:/Users/book/.config/opencode/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/phases/04-remaining-ordinary-monsters/04-CONTEXT.md
@.planning/phases/04-remaining-ordinary-monsters/04-RESEARCH.md
@.planning/phases/04-remaining-ordinary-monsters/04-DEVIATIONS.md
@.planning/phases/04-remaining-ordinary-monsters/scripts/check-biology.ps1
@Sources/Everglow.Function/Templates/Enemies/Caterpillar.cs
@Sources/Modules/Yggdrasil/YggdrasilTown/NPCs/BarkSpicyCaterpillar.cs
@AGENTS.md
@Sources/Modules/Yggdrasil/AGENTS.md
</context>

## Design Values (from the committed snapshot; read-only, D-25)

| Row id | Design row | Heading block id | Behaviour block id(s) | 逕溷多 | 莨､螳ｳ | 髦ｲ蠕｡ | 蜃ｻ騾謚玲ｧ | 蜃丈ｼ､ | 蜈咲稔 | 髓ｱ蟶・|
| --- | --- | --- | --- | --- | --- | --- | --- | --- | --- | --- |
| `bio-valley-of-lush-and-moist-red-needle-caterpillar` | 郤｢髓域ｴ玖ｾ｣蟄・| `XrNWdCklJoFPUyxEtANcQ9tWnNe` | `Ofr1dFMe4ot9tLxq6hicZfLxnzh`, `G1ModIH6no6tFXxeuLCcZ8HYnGc` | 60 | 20・郁ｿ第・・・5・郁ｿ懃ｨ具ｼ・| 4 | -12/-8/4 | (empty) | 荳ｭ豈・| 80體・= 80 |
| `bio-valley-of-lush-and-moist-assassin-raspberry` | 髦ｿ關ｨ霎幄ｦ・尨蟄・| `TjICdnhRvowaSdxcAcXchNGDnco` | `PqdMdnpwQomTTIxDt3PcdyGDnNd` | 80 | 30 | 20・郁｢ｫ蜉ｨ・・・域判蜃ｻ・・| 蜈咲稔蜃ｻ騾 | (empty) | 荳ｭ豈偵∝峅諠・| 1體ｶ = 100 |
| `bio-valley-of-lush-and-moist-serpent-moss` | 陋・｡瑚渠 | `C8fRdR6uhoYxOKxrWzDc8IHwnud` | `RmwmdtUCwoMPI1xMVQrclFS4nuh` | 100 | 15 | 10 | 蜈咲稔蜃ｻ騾 | (empty) | 荳ｭ豈偵∝峅諠・| 2體ｶ = 200 |

`NPC.value` takes the copper number 1:1; 蜃ｻ騾謚玲ｧ maps to `1 - 謚玲ｧ/100`, except 郤｢髓域ｴ玖ｾ｣蟄・whose slash progression maps directly onto the `BarkSpicyCaterpillar` precedent.

**Behaviour, verbatim and abbreviated.**
- 郤｢髓域ｴ玖ｾ｣蟄・ *"AI蜷梧醍坩蛻ｺ豈幄勠・悟玄蛻ｫ譏ｯ荳守自螳ｶ霍晉ｦｻ荳堺ｽ惹ｺ・譬ｼ譌ｶ・梧ｯ剰ｿ・80蟶ｧ莨壼ワ蟆門絢蜿ｲ闔ｱ蟋・ｸ譬ｷ蝨ｨ螟ｴ驛ｨ蜿大ｰ・~6蟆門絢・御ｸ泌多荳ｭ蜷取怏25%讎ら紫莨夐謌蝉ｸｭ豈・0遘抵ｼ・7.5%讎ら紫騾謌蝉ｸｭ豈・0遘偵・* / *"謗芽誠蜷梧醍坩蛻ｺ豈幄勠縲・*
- 髦ｿ關ｨ霎幄ｦ・尨蟄・ *"髱呎ｭ｢逧・阜蟇ｹ逕溽黄・瑚ｦ・尨蟄千憾諤ｪ迚ｩ・檎自螳ｶ蝨ｨ霍晉ｦｻ諱ｰ螂ｽ荳ｺ4~8譬ｼ譌ｶ・御ｼ壻ｼｸ蜃ｺ蟷ｶ蜷醍捩邇ｩ螳ｶ髫乗惻謨｣蟆・~6鬚怜慍蛻ｺ・悟ｰ台ｺ・譬ｼ譌豕墓判蜃ｻ菴・弍荳堺ｼ夂ｼｩ蝗槫悉・瑚ｶ・ｿ・譬ｼ蜷朱㍾譁ｰ郛ｩ蝗槫慍荳九・*
- 陋・｡瑚渠: *"謨悟ｯｹ逕溽黄・悟・逕溷錘莨ｪ陬・・邇ｯ蠅・､咲黄・悟惠邇ｩ螳ｶ謗･霑托ｼ井ｸ堺ｽ惹ｺ・譬ｼ・牙錘・梧據郛夂自螳ｶ蟷ｶ謖∫ｻｭ騾謌蝉ｼ､螳ｳ縲よ據郛夂自螳ｶ蜷趣ｼ梧ｯ・0蟶ｧ蟇ｹ邇ｩ螳ｶ譁ｽ蜉60蟶ｧ譚溽ｼ壼ｹｶ騾謌・5莨､螳ｳ・梧ｯ乗ｬ｡騾謌蝉ｼ､螳ｳ蜷趣ｼ梧怏33%讎ら紫蟇ｹ邇ｩ螳ｶ騾謌・5遘剃ｸｭ豈偵・*

## Artifacts This Plan Produces

- `Sources/Modules/Yggdrasil/KelpCurtain/NPCs/ValleyOfLushAndMoist/RedNeedleCaterpillar.cs` 窶・`Everglow.Yggdrasil.KelpCurtain.NPCs.ValleyOfLushAndMoist.RedNeedleCaterpillar`
- `窶ｦ/ValleyOfLushAndMoist/AssassinRaspberry.cs` 窶・`窶ｦValleyOfLushAndMoist.AssassinRaspberry`
- `窶ｦ/ValleyOfLushAndMoist/SerpentMoss.cs` 窶・`窶ｦValleyOfLushAndMoist.SerpentMoss`
- `Sources/Modules/Yggdrasil/KelpCurtain/Projectiles/Enemies/RedNeedleCaterpillar_Spike.cs` 窶・`窶ｦProjectiles.Enemies.RedNeedleCaterpillar_Spike`
- `Sources/Modules/Yggdrasil/KelpCurtain/Projectiles/Enemies/AssassinRaspberry_Spike.cs` 窶・`窶ｦProjectiles.Enemies.AssassinRaspberry_Spike`

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: 郤｢髓域ｴ玖ｾ｣蟄・`RedNeedleCaterpillar` + `RedNeedleCaterpillar_Spike` 窶・reuse the segmented-worm template and wire the 譬醍坩蛻ｺ豈幄勠 drop</name>
  <reversibility rating="reversible">One creature class extending an existing template plus one projectile; reverting deletes two files and the template is untouched.</reversibility>
  <files>Sources/Modules/Yggdrasil/KelpCurtain/NPCs/ValleyOfLushAndMoist/RedNeedleCaterpillar.cs, Sources/Modules/Yggdrasil/KelpCurtain/Projectiles/Enemies/RedNeedleCaterpillar_Spike.cs</files>
  <read_first>
    - `Sources/Modules/Yggdrasil/YggdrasilTown/NPCs/BarkSpicyCaterpillar.cs` (read in full 窶・the exact precedent: `[NoGameModeScale]`, `: Caterpillar`, the `SetDefaults` call to `base.SetDefaults()` followed by `SegmentBehavioralSize`/`SegmentHitBoxSize`/`SegmentCount`/`AnimationSpeed`/`DustType` and the per-difficulty `NPC.knockBackResist = -0.12f` / `-0.08f` / `-0.04f` progression, the `GetDrawFrame` override, `SpawnChance`, `PreKill` with gores, and `ModifyNPCLoot`. NOTE: do NOT copy `PreKill` (it loads `BarkSpicyCaterpillar_gore*` assets, and creating or referencing new art is forbidden) and do NOT copy its `SpawnChance`, which uses `Main.LocalPlayer`).
    - `Sources/Everglow.Function/Templates/Enemies/Caterpillar.cs` (read in full 窶・the template's `SetDefaults` defaults that must be overridden, the coroutine AI the design's 蜷梧醍坩蛻ｺ豈幄勠 points at, `GetDrawFrame`'s `texture.Width / 3` behaviour with the 256x256 `White_Mod` replacement, and `HitEffect`'s dependency on `DustType`).
    - `Sources/Modules/Yggdrasil/YggdrasilTown/Items/Materials/CaterpillarJuice.cs` (the drop item this class references; confirm the class name and namespace so the using directive is correct).
    - `Sources/Modules/Yggdrasil/KelpCurtain/NPCs/DeathJadeLake/Radiolarian.cs` and `Sources/Modules/Yggdrasil/KelpCurtain/Projectiles/Enemies/Radiolarian_WaterBolt.cs` (the spike-volley projectile shape and the `NPC.GetSource_FromAI()`/`!Main.dedServ` discipline).
    - `.planning/phases/04-remaining-ordinary-monsters/04-DEVIATIONS.md` ﾂｧ6.1 (the OQ1 decision) and ﾂｧ10 (the negative-knockback and `[NoGameModeScale]` notes).
  </read_first>
  <behavior>
    - Class shape: `RedNeedleCaterpillar : Everglow.Commons.Templates.Enemies.Caterpillar`, decorated `[NoGameModeScale]`, with `Texture => Commons.ModAsset.White_Mod;` and a `LocalizationCategory`.
    - Stats: `NPC.lifeMax = 60; NPC.life = 60; NPC.damage = 20; NPC.defDamage = 20; NPC.defense = 4; NPC.defDefense = 4; NPC.value = 80;` (80 copper), `NPC.rare = ItemRarityID.White;`, `NPC.knockBackResist = -0.12f;` normally, `-0.08f` in expert and `0.04f` in master, and `SpecificDebuffImmunity[Type][BuffID.Poisoned] = true;`.
    - Volley: when the player's distance is at least 4 tiles, every 180 frames it fires 4窶・ `RedNeedleCaterpillar_Spike` projectiles from its head, in the design's spiky-slime pattern.
    - Debuff: a spike or contact hit applies 20 seconds of `BuffID.Poisoned` with 25% probability and 10 seconds with 37.5% probability.
    - Spawn: the two tokens plus a dry-land condition, at a land weight from the shared constants.
    - Loot: exactly one rule 窶・`ItemDropRule.Common(ModContent.ItemType<CaterpillarJuice>(), 1, 1, 2)`.
  </behavior>
  <action>
    Create `Sources/Modules/Yggdrasil/KelpCurtain/NPCs/ValleyOfLushAndMoist/RedNeedleCaterpillar.cs` (`namespace Everglow.Yggdrasil.KelpCurtain.NPCs.ValleyOfLushAndMoist`) and `Sources/Modules/Yggdrasil/KelpCurtain/Projectiles/Enemies/RedNeedleCaterpillar_Spike.cs` (`namespace Everglow.Yggdrasil.KelpCurtain.Projectiles.Enemies`); both with the phase's standard formatting and an XML doc comment naming the design row 郤｢髓域ｴ玖ｾ｣蟄・ the region, the block ids `XrNWdCklJoFPUyxEtANcQ9tWnNe` / `Ofr1dFMe4ot9tLxq6hicZfLxnzh` / `G1ModIH6no6tFXxeuLCcZ8HYnGc`, the reuse of the `Caterpillar` template, the `BarkSpicyCaterpillar` mirroring, and the OQ1 drop decision with its `04-DEVIATIONS.md` ﾂｧ6.1 pointer.

    `RedNeedleCaterpillar`: extend `Everglow.Commons.Templates.Enemies.Caterpillar`; annotate with `[NoGameModeScale]` (`using Everglow.Commons.Mechanics.Miscs;`) with a comment that the design supplies the per-difficulty progression; `SetStaticDefaults` calling `NPCSpawnManager.RegisterNPC(Type);` and setting the 荳ｭ豈・immunity; `SetDefaults` calling `base.SetDefaults();` and then overriding `SegmentBehavioralSize`, `SegmentHitBoxSize`, `SegmentCount`, `AnimationSpeed`, `DustType` (either `-1`, which disables the template's `HitEffect` dust entirely, or an existing KelpCurtain/`YggdrasilTown` dust type 窶・do not create one), and the stat block above including the three-mode `knockBackResist` progression. Set `SpawnModBiomes = [ModContent.GetInstance<KelpCurtainBiome>().Type];`.

    Override `SpawnChance(NPCSpawnInfo spawnInfo)` with the two tokens plus `KelpCurtainSpawnConditions.IsDryLand(spawnInfo)` and a land weight from the shared constants; never `Main.LocalPlayer`, never `IsBiomeActive`. Add the volley: a named `const int VolleyIntervalFrames = 180`, a named `const float MinVolleyRangeTiles = 4f`, a named `const int MinSpikes = 4` / `MaxSpikes = 6`, and a named timer over `NPC.localAI[]`; the volley spawns the spikes from `NPC.GetSource_FromAI()` under `Main.netMode != NetmodeID.MultiplayerClient` with `NPC.netUpdate = true`. Where the template's `PreDraw` needs a valid source rectangle, note in a comment that the `White_Mod` texture is 256x256 so the template's `texture.Width / 3` frame math stays valid, and do not override `GetDrawFrame` unless you must. Override `OnHitPlayer(Player target, Player.HurtInfo hurtInfo)` for the design's 20-second / 10-second poison split, unguarded (tML runs the hook on the local client). Do NOT override `PreKill` (it would reference gore art that does not exist), do not add a gore, dust or `.png`, and do not modify `Caterpillar.cs` or `BarkSpicyCaterpillar.cs`. `ModifyNPCLoot(NPCLoot npcLoot)` adds exactly `npcLoot.Add(ItemDropRule.Common(ModContent.ItemType<CaterpillarJuice>(), 1, 1, 2));` with `using Terraria.GameContent.ItemDropRules;` and the `YggdrasilTown.Items.Materials` namespace, plus a comment recording the OQ1 decision.

    `RedNeedleCaterpillar_Spike`: a hostile projectile in the design's spiky-slime pattern 窶・launched toward the player with a spread, gravity-affected, tile-colliding, the design's 15 ranged damage from its owner, a short lifetime, and on-hit the same 25%/37.5% poison split. Its dust sits behind `!Main.dedServ` and it references no item type.
  </action>
  <verify>
    <automated>dotnet build /p:Configuration=Release /p:WarningLevel=0; if ($?) { powershell -NoProfile -ExecutionPolicy Bypass -File ".planning/phases/04-remaining-ordinary-monsters/scripts/check-biology.ps1" }</automated>
    <fails_when>non-zero exit 窶・an `error CS` (in particular an unresolvable `CaterpillarJuice` namespace, an unresolvable `Everglow.Commons.Mechanics.Miscs.NoGameModeScale`, or a template override whose signature does not match), the Phase 4 gate printing `FAIL(` / exiting 2 / reporting `UTF-8 BOM:`, or the gate's `OK: guarded classes =` count failing to rise by two against the previous task's run.</fails_when>
  </verify>
  <acceptance_criteria>
    - `RedNeedleCaterpillar.cs` exists with `namespace Everglow.Yggdrasil.KelpCurtain.NPCs.ValleyOfLushAndMoist` and contains `class RedNeedleCaterpillar : Caterpillar`, `[NoGameModeScale]`, `Texture => Commons.ModAsset.White_Mod`, `LocalizationCategory`, `NPCSpawnManager.RegisterNPC`, `SubworldSystem.IsActive<YggdrasilWorld>`, `KelpCurtainBiome.IsKelpCurtainLayer`, `KelpCurtainSpawnConditions.IsDryLand`, `NPC.lifeMax = 60;`, `NPC.value = 80;`, `NPC.damage = 20`, `NPC.defense = 4`, `NPC.knockBackResist = -0.12f`, `-0.08f`, `0.04f`, `BuffID.Poisoned`, `180`, `ModifyNPCLoot` and `ItemDropRule.Common(ModContent.ItemType<CaterpillarJuice>(), 1, 1, 2)`.
    - The file contains no `override bool PreKill(`, no `Main.LocalPlayer`, no `KelpCurtainBiome.IsBiomeActive`, no `ModContent.ItemType<` token other than `CaterpillarJuice`, and no `Gore.` reference.
    - `RedNeedleCaterpillar_Spike.cs` exists under `窶ｦ/Projectiles/Enemies/` with `Texture => Commons.ModAsset.White_Mod`, `NPC.GetSource_FromAI` and `Main.dedServ`.
    - `Sources/Modules/Yggdrasil/YggdrasilTown/NPCs/BarkSpicyCaterpillar.cs`, `Sources/Everglow.Function/Templates/Enemies/Caterpillar.cs` and `CaterpillarJuice.cs` are unmodified (`git diff --stat` (uncommitted-tree check) empty for all three paths).
  </acceptance_criteria>
  <done>郤｢髓域ｴ玖ｾ｣蟄・reuses the repository's segmented-worm template, carries the design's stats and negative-knockback progression, fires its 4窶・ spike volley every 180 frames at 4+ tiles with the design's poison split, and wires the pre-existing `CaterpillarJuice` drop with no new art or item.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: 髦ｿ關ｨ霎幄ｦ・尨蟄・`AssassinRaspberry` + `AssassinRaspberry_Spike` 窶・buried ambusher with a 4窶・ tile window</name>
  <reversibility rating="reversible">One creature class and one projectile under frozen names; reverting deletes two files and the disguise blocker disappears with them.</reversibility>
  <files>Sources/Modules/Yggdrasil/KelpCurtain/NPCs/ValleyOfLushAndMoist/AssassinRaspberry.cs, Sources/Modules/Yggdrasil/KelpCurtain/Projectiles/Enemies/AssassinRaspberry_Spike.cs</files>
  <read_first>
    - `Sources/Modules/Yggdrasil/KelpCurtain/NPCs/ValleyOfLushAndMoist/RedNeedleCaterpillar.cs` and `Sources/Modules/Yggdrasil/KelpCurtain/Projectiles/Enemies/RedNeedleCaterpillar_Spike.cs` (created moments earlier: the dry-land `SpawnChance`, the distance-gated volley, the state enum/timer-wrapper discipline and the spike shape).
    - `Sources/Modules/Yggdrasil/KelpCurtain/NPCs/GuppyConch.cs` (the two-state `NPC.ai[]` enum with `SetDefaults`-owned defence that changes on the state transition 窶・exactly the 20-while-passive / 4-while-attacking shape; and the `PostAI`/`HitEffect` hook discipline that keeps `ModifyIncomingHit` modifiers-only).
    - `.planning/phases/04-remaining-ordinary-monsters/04-DEVIATIONS.md` ﾂｧ5 (the region gap) and ﾂｧ7 (the disguised-hazard effect blocker).
  </read_first>
  <behavior>
    - Spawn: the two tokens plus a dry-land condition, at a land weight from the shared constants.
    - States: `Hidden` (retracted underground) and `Extended`. Beyond 8 tiles it is `Hidden`; once the player is inside 8 tiles it extends and stays extended; below 4 tiles it cannot attack but does not retract.
    - Attack: while `Extended` and the player is between 4 and 8 tiles, it scatters 4窶・ `AssassinRaspberry_Spike` ground spikes toward the player on its own cadence.
    - Defence: `NPC.defense` is 20 while passive (`Hidden`) and 4 while attacking (`Extended`), restored on every exit path.
    - Immunities: knockback immunity (`NPC.knockBackResist = 0f`) and `BuffID.Poisoned` / `BuffID.Confused`.
    - Loot: `ModifyNPCLoot` adds no rule (the design names none).
  </behavior>
  <action>
    Create `Sources/Modules/Yggdrasil/KelpCurtain/NPCs/ValleyOfLushAndMoist/AssassinRaspberry.cs` (`namespace Everglow.Yggdrasil.KelpCurtain.NPCs.ValleyOfLushAndMoist`) and `Sources/Modules/Yggdrasil/KelpCurtain/Projectiles/Enemies/AssassinRaspberry_Spike.cs` (`namespace Everglow.Yggdrasil.KelpCurtain.Projectiles.Enemies`); both with the phase's standard formatting, `Texture => Commons.ModAsset.White_Mod;`, a `LocalizationCategory`, and an XML doc comment naming the design row 髦ｿ關ｨ霎幄ｦ・尨蟄・ the region, the block ids `TjICdnhRvowaSdxcAcXchNGDnco` / `PqdMdnpwQomTTIxDt3PcdyGDnNd`, the D-46 scope statement, and the note that `AssassinRaspberry.png` must later sit beside the `.cs` (D-49).

    `AssassinRaspberry`: `NPC.lifeMax = 80; NPC.life = 80;`, `NPC.damage = 30; NPC.defDamage = 30;`, `NPC.defDefense = 20;` with `NPC.defense` set per state (20 passive, 4 attacking) by a single `EnterState`-style helper so every exit path restores the passive value 窶・the `GuppyConch` pattern 窶・`NPC.knockBackResist = 0f;`, `NPC.value = 100;` (1體ｶ), `NPC.rare = ItemRarityID.White;`, `NPC.aiStyle = -1;`, a stationary hit box, `NPC.catchItem = 0;`, `SpawnModBiomes = [ModContent.GetInstance<KelpCurtainBiome>().Type];`, and immunities `BuffID.Poisoned` and `BuffID.Confused`. `SpawnChance` gates on the two tokens plus `KelpCurtainSpawnConditions.IsDryLand(spawnInfo)` with a land weight.

    Give the class a private `enum AssassinRaspberryState { Hidden = 0, Extended = 1 }` with a named property over `NPC.ai[0]`, a named `VolleyTimer` wrapper over `NPC.localAI[0]`, named constants `ExtendTiles = 8f`, `AttackMinTiles = 4f`, `MinSpikes = 4`, `MaxSpikes = 6` and a volley interval, plus a named `AnchorPosition` (a `Vector2` field or `NPC.localAI[]` pair) so the stationary creature returns to its spawn point when it retracts. `AI()` implements the whole window: extend when the player is inside `ExtendTiles`, keep the state when the player is closer than `AttackMinTiles`, retract when the player is beyond `ExtendTiles`, and volley only inside the 4窶・ window. Compute the distance from `NPC.Center` and the synced target's centre; never read a screen or camera value, and never call `Main.LocalPlayer`. Every state change and volley spawn runs under `Main.netMode != NetmodeID.MultiplayerClient` with `NPC.netUpdate = true`. Spawn the spikes from `NPC.GetSource_FromAI()`. Add a class comment stating that the design's 莨ｪ陬・(a raspberry indistinguishable from scenery while buried) belongs to the unimplemented Valley disguised-hazard visual system and is a D-46 blocker naming that system and this file; build no disguise trick.

    `AssassinRaspberry_Spike`: a ground spike projectile that emerges from the ground at the creature's position, travels a short ground-hugging arc toward the player with gravity and tile collision, carries the design's spike damage from its owner, has a short lifetime, and emits only `!Main.dedServ`-guarded dust. It references no item type.
  </action>
  <verify>
    <automated>dotnet build /p:Configuration=Release /p:WarningLevel=0; if ($?) { powershell -NoProfile -ExecutionPolicy Bypass -File ".planning/phases/04-remaining-ordinary-monsters/scripts/check-biology.ps1" }</automated>
    <fails_when>non-zero exit 窶・an `error CS`, the Phase 4 gate printing `FAIL(` / exiting 2 / reporting `UTF-8 BOM:`, or the gate's `OK: guarded classes =` count failing to rise by two against the previous task's run.</fails_when>
  </verify>
  <acceptance_criteria>
    - `AssassinRaspberry.cs` exists with `namespace Everglow.Yggdrasil.KelpCurtain.NPCs.ValleyOfLushAndMoist` and contains `Texture => Commons.ModAsset.White_Mod`, `LocalizationCategory`, `NPCSpawnManager.RegisterNPC`, `SubworldSystem.IsActive<YggdrasilWorld>`, `KelpCurtainBiome.IsKelpCurtainLayer`, `KelpCurtainSpawnConditions.IsDryLand`, `NPC.lifeMax = 80;`, `NPC.value = 100;`, `NPC.damage = 30`, `NPC.knockBackResist = 0f`, `BuffID.Poisoned`, `BuffID.Confused`, `enum AssassinRaspberryState`, `ExtendTiles`, `AttackMinTiles`, `MinSpikes`, `MaxSpikes`, `Main.netMode != NetmodeID.MultiplayerClient`, `NPC.netUpdate`, `Main.dedServ` and `ModifyNPCLoot`.
    - The defence switch appears once inside a single state-transition helper (both `20` and `4` reachable there), not duplicated across branches; no branch can leave `NPC.defense` at 4 on the way back to `Hidden`.
    - The file references no item type, no `Main.LocalPlayer`, no `Main.screenPosition` and no `KelpCurtainBiome.IsBiomeActive`.
    - `AssassinRaspberry_Spike.cs` exists under `窶ｦ/Projectiles/Enemies/` with `Texture => Commons.ModAsset.White_Mod`, `NPC.GetSource_FromAI` and `Main.dedServ`.
    - A class comment records the D-46 disguise-visual-system blocker.
  </acceptance_criteria>
  <done>髦ｿ關ｨ霎幄ｦ・尨蟄・stays buried beyond 8 tiles, extends between 8 and 4, volleys 4窶・ ground spikes from the design's window, restores its passive defence on every exit path, and records the missing disguise system.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 3: 陋・｡瑚渠 `SerpentMoss` 窶・plant-disguised binder with a 60-frame damage cadence</name>
  <reversibility rating="reversible">One creature class under a frozen name; reverting deletes one file and no persisted player state depends on it.</reversibility>
  <files>Sources/Modules/Yggdrasil/KelpCurtain/NPCs/ValleyOfLushAndMoist/SerpentMoss.cs</files>
  <read_first>
    - `Sources/Modules/Yggdrasil/KelpCurtain/NPCs/ValleyOfLushAndMoist/AssassinRaspberry.cs` (created moments earlier: the stationary, state-driven, `AnchorPosition`-bearing ambusher shape this class mirrors).
    - `Sources/Modules/Yggdrasil/KelpCurtain/NPCs/VerdantRods.cs` (the `OnHitPlayer` unguarded-debuff shape) and `Sources/Modules/Yggdrasil/KelpCurtain/NPCs/DeathJadeLake/LargeAlgaeOctopus.cs` (created in plan 04-04: the 譚溽ｼ・遯呈・ application path whose choice must be consistent here).
    - `.planning/phases/04-remaining-ordinary-monsters/04-DEVIATIONS.md` ﾂｧ10 (the 譚溽ｼ・-> `BuffID.Webbed` mapping) and ﾂｧ7 (the disguised-hazard effect blocker).
  </read_first>
  <behavior>
    - Spawn: the two tokens plus a dry-land condition, at a land weight from the shared constants.
    - Disguise blocker: the design's 蜃ｺ逕溷錘莨ｪ陬・・邇ｯ蠅・､咲黄 belongs to the unimplemented Valley disguised-hazard visual system; the class implements the trigger, the bind and the damage, and records the presentation as a D-46 blocker.
    - Trigger: when a player comes within 2 tiles it binds them.
    - Bind loop: every 60 frames it applies 60 ticks of `BuffID.Webbed` to the bound player and deals 15 damage; after each damage tick there is a 33% chance of applying 15 seconds of `BuffID.Poisoned`.
    - Immunities: knockback immunity (`NPC.knockBackResist = 0f`) and `BuffID.Poisoned` / `BuffID.Confused`.
    - Loot: `ModifyNPCLoot` adds no rule (the design names none).
  </behavior>
  <action>
    Create `Sources/Modules/Yggdrasil/KelpCurtain/NPCs/ValleyOfLushAndMoist/SerpentMoss.cs`, namespace `Everglow.Yggdrasil.KelpCurtain.NPCs.ValleyOfLushAndMoist`, same formatting rules, `Texture => Commons.ModAsset.White_Mod;`, a `LocalizationCategory`, and an XML doc comment naming the design row 陋・｡瑚渠, the region, the block ids `C8fRdR6uhoYxOKxrWzDc8IHwnud` / `RmwmdtUCwoMPI1xMVQrclFS4nuh`, the D-46 scope statement, and the D-49 migration note.

    Contents: `NPC.lifeMax = 100; NPC.life = 100;`, `NPC.damage = 15; NPC.defDamage = 15;`, `NPC.defense = 10; NPC.defDefense = 10; NPC.knockBackResist = 0f;`, `NPC.value = 200;` (2體ｶ), `NPC.rare = ItemRarityID.White;`, `NPC.aiStyle = -1;`, a stationary hit box, `NPC.catchItem = 0;`, `SpawnModBiomes = [ModContent.GetInstance<KelpCurtainBiome>().Type];`, and immunities `BuffID.Poisoned` and `BuffID.Confused`. `SpawnChance` gates on the two tokens plus `KelpCurtainSpawnConditions.IsDryLand(spawnInfo)` with a land weight.

    Give the class a private `enum SerpentMossState { Camouflaged = 0, Binding = 1 }` with a named property over `NPC.ai[0]`, a named `BindTimer` wrapper over `NPC.localAI[0]`, a named `BoundPlayerIndex` wrapper over `NPC.localAI[1]`, named constants `BindTriggerTiles = 2f`, `BindIntervalFrames = 60`, `BindDebuffTicks = 60`, `BindDamage = 15`, `PoisonChanceDenominator = 3` and `PoisonTicks = 900`, plus a named `AnchorPosition` so the stationary creature keeps its spawn point. `AI()` implements: the 2-tile trigger; the 60-frame bind loop applying `BuffID.Webbed` for 60 ticks and 15 damage to the bound player; the 33% (`Main.rand.NextBool(3)`) 900-tick `BuffID.Poisoned` roll after each damage tick; releasing the bind when the player leaves the trigger radius; and every authoritative write under `Main.netMode != NetmodeID.MultiplayerClient` with `NPC.netUpdate = true`. Apply the debuffs through the same path chosen in `LargeAlgaeOctopus` (state it in a comment, and keep the two consistent) and never store the bind timer on the `Player`. Add a class comment recording the D-46 disguised-hazard-visual-system blocker naming that system and this file. `ModifyNPCLoot(NPCLoot npcLoot)` adds no rule and references no item type. Use only existing KelpCurtain dust behind `if (!Main.dedServ)`; do not create a dust, buff or `.png`.
  </action>
  <verify>
    <automated>dotnet build /p:Configuration=Release /p:WarningLevel=0; if ($?) { powershell -NoProfile -ExecutionPolicy Bypass -File ".planning/phases/04-remaining-ordinary-monsters/scripts/check-biology.ps1" }</automated>
    <fails_when>non-zero exit 窶・an `error CS` (in particular a missing `BuffID.Webbed` member), the Phase 4 gate printing `FAIL(` / exiting 2 / reporting `UTF-8 BOM:`, or the gate's `OK: guarded classes =` count failing to rise by one against the previous task's run.</fails_when>
  </verify>
  <acceptance_criteria>
    - `SerpentMoss.cs` exists with `namespace Everglow.Yggdrasil.KelpCurtain.NPCs.ValleyOfLushAndMoist` and contains `Texture => Commons.ModAsset.White_Mod`, `LocalizationCategory`, `NPCSpawnManager.RegisterNPC`, `SubworldSystem.IsActive<YggdrasilWorld>`, `KelpCurtainBiome.IsKelpCurtainLayer`, `KelpCurtainSpawnConditions.IsDryLand`, `NPC.lifeMax = 100;`, `NPC.value = 200;`, `NPC.damage = 15`, `NPC.defense = 10`, `NPC.knockBackResist = 0f`, `BuffID.Poisoned`, `BuffID.Confused`, `BuffID.Webbed`, `enum SerpentMossState`, `BindTriggerTiles`, `BindIntervalFrames`, `BindDebuffTicks`, `BindDamage`, `PoisonTicks`, `Main.rand.NextBool(3)`, `Main.netMode != NetmodeID.MultiplayerClient`, `NPC.netUpdate`, `Main.dedServ` and `ModifyNPCLoot`.
    - The values `2`, `60`, `15`, `900` and the 33% roll are all reachable through the named constants/expressions; the file references no item type, no `Main.LocalPlayer` and no `KelpCurtainBiome.IsBiomeActive`.
    - No bind timer or bound-player state is stored on a `Player` object; the class comment records the D-46 disguise blocker and names the same debuff-application path as `LargeAlgaeOctopus`.
  </acceptance_criteria>
  <done>陋・｡瑚渠 binds a player who comes within 2 tiles, applies 60 ticks of 譚溽ｼ・and 15 damage every 60 frames with the design's 33% poison roll, is knockback-immune, and records the missing disguise presentation.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Reused template -> new class | `RedNeedleCaterpillar` inherits the repository's segmented-worm AI, so a mis-set segment count or a copied gore reference would break the creature or the build. |
| Cross-module item reference | The `CaterpillarJuice` drop points outside the Kelp Curtain item tree (OQ1). |
| Distance window -> player harm | The 4窶・ tile raspberry window and the 2-tile moss bind decide when a player is damaged or immobilised. |
| Unimplemented system -> implementation | The Valley disguised-hazard presentation does not exist; faking it would be indistinguishable from a bug (D-46). |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-04-41 | Denial of Service | A copied `PreKill` referencing `BarkSpicyCaterpillar_gore*` art that `RedNeedleCaterpillar` does not own | high | mitigate | The task forbids overriding `PreKill` and forbids adding a gore; the acceptance criteria grep for `override bool PreKill(` and `Gore.`; the gate's no-art invariant and the Release build are the second and third checks (D-51, Pitfall 3). |
| T-04-42 | Denial of Service | A `ModContent.ItemType<X>()` for an item that does not exist | high | mitigate | The only item referenced is the verified `CaterpillarJuice`; the gate resolves every item-type token under the whole `Sources/Modules/Yggdrasil` tree, which is exactly why that tree-wide index exists (OQ1, D-58, Pitfall 2). |
| T-04-43 | Tampering | Modifying the shared `Caterpillar` template or `BarkSpicyCaterpillar` to make the new class fit | high | mitigate | Neither file is in this plan's `files_modified`; the acceptance criteria require `git diff --stat` to be empty for the template, `BarkSpicyCaterpillar.cs` and `CaterpillarJuice.cs`; the class adapts to the template, never the reverse. |
| T-04-44 | Tampering | A player left permanently bound, or a defence switch that sticks at the attacking value | high | mitigate | `BuffID.Webbed` is applied for a bounded 60 ticks and never as a persistent state; the bind is released when the player leaves the 2-tile radius; the raspberry's defence restore lives in one `EnterState`-style helper on every exit path; the D-21 client bundle checks the release case (D-55). |
| T-04-45 | Elevation of Privilege | `SpawnChance` leaking a Valley hazard into the ordinary world | high | mitigate | All three classes return `0f` without the subworld token and `KelpCurtainBiome.IsKelpCurtainLayer(spawnInfo.Player)`; the gate asserts both tokens and rejects `Main.LocalPlayer` (D-52/D-55). |
| T-04-46 | Denial of Service | Graphics access on a dedicated server | medium | mitigate | Every dust call added here is inside `if (!Main.dedServ)`; the gate asserts `Main.dedServ` in any guarded class containing a dust/gore/VFX token (D-35). |
| T-04-47 | Repudiation | The disguise presentation presented as implemented | medium | mitigate | No disguise trick is built; the D-46 blocker naming the Valley disguised-hazard visual system and the affected files is written into the class comments and into `03-BIOLOGY.json` at close-out. |
| T-04-SC | Tampering | Package supply chain | low | accept | This phase installs no package (04-RESEARCH ﾂｧPackage Legitimacy Audit: not applicable). |
</threat_model>

<verification>
- `dotnet build /p:Configuration=Release /p:WarningLevel=0` exits 0 with no `error CS`.
- `.planning/phases/04-remaining-ordinary-monsters/scripts/check-biology.ps1` exits 0, prints `OK(0): phase4 in-scope set = 21 rows (rows=31)`, and its `OK: guarded classes =` count has risen by exactly five across this plan.
- All three creature classes carry the `White_Mod` override, the two spawn tokens, `KelpCurtainSpawnConditions.IsDryLand`, a `LocalizationCategory`, `NPCSpawnManager.RegisterNPC`, `Main.dedServ`, `NPC.netUpdate` and `ModifyNPCLoot`; none references `Main.LocalPlayer`, `Main.screenPosition` or `KelpCurtainBiome.IsBiomeActive`.
- `RedNeedleCaterpillar` is the only class referencing an item type, and only `CaterpillarJuice`; it extends `Caterpillar`, carries `[NoGameModeScale]`, the `-0.12f`/`-0.08f`/`0.04f` progression and both behaviour block constants.
- Both spike projectiles exist under the enemy-projectile tree with `White_Mod` and `NPC.GetSource_FromAI`.
- `Sources/Everglow.Function/Templates/Enemies/Caterpillar.cs`, `Sources/Modules/Yggdrasil/YggdrasilTown/NPCs/BarkSpicyCaterpillar.cs` and `CaterpillarJuice.cs` are unmodified.
- `.planning/phases/03-completed-art-ordinary-monsters/03-BIOLOGY.json` and `03-BIOLOGY.md` are byte-identical to their state at the end of plan 04-01.
- No `.png`, `.obj`, `.xnb` or other binary asset was created or modified; no HJSON was edited.
</verification>

<success_criteria>
- All three Valley of Lush and Moist hazard rows are present with their design triggers, windows, damage cadences, debuff mappings and immunities (BIO-03).
- The segmented-worm template and the `CaterpillarJuice` material are reused read-only rather than re-derived or duplicated, satisfying both 謗芽誠蜷梧醍坩蛻ｺ豈幄勠 and D-57 at once (OQ1).
- The two disguised-hazard presentations are recorded as precise D-46 blockers while the mechanics they hide behind are fully built, so no behaviour is missing and no fake stands in for the missing art.
- No new item, buff, dust, gore or binary asset was introduced.
</success_criteria>

<output>
Create `.planning/phases/04-remaining-ordinary-monsters/04-07-SUMMARY.md` when done.
</output>
