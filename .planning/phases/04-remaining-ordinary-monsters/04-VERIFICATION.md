---
phase: 04-remaining-ordinary-monsters
verified: 2026-09-16T11:30:41Z
status: human_needed
score: 31/53 must-haves verified
covered_files: [".planning/REQUIREMENTS.md", ".planning/phases/03-completed-art-ordinary-monsters/03-BIOLOGY.json", ".planning/phases/03-completed-art-ordinary-monsters/03-BIOLOGY.md", ".planning/phases/04-remaining-ordinary-monsters/04-01-PLAN.md", ".planning/phases/04-remaining-ordinary-monsters/04-01-SUMMARY.md", ".planning/phases/04-remaining-ordinary-monsters/04-02-PLAN.md", ".planning/phases/04-remaining-ordinary-monsters/04-02-SUMMARY.md", ".planning/phases/04-remaining-ordinary-monsters/04-03-PLAN.md", ".planning/phases/04-remaining-ordinary-monsters/04-03-SUMMARY.md", ".planning/phases/04-remaining-ordinary-monsters/04-04-PLAN.md", ".planning/phases/04-remaining-ordinary-monsters/04-04-SUMMARY.md", ".planning/phases/04-remaining-ordinary-monsters/04-05-PLAN.md", ".planning/phases/04-remaining-ordinary-monsters/04-05-SUMMARY.md", ".planning/phases/04-remaining-ordinary-monsters/04-06-PLAN.md", ".planning/phases/04-remaining-ordinary-monsters/04-06-SUMMARY.md", ".planning/phases/04-remaining-ordinary-monsters/04-07-PLAN.md", ".planning/phases/04-remaining-ordinary-monsters/04-07-SUMMARY.md", ".planning/phases/04-remaining-ordinary-monsters/04-08-PLAN.md", ".planning/phases/04-remaining-ordinary-monsters/04-08-SUMMARY.md", ".planning/phases/04-remaining-ordinary-monsters/04-09-PLAN.md", ".planning/phases/04-remaining-ordinary-monsters/04-09-SUMMARY.md", ".planning/phases/04-remaining-ordinary-monsters/04-DEVIATIONS.md", ".planning/phases/04-remaining-ordinary-monsters/04-REVIEW-FIX.md", ".planning/phases/04-remaining-ordinary-monsters/04-REVIEW.md", ".planning/phases/04-remaining-ordinary-monsters/04-UAT.md", ".planning/phases/04-remaining-ordinary-monsters/04-VALIDATION.md", ".planning/phases/04-remaining-ordinary-monsters/scripts/check-biology.ps1", "Sources/Modules/Yggdrasil/KelpCurtain/NPCs/DeathJadeLake/AlgaeOctopus.cs", "Sources/Modules/Yggdrasil/KelpCurtain/NPCs/DeathJadeLake/ArmoredShrimp.cs", "Sources/Modules/Yggdrasil/KelpCurtain/NPCs/DeathJadeLake/BombJellyfish.cs", "Sources/Modules/Yggdrasil/KelpCurtain/NPCs/DeathJadeLake/CannonBarnacle.cs", "Sources/Modules/Yggdrasil/KelpCurtain/NPCs/DeathJadeLake/FluorescentHydra.cs", "Sources/Modules/Yggdrasil/KelpCurtain/NPCs/DeathJadeLake/GiantTigerShrimp.cs", "Sources/Modules/Yggdrasil/KelpCurtain/NPCs/DeathJadeLake/GlowSalamander.cs", "Sources/Modules/Yggdrasil/KelpCurtain/NPCs/DeathJadeLake/JadeSpiritAnglerfish.cs", "Sources/Modules/Yggdrasil/KelpCurtain/NPCs/DeathJadeLake/LargeAlgaeOctopus.cs", "Sources/Modules/Yggdrasil/KelpCurtain/NPCs/DeathJadeLake/LargeBombJellyfish.cs", "Sources/Modules/Yggdrasil/KelpCurtain/NPCs/DeathJadeLake/Radiolarian.cs", "Sources/Modules/Yggdrasil/KelpCurtain/NPCs/DeathJadeLake/SailfinSnakehead.cs", "Sources/Modules/Yggdrasil/KelpCurtain/NPCs/DeathJadeLake/ToxicToad.cs", "Sources/Modules/Yggdrasil/KelpCurtain/NPCs/DeathJadeLake/WaterStrider.cs", "Sources/Modules/Yggdrasil/KelpCurtain/NPCs/KelpCurtainSpawnConditions.cs", "Sources/Modules/Yggdrasil/KelpCurtain/NPCs/SpinyMossCourt/AnimatedWitherbarkHound.cs", "Sources/Modules/Yggdrasil/KelpCurtain/NPCs/SpinyMossCourt/AnimatedWitherbarkSoldier.cs", "Sources/Modules/Yggdrasil/KelpCurtain/NPCs/SpinyMossCourt/AnimatedWitherbarkSoldierRanged.cs", "Sources/Modules/Yggdrasil/KelpCurtain/NPCs/SpinyMossCourt/AnimatedWitherbarkSoldierSpell.cs", "Sources/Modules/Yggdrasil/KelpCurtain/NPCs/SpinyMossCourt/BrodieFlydragon.cs", "Sources/Modules/Yggdrasil/KelpCurtain/NPCs/SpinyMossCourt/CourtCommander.cs", "Sources/Modules/Yggdrasil/KelpCurtain/NPCs/SpinyMossCourt/SmallBrodieFlydragon.cs", "Sources/Modules/Yggdrasil/KelpCurtain/NPCs/ValleyOfLushAndMoist/AssassinRaspberry.cs", "Sources/Modules/Yggdrasil/KelpCurtain/NPCs/ValleyOfLushAndMoist/LargeMossyThornTurtle.cs", "Sources/Modules/Yggdrasil/KelpCurtain/NPCs/ValleyOfLushAndMoist/RedNeedleCaterpillar.cs", "Sources/Modules/Yggdrasil/KelpCurtain/NPCs/ValleyOfLushAndMoist/SerpentMoss.cs", "Sources/Modules/Yggdrasil/KelpCurtain/NPCs/ValleyOfLushAndMoist/SmallGuppyConch.cs", "Sources/Modules/Yggdrasil/KelpCurtain/Projectiles/Enemies/AlgaeOctopus_InkCloud.cs", "Sources/Modules/Yggdrasil/KelpCurtain/Projectiles/Enemies/AnimatedWitherbarkSoldier_Boulder.cs", "Sources/Modules/Yggdrasil/KelpCurtain/Projectiles/Enemies/AnimatedWitherbarkSoldier_SpellBeam.cs", "Sources/Modules/Yggdrasil/KelpCurtain/Projectiles/Enemies/AssassinRaspberry_Spike.cs", "Sources/Modules/Yggdrasil/KelpCurtain/Projectiles/Enemies/BombJellyfish_Explosion.cs", "Sources/Modules/Yggdrasil/KelpCurtain/Projectiles/Enemies/LargeMossyThornTurtle_Boulder.cs", "Sources/Modules/Yggdrasil/KelpCurtain/Projectiles/Enemies/LargeMossyThornTurtle_Shockwave.cs", "Sources/Modules/Yggdrasil/KelpCurtain/Projectiles/Enemies/Radiolarian_WaterBolt.cs", "Sources/Modules/Yggdrasil/KelpCurtain/Projectiles/Enemies/RedNeedleCaterpillar_Spike.cs", "Sources/Modules/Yggdrasil/KelpCurtain/Projectiles/Enemies/ToxicToad_PoisonBubble.cs", "Sources/Modules/Yggdrasil/KelpCurtain/Projectiles/Enemies/ToxicToad_PoisonCloud.cs"]
covered_digest: "v1:sha256:292024175d8ef9afd99535b69c7a67af654d18094a194f3c8ca45b8e6b82c331"
behavior_unverified: 22
behavior_unverified_items:
  - truth: "碧灵鮟鱇 (JadeSpiritAnglerfish) spawns at the lake floor, hides far from the player (lamp excepted), opens with a reveal dash (60) then chases (30)."
    test: "Enter the Kelp Curtain's Death Jade Lake floor and approach a 碧灵鮟鱇 from beyond its hide range."
    expected: "It is invisible until the green lamp betrays it, then performs the high-damage reveal dash and settles into the normal chase."
    why_human: "Reveal/hide is a runtime state transition; the offline gate asserts only the spawn tokens and stat constants (D-21)."
  - truth: "In a tModLoader client the jade spirit anglerfish is found at the floor of the Death Jade Lake, is invisible until it reveals itself, and never appears in an ordinary world."
    test: "Run the D-21 bundle row 12, then idle in an ordinary world at the same layer depth."
    expected: "Only the subworld floor spawns it; no 碧灵鮟鱇 in the normal world."
    why_human: "Declared `verification: backstop`; spawn placement and isolation are runtime properties no offline artifact observes."
  - truth: "水黾 (WaterStrider) skims the water surface, dashes 60–200 frames (45–150 with a player nearby), and returns to water from land or the lake bed."
    test: "Watch a 水黾 on the lake surface and shove it onto land and to the bed."
    expected: "Short random surface dashes; on land, small hops toward the nearest water or repeated hops in one direction; from the bed it swims up."
    why_human: "Dash cadence and the return-to-water transition are runtime AI properties (D-21)."
  - truth: "剧毒蟾蜍 (ToxicToad) spawns on dry land, prefers 幽光蝾螈/水蛞蝓 over the player, and its 75 % 中毒 / 25 % 酸性毒液 split fires on contact and from its death cloud."
    test: "Provoke a 剧毒蟾蜍 on land with 幽光蝾螈/水蛞蝓 nearby and let it die."
    expected: "It targets the other creatures first, its bubble and death cloud apply the 75/25 split, and no 毒腺/牛黄 drops."
    why_human: "Target selection and the death-cloud spawn are runtime; only the roll expression and the empty loot table are statically checkable."
  - truth: "幽光蝾螈 (GlowSalamander) spawns underwater, dashes/melees underwater, flees 剧毒蟾蜍, and runs the 60 s / 10 s / suffocation moisture budget."
    test: "Meet a 幽光蝾螈 underwater, drag it onto land, and time the 潮湿值 cycle."
    expected: "60 s out of water, a 10 s returning instinct, then 1 s of BuffID.Suffocation every 30 frames at zero."
    why_human: "The moisture timer is a runtime state machine over NPC.localAI[]; no offline test exercises it (D-21)."
  - truth: "装甲虾 (ArmoredShrimp) spawns in water only, appears in groups of 2–5, moves as a group, and never flees predators."
    test: "Find an 装甲虾 shoal in the lake and observe its group count and movement while a predator is nearby."
    expected: "2–5 members drifting together on one heading; no flee reaction."
    why_human: "Group formation and shoal motion are runtime spawn/AI properties (the gate checks only the marker constants)."
  - truth: "爆弹水母 (BombJellyfish) and LargeBombJellyfish hover without drifting and self-destruct for 30 / 50 damage on death."
    test: "Kill a small and a large 爆弹水母 at melee range and check the explosion damage."
    expected: "No drift; a BombJellyfish_Explosion for 30 (small) / 50 (large)."
    why_human: "The death blast is a runtime hit-effect transition; only the two constants and the class split are static."
  - truth: "帆鳍鳢 (SailfinSnakehead) cruises at constant speed unprovoked, charges only after aggro, avoids 幽光蝾螈 and attacks 水蛞蝓."
    test: "Observe an unprovoked 帆鳍鳢, hit it, and place it with a 幽光蝾螈 and a 水蛞蝓."
    expected: "Constant cruise → aggro charge; it avoids the salamander and strikes the slug."
    why_human: "The aggro window and the cross-creature avoid/attack rules are runtime AI (D-21)."
  - truth: "放射虫 (Radiolarian) fires Radiolarian_WaterBolt while submerged and dashes through a target within 4 tiles on a 300-frame cooldown."
    test: "Approach a submerged 放射虫, then close inside 4 tiles."
    expected: "Continuous bolts at range; a 4-tile dash then resumed fire on a 300-frame cooldown."
    why_human: "The ranged/dash state transition is runtime; only the constants (15 def, 0.9× DR, 0.2 knockback, 300 frames, 4 tiles) are static."
  - truth: "覆藻章鱼 (AlgaeOctopus) hides via NPC.alpha at distance, applies BuffID.Slow on contact, and leaves ink puffs that cause 2 s of BuffID.Darkness while dashing."
    test: "Approach a distant 覆藻章鱼, touch it, then chase it through a dash."
    expected: "It resolves from hidden, slows on contact, and its ink trail inflicts Darkness."
    why_human: "The stealth alpha transition and the ink-trail debuff are runtime properties (OQ4/D-21)."
  - truth: "大型覆藻章鱼 (LargeAlgaeOctopus) grabs with Webbed + Suffocation every 60 frames, emits a 4-cloud wave every 180 frames, and flees leaving ink when low on life."
    test: "Let a 大型覆藻章鱼 grab the player, then drive it to low life."
    expected: "A 2 s grab cycle every 60 frames, a 4-cloud wave every 180 frames (first aimed), and an ink-leaving flee."
    why_human: "The grab/wave/flee cycle is the class's custom state machine; no offline test exercises it (D-21)."
  - truth: "The ranged soldier picks a boulder for 120 immobile frames, throws, then holds ~8 tiles for 200 frames; the spell soldier casts on a fixed interval then teleports; the hound charges."
    test: "Fight each 枯木活化士兵 variant through a full attack cycle."
    expected: "The documented per-variant attack patterns and spacing."
    why_human: "Multi-step attack state machines are runtime; the gate checks only the netmode guard and netUpdate."
  - truth: "王庭号令者 (CourtCommander) wanders unaggroed, summons 1–3 variants on spawn/first staff if none are nearby, and keeps ≥10 tiles from the player."
    test: "Meet a 王庭号令者 and let it aggro with no soldier nearby."
    expected: "An aimless wander, a one-per-aggro 1–3 summon, and a 10-tile stand-off."
    why_human: "The once-per-aggro summon and spacing are runtime AI; only the flag, the 1–3 constants and the netmode guard are static."
  - truth: "布罗迪蝇蜓 (BrodieFlydragon / SmallBrodieFlydragon) fly and melee-approach the player inside the layer."
    test: "Meet both 布罗迪蝇蜓 sizes in the layer and let them aggro."
    expected: "Hover/pursue then contact melee at the two designed stat rows."
    why_human: "The fly-and-press feel is runtime; the two stat rows and the blockers are static."
  - truth: "红针洋辣子 (RedNeedleCaterpillar) fires a 4–6 spike volley every 180 frames at ≥4 tiles and applies the 25 % / 37.5 % poison split."
    test: "Stand 4+ tiles from a 红针洋辣子 and let it volley; take both a contact hit and a spike."
    expected: "4–6 needles every 180 frames from the synced centre; the documented poison split on both hit paths."
    why_human: "Volley cadence, origin placement and the proc outcomes are runtime (the WR-03 fix can only be observed in a client)."
  - truth: "阿萨辛覆盘子 (AssassinRaspberry) stays buried beyond 8 tiles, extends inside 8, and scatters 4–6 ground spikes only in the 4–8 tile window."
    test: "Walk the 4–8 tile band around a 阿萨辛覆盘子, then stand inside 4 tiles."
    expected: "It extends and scatters spikes in the window, and stays extended but silent below 4 tiles."
    why_human: "The ambush window and the static/attacking defence switch are runtime combat properties (D-46)."
  - truth: "蛇行苔 (SerpentMoss) spawns as a plant, binds within 2 tiles, and every 60 frames applies 60 ticks of Webbed + 15 damage with a 33 % poison roll."
    test: "Walk within 2 tiles of a disguised 蛇行苔 and stay bound for several cycles."
    expected: "A 2-tile bind, the 60-frame Webbed/damage cadence, and the 33 % 15 s poison roll after each tick."
    why_human: "The bind cycle is a runtime state machine and the disguise presentation is an unimplemented system (D-46)."
  - truth: "小格普螺 (SmallGuppyConch) is a passive, slow, land-crawling creature with 20 life / 5 defence / 5 % damage reduction."
    test: "Meet a 小格普螺 on land and hit it."
    expected: "A slow crawl with the 0.95× final-damage reduction and an empty loot table."
    why_human: "The crawl/rest rhythm is runtime; only the stat row and the 0.95× modifier are static."
  - truth: "大型荆棘苔龟 (LargeMossyThornTurtle) implements the full four-state Mini Boss machine with the designed frame budgets."
    test: "Fight 大型荆棘苔龟 through states 1→2→3→4 at the designed ranges, including a target lost mid-flight."
    expected: "Retract (999 def, 2–20 reflect, 100/320-frame exits), floor slam (120/120), tile-ignoring dive + landing slam + 3 boulders, 240-frame retreat."
    why_human: "The state machine, the reflect locality and the WR-01 flight-flag sync are runtime and multiplayer-only observations (D-21)."
  - truth: "大型荆棘苔龟 crawls at 1 tile/s passively, turns hostile only when damaged at ≤900 life, and carries the designed stat/immunity row."
    test: "Approach an undamaged 大型荆棘苔龟, then damage it below 900 life."
    expected: "No proactive attack while passive; a hostility transition at the threshold; the designed contact damage/defence/immunity values."
    why_human: "The hostility threshold transition is runtime (the stat row itself is statically verified)."
  - truth: "In a tModLoader client all 21 in-scope creatures can be encountered in the Kelp Curtain in their designed bands and none appears in a normal world."
    test: "Run the consolidated end-of-phase bundle in 04-UAT.md (all 21 rows, isolation, dedicated server, localization)."
    expected: "Every creature is met in its band, each wired drop is received, nothing leaks to the normal world, and the log stays free of missing-resource/disabled-mod entries."
    why_human: "Declared `verification: backstop`; this is the D-21 client bundle, recorded not-executed in 04-UAT.md."
human_verification:
  - test: "All 24 checks in 04-UAT.md — one per in-scope row (checks 1–21), isolation (22), dedicated server / multiplayer (23), localization fallback (24)."
    expected: "Every creature loads and appears in its designed Kelp Curtain band; none appears in an ordinary world; each wired drop fires; no client-only crash on a dedicated server; clean fallback display names with no missing-resource log entry."
    why_human: "The whole runtime half of the phase: spawn placement, isolation, AI feel, drops, dedicated-server behaviour and localization fallback are runtime properties (D-21). The bundle is recorded and NOT executed."
  - test: "Multiplayer AI-branch agreement after the CR-02 fix (the five flags moved from NPC.localAI[] to NPC.ai[])."
    expected: "In a multiplayer client the provoked/aggro/heading flags match the server, so the hound, ranged/spell soldiers, 帆鳍鳢 and 装甲虾 run the same AI branch on both sides."
    why_human: "A compile cannot prove the client branch now matches the server; only a live multiplayer observation can (04-REVIEW-FIX.md: CR-02 'requires human verification')."
  - test: "LargeMossyThornTurtle state-3 dive and RedNeedleCaterpillar volley origin on both sides after the WR-01/WR-03 fixes."
    expected: "A multiplayer client no longer keeps gravity/tile collision through the dive, and the needle volley emerges from the worm the client actually renders."
    why_human: "Both are sync/ordering fixes whose correctness is only observable at runtime (WR-01/WR-03 'requires human verification')."
deferred:
  - truth: "The design's absent drop materials (毒腺, 牛黄, 软体甲壳碎片, 亡碧膏, 枯木碎块, 干涸心脏, the turtle's and large octopus's TBD lists) are obtainable."
    addressed_in: "Item phases (blockers recorded; no item scope promoted into Phase 4)"
    evidence: "04-DEVIATIONS.md §6.2/§14.2 absent-drop register; every one is a `drop:` blocker in 03-BIOLOGY.json (D-58)."
  - truth: "The Spiny Moss Court morale/command system, the Valley egg-laying system and the Valley disguised-hazard visuals are implemented."
    addressed_in: "Later phase (D-46 blockers)"
    evidence: "04-DEVIATIONS.md §14.2/§14.5: the trigger/window/damage/summon/bind mechanics exist; only the named system is deferred."
  - truth: "The 森雨幽谷 / 刺苔庭园 regional spawn predicates exist."
    addressed_in: "Phases 5–6 (terrain)"
    evidence: "ROADMAP Phase 5/6 terrain scope; the `region:` blocker on 覆藻章鱼, 大型覆藻章鱼 and 布罗迪蝇蜓 (D-52/D-53)."
  - truth: "The 26 ModNPC sprites and 11 projectile sprites are delivered."
    addressed_in: "Artwork delivery (D-49 migration)"
    evidence: "04-DEVIATIONS.md §3/§4: the three-step move (add `.png`, delete the `Texture` override, delete the blocker)."
  - truth: "en-US / zh-Hans localization and the per-item Feishu synchronization land."
    addressed_in: "Phase 8 (QUAL-04 / ITEM-07)"
    evidence: "D-20 (localization deferred by user directive) and ROADMAP Phase 8 SC5; every row stays `status: unchecked`."
overrides_applied: 0
---

# Phase 4: Remaining Ordinary Monsters Verification Report

**Phase Goal:** Ordinary creatures without complete design textures are implemented after the completed-art tranche, with all ordinary-creature behavior integrated against the item drops already classified and implemented in Phases 1–2.
**Verified:** 2026-09-16T11:30:41Z
**Status:** human_needed
**Re-verification:** No — initial verification

## Goal Achievement

The remaining ordinary-creature tranche is the 21 non-deferred `phase: 4` rows of the shared `03-BIOLOGY.json` matrix (D-44) — the 23 `phase: 4` rows minus the two `out of phase` hardmode designs (D-44/V2-HARD-01). All 21 rows are `code_complete: true` with an `internal_name` resolving to an on-disk class, implemented across **26 `ModNPC` classes** and **11 hostile projectiles** (26 + 11 = the gate's 37 guarded classes). Five drop types resolve to items that already exist (`MeatLantern`, `Photophore`, `RadialCarapace`, `ActivatedDogStaff`, and the cross-namespace `CaterpillarJuice`); every absent drop, capture item, unimplemented system and missing region predicate is recorded as a precise blocker with no dangling type reference, so no item scope was promoted into the phase. The shared matrix, the consolidated ledger, the D-21 client bundle (`04-UAT.md`, 24 checks) and the completed validation contract (`04-VALIDATION.md`, 23 rows) are all on disk and internally consistent. The post-close-out code review's 2 Critical + 3 Warning findings are all fixed in the committed source.

Nothing is FAILED and no artifact is MISSING/STUB/unwired. The phase's entire runtime half — spawn placement, isolation, AI feel, drop acquisition, dedicated-server behaviour, localization fallback — is, by the design-owner's rule D-21, observable only in a live tModLoader client and is therefore recorded under human verification rather than counted as a gap.

### Observable Truths

| # | Truth | Status | Evidence |
| --- | --- | --- | --- |
| 1 | Phase 4's in-scope set is exactly the 21 non-deferred `phase == 4` rows (D-44); the two hardmode rows stay `deferred: true` (V2-HARD-01). | ✓ VERIFIED | `03-BIOLOGY.json` counts `{rows 31, phase3 5, phase4 23, deferred 2}`; in-scope query = 21; deferred set = `withered-seed`, `withered-tree-guardian` with non-empty `deferred_reason`. Gate invariant 3/4 green. |
| 2 | Every one of the 21 in-scope rows carries a non-empty artwork blocker naming `Commons.ModAsset.White_Mod` (D-48/D-50). | ✓ VERIFIED | Every in-scope row has an `artwork:` blocker containing `Commons.ModAsset.White_Mod` (e.g. "artwork: approved texture (贴图) missing from repository; class uses Commons.ModAsset.White_Mod (D-48)"). Gate invariant 5 green. |
| 3 | `scripts/check-biology.ps1` is 100% ASCII PS 5.1, reads the shared JSON via `[IO.File]::ReadAllText`, resolves classes with `Get-ChildItem -Recurse` (never `git ls-files`), exits 0/1/2, and leaves the Phase 3 script byte-identical (OQ3). | ✓ VERIFIED | 393-line gate; `ReadAllText`/`Get-ChildItem` used; no `ls-files` token; `exit 2`/`exit 1`/`exit 0`. Phase 3 gate byte-identical: `git log 7513c752f..HEAD -- <phase3 gate>` empty, 15968 bytes unchanged. |
| 4 | `KelpCurtainSpawnConditions.cs` provides the three server-safe water/land predicates (`IsDryLand`, `IsWaterSurface`, `IsWaterBottom`) derived only from `NPCSpawnInfo` and world tile state (D-52/D-53/D-55). | ✓ VERIFIED | Public `IsDryLand`/`IsWaterSurface`/`IsWaterBottom` present plus the D-54 weight bands (`LandWeight 1f`, `WaterWeight 0.75f`, `RareWaterBottomWeight 0.35f`, `MiniBossWeight 0.1f`). |
| 5 | 碧灵鮟鱇 (`JadeSpiritAnglerfish`) spawns at the lake bottom, hides until its lamp reveals it, opens with a 60-damage reveal dash then chases at 30, and drops `MeatLantern` 25 % / `Photophore` 12.5 %. | ⚠️ PRESENT_BEHAVIOR_UNVERIFIED | Present + wired: spawn gated on subworld + layer + `IsWaterBottom`; drops `ItemDropRule.Common(MeatLantern, 4, 1, 1)` + `(Photophore, 8, 1, 1)` (both items on disk). The hide/reveal state transition is runtime — see Human Verification (D-21). |
| 6 | `04-DEVIATIONS.md` opens the phase ledger and records every OQ1–OQ5 resolution, the variant strategy, the region-folder decision, the conservative spawn weights/drop denominators, and the 酸性毒液 → `BuffID.Venom` mapping. | ✓ VERIFIED | 14 sections + subsection headings; §2 variant strategy, §3 folder decision, §5 spawn routing, §6 drop wiring, §7 OQ4, §8 OQ5, §9 OQ3, §10 defaults register, §14 close-out. |
| 7 | [backstop] In a client the jade spirit anglerfish is found at the lake floor, invisible until its green lamp reveals it, and never in an ordinary world. | ⚠️ PRESENT_BEHAVIOR_UNVERIFIED | Code present and wired; declared `verification: backstop`; no offline test exercises the reveal/isolation. Routed to human verification. |
| 8 | 水黾 (`WaterStrider`) skims the water surface, dashes 60–200 frames (45–150 near a player), returns to water from land and from the bed, and drops nothing. | ⚠️ PRESENT_BEHAVIOR_UNVERIFIED | Present + wired: `SpawnChance` → `IsWaterSurface`, empty `ModifyNPCLoot`. Dash cadence and the return-to-water transition are runtime (D-21). |
| 9 | 剧毒蟾蜍 (`ToxicToad`) spawns on dry land, prefers 幽光蝾螈/水蛞蝓 over the player, applies the 75 %/25 % split on contact and its death cloud, and ships an empty loot table with a 毒腺/牛黄 blocker. | ⚠️ PRESENT_BEHAVIOR_UNVERIFIED | Present + wired: `SpawnChance` → `IsDryLand`; `Main.rand.NextBool(3, 4)` at both hit paths (CR-01 fixed); `ToxicToad_PoisonCloud` on death; empty loot. Target preference/death-cloud are runtime. |
| 10 | 幽光蝾螈 (`GlowSalamander`) spawns underwater, dashes/melees underwater, flees 剧毒蟾蜍, and runs the 60 s / 10 s / suffocation budget. | ⚠️ PRESENT_BEHAVIOR_UNVERIFIED | Present + wired: `spawnInfo.Water` gate; `SuffocationInterval = 30`. The moisture state machine is runtime (D-21). |
| 11 | All three (04-02) classes override `White_Mod`, carry `LocalizationCategory`, register via `RegisterNPC`, keep dust/lighting behind `!Main.dedServ`, and write authoritative state under `NetmodeID.MultiplayerClient` guards with `NPC.netUpdate`. | ✓ VERIFIED | Byte scan per class: `White_Mod` 2×, `LocalizationCategory` 1×, `RegisterNPC` 1×, `Main.dedServ` present; gate invariants 7/8 green. |
| 12 | No class in 04-02 touches `03-BIOLOGY.json` (D-24/D-50). | ✓ VERIFIED | `03-BIOLOGY.json`/`.md` diff shows only the 04-01 seed and the 04-09 reconciliation commits; no 04-02/03/07/08 edit (each wave summary asserts the same and the commit range confirms). |
| 13 | 装甲虾 (`ArmoredShrimp`) is passive, water-only subworld spawn, groups of 2–5 moving together, never flees, absent drop out of the table. | ⚠️ PRESENT_BEHAVIOR_UNVERIFIED | Present + wired: `MinGroupSize 2`/`MaxGroupSize 5`, bounded via the `FollowerMarker` (ai[1]); empty loot. Group formation/shoal motion are runtime. |
| 14 | 爆弹水母 (`BombJellyfish`) and `LargeBombJellyfish` are passive hovering, no-drift, knockback-immune classes that self-destruct for 30 / 50 damage through `BombJellyfish_Explosion`. | ⚠️ PRESENT_BEHAVIOR_UNVERIFIED | Present + wired: `DeathBlastDamage 30` / `50`, both classes `White_Mod` + hostile; `BombJellyfish_Explosion` exists. Hover/no-drift and the death blast are runtime. |
| 15 | 帆鳍鳢 (`SailfinSnakehead`) is neutral: constant cruise unprovoked, charges after aggro, avoids 幽光蝾螈 and attacks 水蛞蝓, empty loot. | ⚠️ PRESENT_BEHAVIOR_UNVERIFIED | Present + wired: `AggroTimer`/`ChargeCooldown` now on synced `ai[1]`/`ai[2]` (CR-02 fixed), empty loot. The aggro window and cross-creature rules are runtime. |
| 16 | Every 04-03 class overrides `White_Mod`, registers, is subworld+layer gated, keeps graphics behind `!Main.dedServ`, writes state under the netmode guard with `netUpdate`. | ✓ VERIFIED | Per-file token scan clean; gate invariants 7/8 green. |
| 17 | No catch item is invented: 装甲虾 and 爆弹水母 keep `NPC.catchItem = 0` with no `Main.npcCatchable` and no `NPCID.Sets.CountsAsCritter`. | ✓ VERIFIED | Zero `npcCatchable`/`CountsAsCritter` writes across all 26 classes; `catchItem = 0` present in all applicable classes. |
| 18 | 放射虫 (`Radiolarian`) spawns in shallow water at the lowest aquatic weight, fires `Radiolarian_WaterBolt` submerged, dashes on a 300-frame/4-tile trigger, carries 15 def / 10 % DR / 80 % knockback resist / confusion immunity, and wires `RadialCarapace` 6.7 % with blockers. | ⚠️ PRESENT_BEHAVIOR_UNVERIFIED | Present + wired: `DashTriggerTiles 4f`, `DashCooldownFrames 300`, `defense 15`, `modifiers.FinalDamage *= 0.9f`, `knockBackResist 0.2f`, `SpecificDebuffImmunity[Confused]`, `ItemDropRule.Common(RadialCarapace, 15, 1, 1)`. Fire/dash cadence is runtime. |
| 19 | 覆藻章鱼 (`AlgaeOctopus`) hides via `NPC.alpha` at distance, applies `BuffID.Slow` on contact, and leaves ink puffs causing 2 s `BuffID.Darkness` while dashing. | ⚠️ PRESENT_BEHAVIOR_UNVERIFIED | Present + wired: `NPC.alpha` stealth + `AlgaeOctopus_InkCloud` (hostile, `White_Mod`). The stealth transition and ink debuff are runtime (OQ4). |
| 20 | 大型覆藻章鱼 (`LargeAlgaeOctopus`) grabs with Webbed + Suffocation for 2 s every 60 frames, emits a 4-cloud wave every 180 frames, and flees with ink at low life. | ⚠️ PRESENT_BEHAVIOR_UNVERIFIED | Present + wired: `GrabInterval 60`, `GrabDebuffTicks 120`, `InkWaveInterval 180`. The grab/wave/flee cycle is runtime (D-21). |
| 21 | All three (04-04) classes override `White_Mod`, register, are subworld+layer gated, keep dust behind `!Main.dedServ`, write state under the netmode guard with `netUpdate`. | ✓ VERIFIED | Per-file token scan clean; gate invariants 7/8 green. |
| 22 | The 森雨幽谷 regional half of 覆藻章鱼's spawn condition is recorded as a precise Phase 5–6 blocker, never approximated with a coordinate test. | ✓ VERIFIED | `region:` blocker on `bio-death-jade-lake-algae-octopus` (and the large octopus); no coordinate range in any `SpawnChance` (gate + review). |
| 23 | 荧光水螅 (`FluorescentHydra`), 巨型虎虾 (`GiantTigerShrimp`) and 炮弹藤壶 (`CannonBarnacle`) exist as loadable identity-only shell classes (D-45). | ✓ VERIFIED | Three 97–99-line classes under `NPCs/DeathJadeLake/`, registered, `White_Mod`, `LocalizationCategory`; each row `code_complete: true` with the `behavior undefined in the design row (D-29/D-45 shell)` blocker. |
| 24 | Each shell registers, overrides `White_Mod` and `LocalizationCategory`, spawns only inside the subworld+layer, and carries the behaviour-undefined blocker plus its missing-artwork blocker. | ✓ VERIFIED | Per-file tokens present; blockers confirmed on all three rows; `SpawnChance` gated on both spawn tokens. |
| 25 | Each shell declares conservative documented defaults with a comment stating every value is this phase's conservative default pending the design (D-45/D-54). | ✓ VERIFIED | e.g. `CannonBarnacle`: `aiStyle 0`, `lifeMax 50`, `damage 15`, `defense 10`, `value 0`, `ItemRarityID.White`, with the D-45/D-54 comment block at `SetDefaults`. |
| 26 | No shell declares a real `AI()`, custom attack, projectile, buff or loot rule, so the D-49 migration is add-`.png` / delete-override / delete-blocker. | ✓ VERIFIED | 0 `Projectile.NewProjectile`, 0 `ModifyNPCLoot`, 0 `ItemDropRule`, 0 `BuffID.` across the three shells; the single `AI(` token is a comment ("needs no AI() body of its own"). |
| 27 | The shells keep `NPC.catchItem = 0` with no `Main.npcCatchable` and no `NPCID.Sets.CountsAsCritter`. | ✓ VERIFIED | `catchItem = 0` present; zero catchable/critter writes across all 26 classes. |
| 28 | 枯木活化士兵 is four separate `ModNPC` classes each carrying its own design stat row, never faked by mutating `NPC.lifeMax` at runtime (OQ2, Pitfall 5). | ✓ VERIFIED | Four classes with distinct `SetDefaults`: 80/22/4, 60/35/0, 55/24/0, 75/20/2. `lifeMax =` appears exactly once per class (in `SetDefaults`) — no runtime mutation anywhere in the 26 classes. |
| 29 | The melee/ranged/spell/hound variants run the documented per-variant attack cycles. | ⚠️ PRESENT_BEHAVIOR_UNVERIFIED | Present + wired (ranged boulder projectile, spell `_SpellBeam` 3-ray spray + teleport, hound charge, all under the netmode guard). Multi-step attack state machines are runtime. |
| 30 | All four soldier variants are neutral by default and carry the morale/command system as a D-46 blocker; the classes expose a seam so a later buff needs no rework. | ✓ VERIFIED | `ProvokedFlag`/`IsNeutral` on synced `ai[1]`; named morale seam members; the D-46 `system:` blocker on the soldier and commander rows. |
| 31 | Only the hound wires a drop (`ActivatedDogStaff` 9 %, denominator 11); 枯木碎块/干涸心脏 are omitted with a blocker and no type reference. | ✓ VERIFIED | Single `ItemDropRule.Common(ActivatedDogStaff, 11, 1, 1)` in `AnimatedWitherbarkHound`; the other eight classes reference no item type; gate invariant 8 green. |
| 32 | 王庭号令者 (`CourtCommander`) spawns at low weight, wanders unaggroed, summons 1–3 random variants once per aggro entry, keeps ≥10 tiles, and ships an empty commented loot table. | ⚠️ PRESENT_BEHAVIOR_UNVERIFIED | Present + wired: `SummonMin 1`/`SummonMax 3`, `HasSummonedThisAggro` on `localAI[2]`, `NPC.NewNPC` under the netmode guard, `MiniBossWeight`-style low weight. The summon/spacing feel is runtime. |
| 33 | 布罗迪蝇蜓 (`BrodieFlydragon` + `SmallBrodieFlydragon`) fly and melee-approach the player; the egg system and 刺苔庭园/森雨幽谷 region halves are precise blockers. | ⚠️ PRESENT_BEHAVIOR_UNVERIFIED | Two classes with the design's two stat rows (40/25/5 and 20/15/2), no egg class, `region:` blocker present. The fly/melee feel is runtime. |
| 34 | Every 04-06 class overrides `White_Mod`, registers, is subworld+layer gated, keeps graphics behind `!Main.dedServ`, writes state under the netmode guard with `netUpdate`. | ✓ VERIFIED | Per-file token scan clean over the nine files; gate invariants 7/8 green (guarded classes 9 → 18 across the plan). |
| 35 | 红针洋辣子 (`RedNeedleCaterpillar`) reuses the `Caterpillar` template and mirrors `BarkSpicyCaterpillar` with 60 life / 20 damage / 4 defence / `-0.12f`–`-0.04f` knockback, poison immunity, 80 copper, and a 4–6 spike volley every 180 frames at ≥4 tiles. | ⚠️ PRESENT_BEHAVIOR_UNVERIFIED | Present + wired: constants `MinVolleyRangeTiles 4f`, `VolleyIntervalFrames 180`, `MinSpikes 4`/`MaxSpikes 6`, `lifeMax 60`/`damage 20`/`defense 4`/`value 80`, knockback `-0.12/-0.08/-0.04`, `SpecificDebuffImmunity[Poisoned]`, template reuse. The volley cadence/origin are runtime (WR-03 fix is client-only). |
| 36 | Its contact and spike hits apply the 25 %/37.5 % split, and its loot wires the pre-existing `CaterpillarJuice` (denominator 1, 1–2). | ⚠️ PRESENT_BEHAVIOR_UNVERIFIED | Present + wired: `NextBool(4)` → 1200 ticks, `Next(8) < 3` → 600 ticks, `ItemDropRule.Common(CaterpillarJuice, 1, 1, 2)`. The on-hit proc outcomes are runtime. |
| 37 | 阿萨辛覆盘子 (`AssassinRaspberry`) stays buried beyond 8 tiles, extends inside 8, scatters 4–6 spikes only in the 4–8 window, defence 20 passive / 4 attacking, knockback-immune, confusion+poison immune. | ⚠️ PRESENT_BEHAVIOR_UNVERIFIED | Present + wired: `ExtendTiles 8f`, `AttackMinTiles 4f`, `MinSpikes 4`/`MaxSpikes 6`, defence ternary in `EnterState`, `knockBackResist 0f`, both immunities. The ambush window is runtime. |
| 38 | 蛇行苔 (`SerpentMoss`) spawns disguised, binds within 2 tiles, applies 60 ticks of Webbed + 15 damage every 60 frames with a 33 % 15 s poison roll, knockback-immune, confusion+poison immune. | ⚠️ PRESENT_BEHAVIOR_UNVERIFIED | Present + wired: bind cadence in `NPC.localAI`, `BuffID.Webbed`, the 1-in-3 roll, both immunities. The bind cycle is runtime (D-46). |
| 39 | All three 04-07 classes override `White_Mod`, register, are subworld+layer+dry-land gated, keep graphics behind `!Main.dedServ`, write state under the netmode guard with `netUpdate`. | ✓ VERIFIED | Per-file token scan clean; gate invariants 7/8 green. |
| 40 | The Valley disguise presentation for 阿萨辛覆盘子 and 蛇行苔 is recorded as a precise D-46 blocker naming the missing visual system and affected files. | ✓ VERIFIED | `system:` blocker on both rows naming the Valley disguised-hazard visual system; no disguise trick built. |
| 41 | 小格普螺 (`SmallGuppyConch`) is a passive, slow, land-crawling creature with 20 life / 5 defence / 5 % reduction, `catchItem = 0`, and an empty commented loot table. | ⚠️ PRESENT_BEHAVIOR_UNVERIFIED | Present + wired: `lifeMax 20`, `defense 5`, `modifiers.FinalDamage *= 0.95f`, `catchItem = 0`, empty loot. The crawl/rest feel is runtime. |
| 42 | 大型荆棘苔龟 (`LargeMossyThornTurtle`) implements the full four-state Mini Boss machine with the designed frame budgets (OQ5). | ⚠️ PRESENT_BEHAVIOR_UNVERIFIED | Present + wired: the state enum (Retracted/FloorSlam/AerialSlam/Retreating), `RetractedDefense 999`, `FlightMaxFrames 180`, `BoulderSpawnHeightTiles 30f`, reflect in `OnHitByItem`/`OnHitByProjectile` clamped 2–20. The state cycle is runtime. |
| 43 | The turtle crawls at 1 tile/s passively, turns hostile only when damaged at ≤900 life, and carries 1000 life / 50 contact / defence 10 normally and 999 retracted / knockback immunity / 20 % DR / confusion+venom immunity / 10000 copper with an empty loot table. | ⚠️ PRESENT_BEHAVIOR_UNVERIFIED | Present + wired: the stat row, the single `EnterState` defence helper (`NPC.defense` assigned only in `SetDefaults` + the helper), empty loot (design TBD blocker). The hostility threshold transition is runtime. |
| 44 | Both 04-08 classes override `White_Mod`, register, are subworld+layer+dry-land gated, keep graphics behind `!Main.dedServ`, write state under the netmode guard with `netUpdate`. | ✓ VERIFIED | Per-file token scan clean; gate invariants 7/8 green (guarded classes 23 → 27). |
| 45 | No state-3 blocker is recorded for the turtle: the OQ5 call is full implementation, recorded in `04-DEVIATIONS.md` §8. | ✓ VERIFIED | §8 records the full-implementation call; the turtle's row blockers are only artwork + TBD loot + the canonical tail. |
| 46 | All 21 in-scope rows read `code_complete: true` with a resolving `internal_name`, and `check-biology.ps1 -RequireAll` exits 0 (D-24, QUAL-04). | ✓ VERIFIED | Ran the gate: `OK(0): phase4 in-scope set = 21 rows (rows=31)`, `OK: reconciled rows = 21 / 21`, `OK: guarded classes = 37`, BOM check passed, exit 0. All 21 `internal_name`s resolve to on-disk classes. |
| 47 | Every in-scope row's blockers state its real disposition (artwork/`White_Mod`, D-45 behaviour-undefined, absent drops/captures, unimplemented systems, region gaps, the D-20/D-21 tail). | ✓ VERIFIED | Read all 21 blocker arrays: 3–7 elements each; the D-45 marker on the three shells, `drop:`/`capture:` on the item-scope gaps, `system:` on the five D-46 rows, `region:` on the three Phase 5–6 rows, and the canonical tail on every row. |
| 48 | The frozen counts/sets are unchanged, the two hardmode rows stay deferred, and every row's `status` stays `unchecked` (no Feishu writes). | ✓ VERIFIED | Counts identical to the frozen block; 5/6/9-id sets intact; 31 rows all `unchecked`; gate invariants 2/3/4 green. |
| 49 | `04-DEVIATIONS.md` becomes the single consolidated ledger with the mechanically generated Blockers table (byte-identical to the JSON), close-out counts, decision disposition and deferred registry. | ✓ VERIFIED | §14.1–§14.7 present; §14.1 is an 83-row table generated from the JSON (byte-diff recorded in `04-09-SUMMARY`); §14.2 counts, §14.3 D-44…D-60 disposition, §14.5 deferred registry, §14.7 review-fix register. |
| 50 | The full offline chain is green in one run (Release build, Phase 4 + Phase 3 gates, five Phase 1 gates, Phase 2 gate, Yggdrasil tests, BOM check). | ✓ VERIFIED | Independently re-ran: Release build exit 0 (0 warnings / 0 errors, `Everglow.tmod` packed); Phase 4 gate exit 0; Phase 3 gate exit 0 (`5 / 5`); Phase 2 gate exit 0 (`21 / 21`). The Phase 1 gates were re-run by the close-out and are not re-executed here. |
| 51 | `04-UAT.md` records the D-21 client bundle with every entry marked not executed. | ✓ VERIFIED | 24 `### N.` checks, every one `result: not-executed` / `blocked_by: runtime-environment`; the header states the bundle is recorded, not run. |
| 52 | `04-VALIDATION.md` is completed rather than left as a placeholder: a one-row-per-task map across the nine plans, a ticked sign-off checklist, and `nyquist_compliant: true`. | ✓ VERIFIED | 23 rows (`04-01-T1`…`04-09-T2`), matching the 23 `<task>` elements; Wave 0 and sign-off boxes ticked; frontmatter `nyquist_compliant: true` (`status: draft`, as the lifecycle intends). |
| 53 | [backstop] In a client all 21 in-scope creatures can be encountered in the Kelp Curtain in their designed bands and none appears in a normal world. | ⚠️ PRESENT_BEHAVIOR_UNVERIFIED | Declared `verification: backstop`; the D-21 end-of-phase bundle, recorded not-executed in `04-UAT.md`. Routed to human verification. |

**Score:** 31/53 truths verified (22 present, behavior-unverified — the design-owner-deferred D-21 runtime tranche).

### Deferred Items

Items the phase explicitly scopes out and records as blockers for later phases (not gaps).

| # | Item | Addressed In | Evidence |
| --- | --- | --- | --- |
| 1 | Absent drop materials (毒腺, 牛黄, 软体甲壳碎片, 亡碧膏, 枯木碎块, 干涸心脏, the turtle's and large octopus's TBD lists) | Item phases | `drop:` blockers in `03-BIOLOGY.json`; §6.2/§14.2 absent-drop register (D-58) |
| 2 | Spiny Moss Court morale/command, Valley egg-laying, Valley disguised-hazard visuals | Later phase | `system:` blockers on five rows; §14.2 (D-46) |
| 3 | 森雨幽谷 / 刺苔庭园 regional spawn predicates | Phases 5–6 (terrain) | `region:` blockers on three rows; §5/§14.5 (D-52/D-53) |
| 4 | 26 `ModNPC` sprites + 11 projectile sprites | Artwork delivery | §3/§4 three-step D-49 migration |
| 5 | en-US / zh-Hans localization and per-item Feishu synchronization | Phase 8 | D-20; ROADMAP Phase 8 SC2/SC5; rows stay `unchecked` |

### Required Artifacts

| Artifact | Expected | Status | Details |
| --- | --- | --- | --- |
| 26 `ModNPC` classes under `NPCs/{DeathJadeLake,SpinyMossCourt,ValleyOfLushAndMoist}/` | Full implementations + 3 D-45 shells | ✓ VERIFIED | 97–892 lines each; all registered; all `White_Mod`; gate reports `guarded classes = 37` (26 + 11). |
| 11 hostile projectiles under `Projectiles/Enemies/` | The frozen projectile set | ✓ VERIFIED | All 11 exist, `White_Mod`, `hostile = true`, dust behind `!Main.dedServ`. |
| `NPCs/KelpCurtainSpawnConditions.cs` | The shared server-safe predicate seam | ✓ VERIFIED | `IsDryLand`/`IsWaterSurface`/`IsWaterBottom` + the D-54 weight bands. |
| `.planning/.../04-01...04-09-PLAN.md` | 9 plans | ✓ VERIFIED | All 9 present with frontmatter must_haves; 23 tasks total. |
| `.planning/.../04-01...04-09-SUMMARY.md` | 9 summaries | ✓ VERIFIED | All 9 `status: complete`, `requirements-advanced` BIO-01/02/03. |
| `.planning/phases/03-.../03-BIOLOGY.json` + `.md` | 31-row matrix, 21 in-scope rows reconciled | ✓ VERIFIED | 31 rows; 21 `code_complete: true`; counts frozen; Markdown parity green. |
| `.planning/.../04-DEVIATIONS.md` | Consolidated ledger | ✓ VERIFIED | 14 sections; §14.1 83-row blocker table; §14.2–§14.7. |
| `.planning/.../04-UAT.md` | D-21 client bundle | ✓ VERIFIED | 24 not-executed checks. |
| `.planning/.../04-VALIDATION.md` | Completed validation contract | ✓ VERIFIED | 23-row map; `nyquist_compliant: true`. |
| `.planning/.../scripts/check-biology.ps1` | ASCII phase gate | ✓ VERIFIED | 393 lines; 100% ASCII; runs green. |
| `.planning/.../04-REVIEW.md` + `04-REVIEW-FIX.md` | Review + fix record | ✓ VERIFIED | 2 CR + 3 WR + 5 IN; all 2 CR + 3 WR fixed (commits `b7d134fe0`, `dd2572f48`, `0c79bd497`, `86f2ff1f9`, `043ce97c5`). |

### Key Link Verification

| From | To | Via | Status | Details |
| --- | --- | --- | --- | --- |
| Every `SpawnChance` | Main-world isolation | `SubworldSystem.IsActive<YggdrasilWorld>()` + `KelpCurtainBiome.IsKelpCurtainLayer(spawnInfo.Player)` | ✓ WIRED | Gate invariant 7/8 asserts both tokens in every guarded class; returns `0f` outside the subworld (BIO-06). |
| Every NPC | `NPCSpawnManager.RegisterNPC` | `SetStaticDefaults` | ✓ WIRED | 26/26 classes register (token scan + gate). |
| `JadeSpiritAnglerfish.ModifyNPCLoot` | `MeatLantern`, `Photophore` | `ItemDropRule.Common(..., 4/8, 1, 1)` | ✓ WIRED | Both items exist on disk (`Items/Weapons`, `Items/Pets`). |
| `Radiolarian.ModifyNPCLoot` | `RadialCarapace` | `ItemDropRule.Common(..., 15, 1, 1)` | ✓ WIRED | Item exists on disk. |
| `AnimatedWitherbarkHound.ModifyNPCLoot` | `ActivatedDogStaff` | `ItemDropRule.Common(..., 11, 1, 1)` | ✓ WIRED | Item exists on disk. |
| `RedNeedleCaterpillar.ModifyNPCLoot` | `CaterpillarJuice` | `ItemDropRule.Common(..., 1, 1, 2)` | ✓ WIRED | Pre-existing `YggdrasilTown` item; template/item untouched (OQ1). |
| `ToxicToad` death / `BombJellyfish` death | `ToxicToad_PoisonCloud` / `BombJellyfish_Explosion` | `Projectile.NewProjectile` under `Main.netMode != NetmodeID.MultiplayerClient` | ✓ WIRED | Both projectiles exist, hostile, `White_Mod`. |
| `LargeMossyThornTurtle` state 2/3 | `_Shockwave` / `_Boulder` | netmode-guarded spawns | ✓ WIRED | Both projectiles exist. |
| Five CR-02 flags | Client AI branch | synced `NPC.ai[]` slots | ✓ WIRED | `ProvokedFlag`→`ai[1]` (3 classes), `AggroTimer`→`ai[1]`/`ChargeCooldown`→`ai[2]`, `Heading`→`ai[2]`; `localAI[0]` timers stay authoritative-only by design. |
| `03-BIOLOGY.json` | `03-BIOLOGY.md` + gate | id-set/per-row-status parity (invariant 11) | ✓ WIRED | Gate green. |

### Data-Flow Trace (Level 4)

| Artifact | Data Variable | Source | Produces Real Data | Status |
| --- | --- | --- | --- | --- |
| The five loot tables (`ModifyNPCLoot`) | drop rules | `ModContent.ItemType<...>` of real items on disk | Yes — gate invariant 8 resolves every item-type token under `Sources/Modules/Yggdrasil` | ✓ FLOWING |
| Every `SpawnChance` | spawn weight | `NPCSpawnInfo` + `SubworldSystem`/`KelpCurtainBiome` + tile/liquid state | Yes — real spawn-info/tile reads, no client camera | ✓ FLOWING |
| Every class `Texture` override | sprite | `Commons.ModAsset.White_Mod` | Yes — the existing shared fallback resolves (Release build + mod pack succeed) | ✓ FLOWING |
| `04-DEVIATIONS.md` §14.1 | blocker text | `03-BIOLOGY.json` `blockers` arrays | Yes — 83/83 cells byte-identical (recorded in `04-09-SUMMARY`) | ✓ FLOWING |

### Behavioral Spot-Checks

| Behavior | Command | Result | Status |
| --- | --- | --- | --- |
| Phase 4 biology gate (all 13 invariants, require-all) | `check-biology.ps1 -RequireAll` | exit 0 — `21 rows`, `reconciled 21/21`, `guarded classes 37`, BOM ok | ✓ PASS |
| Phase 3 gate (regression) | `03-.../check-biology.ps1 -RequireAll` | exit 0 — `phase3 tranche 5/5`, `implemented classes 5/5` | ✓ PASS |
| Phase 2 gate (regression) | `02-.../check-phase2.ps1` | exit 0 — `phase2 implemented 21/21 (full=9, shell=12)` | ✓ PASS |
| Release build | `dotnet build /p:Configuration=Release /p:WarningLevel=0` | exit 0 — 0 warnings / 0 errors, `Everglow.tmod` packaged and enabled | ✓ PASS |
| CR-01 debuff split | grep `NextBool` in `ToxicToad.cs` / `ToxicToad_PoisonBubble.cs` | both use `Main.rand.NextBool(3, 4)` (75 %); doc comments corrected | ✓ PASS |
| CR-02 synced state | grep `localAI`/`NPC.ai[` across the five classes | `ProvokedFlag`/`AggroTimer`/`ChargeCooldown`/`Heading` on `ai[]`; only authoritative-only timers remain in `localAI[0]` | ✓ PASS |
| WR-01/02/03 fixes | grep in the three classes | `ApplyStateMotion` re-derives `bool flying = State == AerialSlam`; teleport bottom-aligns on `(tileY + 1) * 16f - NPC.height / 2f`; volley origin is `NPC.Center` | ✓ PASS |
| Runtime spawn/AI/drops/dedicated-server/localization | — | no live client session (D-21) | ? SKIP → human verification |

### Probe Execution

No `probe-*.sh` probes exist for this phase; the phase's runnable check is the PowerShell gate `scripts/check-biology.ps1`, exercised above. **N/A.**

### Requirements Coverage

| Requirement | Source Plan | Description | Status | Evidence |
| --- | --- | --- | --- | --- |
| BIO-01 | 04-01…04-04, 04-09 | Death Jade Lake creatures | ✓ SATISFIED (code side) | 13 of 13 rows code-complete across 14 classes + 5 projectiles; drops wire to real items; requirement stays `Pending` (Phase 8 verification). |
| BIO-02 | 04-01, 04-06, 04-09 | Spiny Moss Court creatures | ✓ SATISFIED (code side) | 3 of 3 rows code-complete across 7 classes + 2 projectiles; morale system recorded as a D-46 blocker; stays `Pending`. |
| BIO-03 | 04-01, 04-07, 04-08, 04-09 | Valley of Lush and Moist creatures | ✓ SATISFIED (code side) | 5 of 5 rows code-complete across 5 classes + 4 projectiles; stays `Pending`. |
| BIO-06 (structural half) | 04-02…04-09 | No main-world leakage | ✓ SATISFIED (structural half) | Subworld + layer gate on every `SpawnChance`; runtime isolation is a human item. Stays `Pending` (Phase 8). |
| QUAL-03 (structural half) | 04-02…04-09 | Multiplayer/dedicated-server safety | ✓ SATISFIED (structural half) | `!Main.dedServ` guards; netmode-guarded authoritative spawns/transitions; runtime verification is a human item. Stays `Pending`. |
| QUAL-04 (record half) | 04-09 | Per-creature comparison record | ✓ SATISFIED | 21-row matrix + 83-blocker ledger + UAT bundle. Feishu synchronization stays in Phase 8. |

**Orphaned requirements:** None. `REQUIREMENTS.md` maps BIO-01/BIO-02/BIO-03 to Phase 4 and all three appear in the plans' `requirements` frontmatter; no other v1 requirement is mapped to Phase 4.

### Decision Coverage

All 16 trackable `04-CONTEXT.md` decisions are honored by shipped artifacts (`check.decision-coverage-verify`: `{total: 16, honored: 16, not_honored: []}`).

### Anti-Patterns Found

| File | Line | Pattern | Severity | Impact |
| --- | --- | --- | --- | --- |
| `LargeMossyThornTurtle.cs` | 806 | `TBD` inside a doc comment quoting the design's 死亡掉落物暂定 (TBD) cell | ℹ️ Info | Not an unresolved implementation debt marker — the same line names the formal follow-up (`D-57/D-58`, `03-BIOLOGY.json`, `04-DEVIATIONS.md` §6.2) and the drop gap is a recorded blocker. No fix required. |
| 11 projectiles | comments | the word "placeholder" | ℹ️ Info | Documents the deliberate `White_Mod` fallback in place of forbidden placeholder art (D-48/D-51); not a debt marker. |
| `LargeMossyThornTurtle.cs` / `SerpentMoss.cs` | 636 / 285, 314 | `return null;` | ℹ️ Info | Legitimate "no valid target / no bound player" sentinels, not stubs. |
| 9 classes + 2 jellyfish | — | unused `using` directives (IN-01) | ℹ️ Info | Left open by the review's Critical-and-Warning scope; no functional impact. |
| `WaterStrider.cs` | 98–102, 333 | `DashAngle` written but never read (IN-02) | ℹ️ Info | Dead state; no functional impact. |
| `AlgaeOctopus.cs` | 474 | redundant `NPC.netUpdate` on a projectile-only spawn (IN-03) | ℹ️ Info | Minor bandwidth; no functional impact. |
| 3 D-45 shells | `SpawnChance` | water weight returned without a water test (IN-04) | ℹ️ Info | Documented as deliberate; the shells can spawn on dry land inside the layer. No phase-goal impact. |
| `GlowSalamander.cs` | 492 | suffocation cadence rides `Main.GameUpdateCount` (IN-05) | ℹ️ Info | Global-tick cadence rather than per-NPC; no phase-goal impact. |
| (all 38 phase files) | — | `FIXME`/`XXX`/`TODO`/`HACK`/`console.log` debt markers | — | None found outside the one design-quote `TBD` above. |

### Human Verification Required

The phase carries 22 present-but-behavior-unverified truths and the D-21 client bundle. The full executable list is `04-UAT.md` (24 checks, all `not-executed`). In short:

1. **Per-creature spawn, behaviour and drops (checks 1–21)** — each of the 21 rows appears in its designed Kelp Curtain band, runs its documented water/land behaviour, hostility and status effects, and yields its wired drop (and only its wired drop).
2. **Isolation (check 22, BIO-06)** — none of the 21 appears in an ordinary world.
3. **Dedicated server / multiplayer (check 23, QUAL-03)** — no client-only crash on a dedicated server; no duplicated authoritative spawns; and, specifically, the CR-02 synced-`ai[]` fix means the client AI branch matches the server, while WR-01/WR-03 are observable only on both sides.
4. **Localization fallback (check 24, D-20)** — clean fallback display names with no missing-resource/disabled-mod log entry; no HJSON key was fabricated.
5. **Runtime edges** — 大型荆棘苔龟 losing its target mid-flight; a melee reflect landing while its shell is closed; the turtle's 214-px-scale spawn-area fit (carried from Phase 3).

### Gaps Summary

**No gaps.** All 31 programmatically-verifiable must-have truths pass: the 21 in-scope rows are real, loadable, wired, non-stub `ModNPC` implementations with gated subworld spawns, real hostile projectiles and drops that resolve to Phases 1–2 items; the 31-row matrix, its Markdown mirror, the consolidated ledger and the validation contract are machine-consistent with their gate; the offline chain (Release build, Phase 4 + Phase 3 + Phase 2 gates) is green under independent re-run; and no item, asset or localization scope was promoted into the phase. The 22 remaining truths are the design-owner's D-21 runtime tranche — observable only in a live tModLoader client — recorded in `04-UAT.md` and routed to human verification rather than counted as gaps.

Two carry-forward observations, neither a phase-goal gap: (a) `04-VALIDATION.md` keeps `status: draft` by design — the `/gsd-validate-phase` §6 step owns the promotion to `validated`; (b) the AGENTS.md byte-level BOM block is advisory and non-isolating on this long-lived branch (its `merge-base origin/master` anchor predates the phase), with the binding phase-scoped equivalent being gate invariant 13, which runs green.

---

_Verified: 2026-09-16T11:30:41Z_
_Verifier: the agent (gsd-verifier)_
