---
phase: "04"
slug: "remaining-ordinary-monsters"
# status lifecycle: draft (seeded by plan-phase) → validated (set by validate-phase §6)
status: draft
nyquist_compliant: true
wave_0_complete: true
created: "2026-09-15"
---

# Phase 04 — Validation Strategy

> Per-phase validation contract for feedback sampling during execution. Seeded from `04-RESEARCH.md` § Validation Architecture.

---

## Test Infrastructure

| Property | Value |
|----------|-------|
| **Framework** | **none for `ModNPC` content** — MSTest 3.10.2 (`Sources/Everglow.UnitTests`) cannot construct `Main`, load mod content, or run the game loop; verification is offline PowerShell gates + `dotnet build` + a client UAT batch (mirrors `02/03-VALIDATION.md`). |
| **Config file** | none — tests are discovered by the SDK |
| **Quick run command** | `dotnet build /p:Configuration=Release /p:WarningLevel=0` |
| **Full suite command** | `dotnet build /p:Configuration=Release /p:WarningLevel=0` → the Phase 4 gate (reads `03-BIOLOGY.json`) → `03-.../scripts/check-biology.ps1 -RequireAll` → Phase 1 gates (`check-inventory-reconciliation`, `check-carryover`, `check-tranche-A`, `check-tranche-B`, `check-armofgianttree-charge`) → `check-phase2.ps1` → `dotnet test --filter "FullyQualifiedName~Yggdrasil"` → the AGENTS.md byte-level BOM block |
| **Estimated runtime** | ~60–150 s (build dominates) |

---

## Phase Requirements → Test Map

| Req ID | Behavior | Test Type | Automated Command | File Exists? |
|--------|----------|-----------|-------------------|--------------|
| BIO-01/02/03 | In-scope set is exactly the frozen 21 rows (`[phase]==4 && !deferred`) | structural (JSON) | Phase 4 gate set-compare | ❌ Wave 0 |
| BIO-01/02/03 | Every in-scope row has a loadable `ModNPC` class on disk | structural | gate: `internal_name` → `<Class>.cs` via `Get-ChildItem -Recurse` | ❌ Wave 0 |
| BIO-01/02/03 | No Phase 4 class references a non-existent texture | static + build | gate: `White_Mod` override present or beside-`.png`; Release build proves resolution | ❌ Wave 0 |
| BIO-01/02/03 | Spawn is subworld-isolated | static + client | gate asserts `SubworldSystem.IsActive<YggdrasilWorld>` **and** `KelpCurtainBiome.IsKelpCurtainLayer` in every `SpawnChance` | ❌ Wave 0 |
| BIO-01/02/03 | Loot tables reference no unimplemented item type | static + build | gate resolves every `ModContent.ItemType<X>()` under the whole `Sources/Modules/Yggdrasil` tree; `dotnet build` proves resolution | ❌ Wave 0 |
| BIO-06 (advanced) | No main-world behavior introduced | static + dedicated-server smoke | gate regex + normal-world idle run + dedicated-server launch | ❌ Wave 0 |
| QUAL-01 (advanced) | Build clean | build | `dotnet build /p:Configuration=Release /p:WarningLevel=0` | ✅ |
| QUAL-03 (advanced) | Multiplayer/dedicated-server safety | static + manual | gate asserts `spawnInfo.Player` (not `Main.LocalPlayer`) and `!Main.dedServ`; manual dedicated-server launch | ❌ Wave 0 |
| QUAL-04 (advanced) | Per-creature comparison record | structural | Phase 4 gate invariants 1–4, 10 | ❌ Wave 0 |

---

## Per-Task Verification Map

Populated from the finalized `04-*-PLAN.md` files after planning (one row per `<task>` element; every task carries an `<automated>` verify bound to a `<fails_when>`). The requirement→test map above is the stable contract; this table tracks task-level coverage during execution.

**Filled by:** plan `04-09` Task 2 (the phase close-out), because the row set only becomes final once every wave plan exists on disk. The rows are generated mechanically from the plan files — a script that reads them and counts `<task>` elements is preferred — so the map cannot drift from the plans it describes.

| Task id | Plan | Wave | Requirement | Automated command | Fails when | Result |
| --- | --- | --- | --- | --- | --- | --- |
| `04-01-T1` | `04-01` | 1 | [BIO-01, BIO-02, BIO-03] | dotnet build /p:Configuration=Release /p:WarningLevel=0; if ($?) { powershell -NoProfile -ExecutionPolicy Bypass -File ".planning/phases/04-remaining-ordinary-monsters/scripts/check-biology.ps1" }; if ($?) { powershell -NoProfile -ExecutionPolicy Bypass -File ".planning/phases/03-completed-art-ordinary-monsters/scripts/check-biology.ps1" -RequireAll } | non-zero exit – an `error CS` in the Release build; the Phase 4 gate printing `FAIL(`, exiting 2, reporting `UTF-8 BOM:`, or not printing all four success lines `OK(0): phase4 in-scope set = 21 rows (rows=31)`, `OK: reconciled rows = 1 / 21`, `OK: guarded classes = 1`, `OK: UTF-8 BOM check passed (`; the Phase 3 gate printing `FAIL(` or not printing `OK(0): phase3 tranche = 5 / 5 (rows=31)` and `OK: implemented classes = 5 / 5`; either gate script containing a byte above 0x7F; or `git diff --stat` (uncommitted-tree check, run before this task's commit) reporting any change to `.planning/phases/03-completed-art-ordinary-monsters/scripts/check-biology.ps1`. | Pass - plan-time verify green when the task committed; re-run green by the close-out chain: Phase 4 gate (chain link 2); Phase 3 gate (chain link 3); Release build (chain link 1). |
| `04-01-T2` | `04-01` | 1 | [BIO-01, BIO-02, BIO-03] | powershell -NoProfile -ExecutionPolicy Bypass -File ".planning/phases/04-remaining-ordinary-monsters/scripts/check-biology.ps1"; if ($?) { $p = ".planning/phases/04-remaining-ordinary-monsters/04-DEVIATIONS.md"; if (-not (Test-Path -LiteralPath $p -PathType Leaf)) { throw "04-DEVIATIONS.md missing" }; $t = [IO.File]::ReadAllText($p); foreach ($token in @('CaterpillarJuice','BuffID.Venom','AnimatedWitherbarkSoldierRanged','LargeBombJellyfish','SmallBrodieFlydragon','check-biology.ps1','DeathJadeLake','D-44','D-47','D-52','D-58','OQ1','OQ2','OQ3','OQ4','OQ5','V2-HARD-01')) { if (-not $t.Contains($token)) { throw "04-DEVIATIONS.md missing token: $token" } }; $b = [IO.File]::ReadAllBytes($p); if ($b.Length -ge 3 -and $b[0] -eq 0xEF -and $b[1] -eq 0xBB -and $b[2] -eq 0xBF) { throw "UTF-8 BOM in 04-DEVIATIONS.md" }; "OK: 04-DEVIATIONS.md present, token-complete, BOM-free" } | non-zero exit or a thrown message – the Phase 4 gate printing `FAIL(`; `04-DEVIATIONS.md` missing; any required token absent from its text (`CaterpillarJuice`, `BuffID.Venom`, `AnimatedWitherbarkSoldierRanged`, `LargeBombJellyfish`, `SmallBrodieFlydragon`, `check-biology.ps1`, `DeathJadeLake`, `D-44`, `D-47`, `D-52`, `D-58`, `OQ1`–`OQ5`, `V2-HARD-01`); or a `UTF-8 BOM in 04-DEVIATIONS.md` throw. | Pass - plan-time verify green when the task committed; re-run green by the close-out chain: Phase 4 gate (chain link 2). |
| `04-02-T1` | `04-02` | 2 | [BIO-01] | dotnet build /p:Configuration=Release /p:WarningLevel=0; if ($?) { powershell -NoProfile -ExecutionPolicy Bypass -File ".planning/phases/04-remaining-ordinary-monsters/scripts/check-biology.ps1" } | non-zero exit — an `error CS` (including a `ModContent.ItemType<X>()` whose `X` has no class), the Phase 4 gate printing `FAIL(`, exiting 2, reporting `UTF-8 BOM:`, not printing `OK(0): phase4 in-scope set = 21 rows (rows=31)`, or failing to show an `OK: guarded classes =` count one higher than the previous task's run. | Pass - plan-time verify green when the task committed; re-run green by the close-out chain: Phase 4 gate (chain link 2); Release build (chain link 1). |
| `04-02-T2` | `04-02` | 2 | [BIO-01] | dotnet build /p:Configuration=Release /p:WarningLevel=0; if ($?) { powershell -NoProfile -ExecutionPolicy Bypass -File ".planning/phases/04-remaining-ordinary-monsters/scripts/check-biology.ps1" } | non-zero exit — an `error CS` (in particular a `ModContent.ItemType<X>()` for an absent `X`, a projectile-type reference that does not resolve, or a `SendExtraAI`/`ReceiveExtraAI` override signature that does not match the tML hook), the Phase 4 gate printing `FAIL(` / exiting 2 / reporting `UTF-8 BOM:`, or the gate `OK: guarded classes =` count failing to rise by four against the previous task run (this task adds four guarded files: `NPCs/DeathJadeLake/ToxicToad.cs` and `NPCs/DeathJadeLake/GlowSalamander.cs` plus the two new phase projectiles `ToxicToad_PoisonBubble.cs` and `ToxicToad_PoisonCloud.cs` under `$enemiesRel`). | Pass - plan-time verify green when the task committed; re-run green by the close-out chain: Phase 4 gate (chain link 2); Release build (chain link 1). |
| `04-03-T1` | `04-03` | 3 | [BIO-01] | dotnet build /p:Configuration=Release /p:WarningLevel=0; if ($?) { powershell -NoProfile -ExecutionPolicy Bypass -File ".planning/phases/04-remaining-ordinary-monsters/scripts/check-biology.ps1" } | non-zero exit — an `error CS` (especially a `ModContent.ItemType<X>()` for an absent material, or a missing `Terraria.DataStructures` import for the `IEntitySource` parameter), the Phase 4 gate printing `FAIL(` / exiting 2 / reporting `UTF-8 BOM:`, or the gate's `OK: guarded classes =` count failing to rise by one against the previous task's run. | Pass - plan-time verify green when the task committed; re-run green by the close-out chain: Phase 4 gate (chain link 2); Release build (chain link 1). |
| `04-03-T2` | `04-03` | 3 | [BIO-01] | dotnet build /p:Configuration=Release /p:WarningLevel=0; if ($?) { powershell -NoProfile -ExecutionPolicy Bypass -File ".planning/phases/04-remaining-ordinary-monsters/scripts/check-biology.ps1" } | non-zero exit — an `error CS` (in particular an unresolvable projectile-type reference from `HitEffect`, or a `ModContent.ItemType<X>()` for an absent `X`), the Phase 4 gate printing `FAIL(` / exiting 2 / reporting `UTF-8 BOM:`, or the gate's `OK: guarded classes =` count failing to rise by three against the previous task's run. | Pass - plan-time verify green when the task committed; re-run green by the close-out chain: Phase 4 gate (chain link 2); Release build (chain link 1). |
| `04-03-T3` | `04-03` | 3 | [BIO-01] | dotnet build /p:Configuration=Release /p:WarningLevel=0; if ($?) { powershell -NoProfile -ExecutionPolicy Bypass -File ".planning/phases/04-remaining-ordinary-monsters/scripts/check-biology.ps1" } | non-zero exit — an `error CS` (in particular an unresolvable `GlowSalamander` or `RiverSlug` type lookup), the Phase 4 gate printing `FAIL(` / exiting 2 / reporting `UTF-8 BOM:`, or the gate's `OK: guarded classes =` count failing to rise by one against the previous task's run. | Pass - plan-time verify green when the task committed; re-run green by the close-out chain: Phase 4 gate (chain link 2); Release build (chain link 1). |
| `04-04-T1` | `04-04` | 3 | [BIO-01] | dotnet build /p:Configuration=Release /p:WarningLevel=0; if ($?) { powershell -NoProfile -ExecutionPolicy Bypass -File ".planning/phases/04-remaining-ordinary-monsters/scripts/check-biology.ps1" } | non-zero exit — an `error CS` (in particular an unresolvable projectile-type reference, a `RadialCarapace` namespace that does not resolve, or a `ModContent.ItemType<X>()` for an absent `X` such as 软体甲壳碎片), the Phase 4 gate printing `FAIL(` / exiting 2 / reporting `UTF-8 BOM:`, or the gate's `OK: guarded classes =` count failing to rise by two against the previous task's run. | Pass - plan-time verify green when the task committed; re-run green by the close-out chain: Phase 4 gate (chain link 2); Release build (chain link 1). |
| `04-04-T2` | `04-04` | 3 | [BIO-01] | dotnet build /p:Configuration=Release /p:WarningLevel=0; if ($?) { powershell -NoProfile -ExecutionPolicy Bypass -File ".planning/phases/04-remaining-ordinary-monsters/scripts/check-biology.ps1" } | non-zero exit — an `error CS` (in particular an unresolvable `JadeSpiritAnglerfish` type lookup, a projectile-type reference that does not resolve, or a `ModContent.ItemType<X>()` for an absent `X`), the Phase 4 gate printing `FAIL(` / exiting 2 / reporting `UTF-8 BOM:`, or the gate's `OK: guarded classes =` count failing to rise by two against the previous task's run. | Pass - plan-time verify green when the task committed; re-run green by the close-out chain: Phase 4 gate (chain link 2); Release build (chain link 1). |
| `04-04-T3` | `04-04` | 3 | [BIO-01] | dotnet build /p:Configuration=Release /p:WarningLevel=0; if ($?) { powershell -NoProfile -ExecutionPolicy Bypass -File ".planning/phases/04-remaining-ordinary-monsters/scripts/check-biology.ps1" } | non-zero exit — an `error CS` (in particular an unresolvable `AlgaeOctopus_InkCloud` projectile reference, a missing `BuffID.Webbed` member, or a `ModContent.ItemType<X>()` for an absent `X`), the Phase 4 gate printing `FAIL(` / exiting 2 / reporting `UTF-8 BOM:`, or the gate's `OK: guarded classes =` count failing to rise by one against the previous task's run. | Pass - plan-time verify green when the task committed; re-run green by the close-out chain: Phase 4 gate (chain link 2); Release build (chain link 1). |
| `04-05-T1` | `04-05` | 2 | [BIO-01] | dotnet build /p:Configuration=Release /p:WarningLevel=0; if ($?) { powershell -NoProfile -ExecutionPolicy Bypass -File ".planning/phases/04-remaining-ordinary-monsters/scripts/check-biology.ps1" } | non-zero exit — an `error CS`, the Phase 4 gate printing `FAIL(` / exiting 2 / reporting `UTF-8 BOM:`, or the gate's `OK: guarded classes =` count failing to rise by one against the previous task's run. | Pass - plan-time verify green when the task committed; re-run green by the close-out chain: Phase 4 gate (chain link 2); Release build (chain link 1). |
| `04-05-T2` | `04-05` | 2 | [BIO-01] | dotnet build /p:Configuration=Release /p:WarningLevel=0; if ($?) { powershell -NoProfile -ExecutionPolicy Bypass -File ".planning/phases/04-remaining-ordinary-monsters/scripts/check-biology.ps1" } | non-zero exit — an `error CS`, the Phase 4 gate printing `FAIL(` / exiting 2 / reporting `UTF-8 BOM:`, or the gate's `OK: guarded classes =` count failing to rise by one against the previous task's run. | Pass - plan-time verify green when the task committed; re-run green by the close-out chain: Phase 4 gate (chain link 2); Release build (chain link 1). |
| `04-05-T3` | `04-05` | 2 | [BIO-01] | dotnet build /p:Configuration=Release /p:WarningLevel=0; if ($?) { powershell -NoProfile -ExecutionPolicy Bypass -File ".planning/phases/04-remaining-ordinary-monsters/scripts/check-biology.ps1" } | non-zero exit — an `error CS`, the Phase 4 gate printing `FAIL(` / exiting 2 / reporting `UTF-8 BOM:`, the gate's `OK: guarded classes =` count failing to rise by one against the previous task's run, or a new projectile file appearing under the enemy-projectile tree. | Pass - plan-time verify green when the task committed; re-run green by the close-out chain: Phase 4 gate (chain link 2); Release build (chain link 1). |
| `04-06-T1` | `04-06` | 3 | [BIO-02] | dotnet build /p:Configuration=Release /p:WarningLevel=0; if ($?) { powershell -NoProfile -ExecutionPolicy Bypass -File ".planning/phases/04-remaining-ordinary-monsters/scripts/check-biology.ps1" } | non-zero exit — an `error CS` (in particular an unresolvable `ActivatedDogStaff` namespace, a vanilla caster `NPCID` that does not exist for the clone, or a `ModContent.ItemType<X>()` for an absent `X` such as 枯木碎块), the Phase 4 gate printing `FAIL(` / exiting 2 / reporting `UTF-8 BOM:`, or the gate's `OK: guarded classes =` count failing to rise by six against the previous task's run. | Pass - plan-time verify green when the task committed; re-run green by the close-out chain: Phase 4 gate (chain link 2); Release build (chain link 1). |
| `04-06-T2` | `04-06` | 3 | [BIO-02] | dotnet build /p:Configuration=Release /p:WarningLevel=0; if ($?) { powershell -NoProfile -ExecutionPolicy Bypass -File ".planning/phases/04-remaining-ordinary-monsters/scripts/check-biology.ps1" } | non-zero exit — an `error CS` (in particular an unresolvable `ModContent.NPCType<…>()` for a soldier variant, or a `ModContent.ItemType<X>()` for an absent `X`), the Phase 4 gate printing `FAIL(` / exiting 2 / reporting `UTF-8 BOM:`, or the gate's `OK: guarded classes =` count failing to rise by one against the previous task's run. | Pass - plan-time verify green when the task committed; re-run green by the close-out chain: Phase 4 gate (chain link 2); Release build (chain link 1). |
| `04-06-T3` | `04-06` | 3 | [BIO-02] | dotnet build /p:Configuration=Release /p:WarningLevel=0; if ($?) { powershell -NoProfile -ExecutionPolicy Bypass -File ".planning/phases/04-remaining-ordinary-monsters/scripts/check-biology.ps1" } | non-zero exit — an `error CS`, the Phase 4 gate printing `FAIL(` / exiting 2 / reporting `UTF-8 BOM:`, the gate's `OK: guarded classes =` count failing to rise by two against the previous task's run, or a new egg or dust file appearing under the KelpCurtain tree. | Pass - plan-time verify green when the task committed; re-run green by the close-out chain: Phase 4 gate (chain link 2); Release build (chain link 1). |
| `04-07-T1` | `04-07` | 3 | [BIO-03] | dotnet build /p:Configuration=Release /p:WarningLevel=0; if ($?) { powershell -NoProfile -ExecutionPolicy Bypass -File ".planning/phases/04-remaining-ordinary-monsters/scripts/check-biology.ps1" } | non-zero exit – an `error CS` (in particular an unresolvable `CaterpillarJuice` namespace, an unresolvable `Everglow.Commons.Mechanics.Miscs.NoGameModeScale`, or a template override whose signature does not match), the Phase 4 gate printing `FAIL(` / exiting 2 / reporting `UTF-8 BOM:`, the gate's `OK: guarded classes =` count failing to rise by two against the previous task's run, or `git diff --stat` (the uncommitted-tree check, run before this task's commit) reporting any change to `Sources/Modules/Yggdrasil/YggdrasilTown/NPCs/BarkSpicyCaterpillar.cs`, `Sources/Everglow.Function/Templates/Enemies/Caterpillar.cs` or `Sources/Modules/Yggdrasil/YggdrasilTown/Items/Materials/CaterpillarJuice.cs`. | Pass - plan-time verify green when the task committed; re-run green by the close-out chain: Phase 4 gate (chain link 2); Release build (chain link 1). |
| `04-07-T2` | `04-07` | 3 | [BIO-03] | dotnet build /p:Configuration=Release /p:WarningLevel=0; if ($?) { powershell -NoProfile -ExecutionPolicy Bypass -File ".planning/phases/04-remaining-ordinary-monsters/scripts/check-biology.ps1" } | non-zero exit – an `error CS`, the Phase 4 gate printing `FAIL(` / exiting 2 / reporting `UTF-8 BOM:`, or the gate's `OK: guarded classes =` count failing to rise by two against the previous task's run. | Pass - plan-time verify green when the task committed; re-run green by the close-out chain: Phase 4 gate (chain link 2); Release build (chain link 1). |
| `04-07-T3` | `04-07` | 3 | [BIO-03] | dotnet build /p:Configuration=Release /p:WarningLevel=0; if ($?) { powershell -NoProfile -ExecutionPolicy Bypass -File ".planning/phases/04-remaining-ordinary-monsters/scripts/check-biology.ps1" } | non-zero exit – an `error CS` (in particular a missing `BuffID.Webbed` member), the Phase 4 gate printing `FAIL(` / exiting 2 / reporting `UTF-8 BOM:`, or the gate's `OK: guarded classes =` count failing to rise by one against the previous task's run. | Pass - plan-time verify green when the task committed; re-run green by the close-out chain: Phase 4 gate (chain link 2); Release build (chain link 1). |
| `04-08-T1` | `04-08` | 3 | [BIO-03] | dotnet build /p:Configuration=Release /p:WarningLevel=0; if ($?) { powershell -NoProfile -ExecutionPolicy Bypass -File ".planning/phases/04-remaining-ordinary-monsters/scripts/check-biology.ps1" } | non-zero exit — an `error CS`, the Phase 4 gate printing `FAIL(` / exiting 2 / reporting `UTF-8 BOM:`, or the gate's `OK: guarded classes =` count failing to rise by one against the previous task's run. | Pass - plan-time verify green when the task committed; re-run green by the close-out chain: Phase 4 gate (chain link 2); Release build (chain link 1). |
| `04-08-T2` | `04-08` | 3 | [BIO-03] | dotnet build /p:Configuration=Release /p:WarningLevel=0; if ($?) { powershell -NoProfile -ExecutionPolicy Bypass -File ".planning/phases/04-remaining-ordinary-monsters/scripts/check-biology.ps1" } | non-zero exit — an `error CS` (in particular an unresolvable projectile-type reference, a `NPCID.GiantTortoise` clone that does not exist, or a `ModContent.ItemType<X>()` for an absent `X`), the Phase 4 gate printing `FAIL(` / exiting 2 / reporting `UTF-8 BOM:`, or the gate's `OK: guarded classes =` count failing to rise by three against the previous task's run. | Pass - plan-time verify green when the task committed; re-run green by the close-out chain: Phase 4 gate (chain link 2); Release build (chain link 1). |
| `04-09-T1` | `04-09` | 4 | [BIO-01, BIO-02, BIO-03] | powershell -NoProfile -ExecutionPolicy Bypass -File ".planning/phases/04-remaining-ordinary-monsters/scripts/check-biology.ps1" -RequireAll; if ($?) { powershell -NoProfile -ExecutionPolicy Bypass -File ".planning/phases/03-completed-art-ordinary-monsters/scripts/check-biology.ps1" -RequireAll } | non-zero exit — the Phase 4 gate printing `FAIL(`, exiting 2, reporting `UTF-8 BOM:`, reporting a require-all failure for an in-scope row that is not `code_complete`, or not printing `OK(0): phase4 in-scope set = 21 rows (rows=31)` followed by `OK: reconciled rows = 21 / 21`; or the Phase 3 gate printing `FAIL(` or not printing `OK(0): phase3 tranche = 5 / 5 (rows=31)` and `OK: implemented classes = 5 / 5`. | Pass - plan-time verify green when the task committed; re-run green by the close-out chain: Phase 4 gate (chain link 2); Phase 3 gate (chain link 3). |
| `04-09-T2` | `04-09` | 4 | [BIO-01, BIO-02, BIO-03] | dotnet build /p:Configuration=Release /p:WarningLevel=0; if ($?) { powershell -NoProfile -ExecutionPolicy Bypass -File ".planning/phases/04-remaining-ordinary-monsters/scripts/check-biology.ps1" -RequireAll }; if ($?) { powershell -NoProfile -ExecutionPolicy Bypass -File ".planning/phases/03-completed-art-ordinary-monsters/scripts/check-biology.ps1" -RequireAll }; if ($?) { powershell -NoProfile -ExecutionPolicy Bypass -File ".planning/phases/01-item-inventory-completed-art-items/scripts/check-inventory-reconciliation.ps1" }; if ($?) { powershell -NoProfile -ExecutionPolicy Bypass -File ".planning/phases/01-item-inventory-completed-art-items/scripts/check-carryover.ps1" }; if ($?) { powershell -NoProfile -ExecutionPolicy Bypass -File ".planning/phases/01-item-inventory-completed-art-items/scripts/check-tranche-A.ps1" }; if ($?) { powershell -NoProfile -ExecutionPolicy Bypass -File ".planning/phases/01-item-inventory-completed-art-items/scripts/check-tranche-B.ps1" }; if ($?) { powershell -NoProfile -ExecutionPolicy Bypass -File ".planning/phases/01-item-inventory-completed-art-items/scripts/check-armofgianttree-charge.ps1" }; if ($?) { powershell -NoProfile -ExecutionPolicy Bypass -File ".planning/phases/02-remaining-items-unfinished-art-materials/scripts/check-phase2.ps1" }; if ($?) { dotnet test --filter "FullyQualifiedName~Yggdrasil" }; if ($?) { $p = ".planning/phases/04-remaining-ordinary-monsters/04-UAT.md"; if (-not (Test-Path -LiteralPath $p -PathType Leaf)) { throw "04-UAT.md missing" }; $t = [IO.File]::ReadAllText($p); foreach ($token in @('not executed','isolation','dedicated','localization','JadeSpiritAnglerfish','LargeMossyThornTurtle','RedNeedleCaterpillar','AnimatedWitherbarkSoldier','CourtCommander','AlgaeOctopus','ToxicToad','ArmoredShrimp','BombJellyfish','SailfinSnakehead','Radiolarian','WaterStrider','GlowSalamander','SmallGuppyConch','AssassinRaspberry','SerpentMoss','BrodieFlydragon','FluorescentHydra','GiantTigerShrimp','CannonBarnacle')) { if (-not $t.Contains($token)) { throw "04-UAT.md missing token: $token" } }; $b = [IO.File]::ReadAllBytes($p); if ($b.Length -ge 3 -and $b[0] -eq 0xEF -and $b[1] -eq 0xBB -and $b[2] -eq 0xBF) { throw "UTF-8 BOM in 04-UAT.md" }; "OK: 04-UAT.md present, token-complete, BOM-free" }; if ($?) { $vp = ".planning/phases/04-remaining-ordinary-monsters/04-VALIDATION.md"; if (-not (Test-Path -LiteralPath $vp -PathType Leaf)) { throw "04-VALIDATION.md missing" }; $vt = [IO.File]::ReadAllText($vp); foreach ($token in @('Per-Task Verification Map','04-01-T1','04-02-T2','04-09-T2','nyquist_compliant: true','Fails when','Result')) { if (-not $vt.Contains($token)) { throw "04-VALIDATION.md missing token: $token" } }; if ($vt -notmatch '(?m)^nyquist_compliant:\s*true\s*$') { throw "04-VALIDATION.md frontmatter nyquist_compliant is not true" }; $tag = [string][char]0x3C + 'task '; $planTasks = 0; foreach ($pf in Get-ChildItem ".planning/phases/04-remaining-ordinary-monsters/04-0*-PLAN.md") { $planTasks += ([regex]::Matches([IO.File]::ReadAllText($pf.FullName), [regex]::Escape($tag))).Count }; $rowCount = ([regex]::Matches($vt, '(?m)^\\|\s*`04-0\d-T\d`')).Count; if ($rowCount -ne $planTasks) { throw "04-VALIDATION.md rows $rowCount != plan tasks $planTasks" }; $vb = [IO.File]::ReadAllBytes($vp); if ($vb.Length -ge 3 -and $vb[0] -eq 0xEF -and $vb[1] -eq 0xBB -and $vb[2] -eq 0xBF) { throw "UTF-8 BOM in 04-VALIDATION.md" }; "OK: 04-VALIDATION.md present, $rowCount rows match $planTasks plan tasks, BOM-free" } | non-zero exit at any link in the chain — an `error CS` in the Release build, a failing `dotnet test --filter "FullyQualifiedName~Yggdrasil"`, any gate printing `FAIL(` / exiting 2 / not printing its success line (the Phase 4 gate's lines are `OK(0): phase4 in-scope set = 21 rows (rows=31)` and `OK: reconciled rows = 21 / 21`; the Phase 3 gate's are `OK(0): phase3 tranche = 5 / 5 (rows=31)` and `OK: implemented classes = 5 / 5`), a `04-UAT.md missing` / `04-UAT.md missing token: …` / `UTF-8 BOM in 04-UAT.md` throw, a `04-VALIDATION.md missing` / `04-VALIDATION.md missing token: …` / `04-VALIDATION.md frontmatter nyquist_compliant is not true` / `04-VALIDATION.md rows N != plan tasks M` / `UTF-8 BOM in 04-VALIDATION.md` throw, or the AGENTS.md BOM block reporting a BOM in a file outside Phase 4's change set, unless the exact failure is triaged as a stale out-of-phase baseline and recorded in `04-DEVIATIONS.md` §12 with the failing line quoted. | Pass - plan-time verify green when the task committed; re-run green by the close-out chain: Phase 4 gate (chain link 2); Phase 3 gate (chain link 3); Phase 1 inventory gate (chain link 4); Phase 1 carry-over gate (chain link 5); Phase 1 tranche-A gate (chain link 6); Phase 1 tranche-B gate (chain link 7); Phase 1 ArmOfGiantTree gate (chain link 8); Phase 2 gate (chain link 9); Yggdrasil MSTest filter (chain link 10); the 04-UAT.md token contract (its own verify); the 04-VALIDATION.md contract (its own verify); Release build (chain link 1). |

**Close-out evidence legend** (the run this table records; plan `04-09` Task 2, one chain):

| # | Link | Result |
| --- | --- | --- |
| 1 | `dotnet build /p:Configuration=Release /p:WarningLevel=0` | exit 0 - build succeeded, 0 warnings, 0 errors, `Everglow.tmod` deployed and the mod enabled |
| 2 | Phase 4 gate `scripts/check-biology.ps1 -RequireAll` | exit 0 - `OK(0): phase4 in-scope set = 21 rows (rows=31)`, `OK: reconciled rows = 21 / 21`, `OK: guarded classes = 37`, UTF-8 BOM check passed |
| 3 | Phase 3 gate `../03-.../scripts/check-biology.ps1 -RequireAll` | exit 0 - `OK(0): phase3 tranche = 5 / 5 (rows=31)`, `OK: implemented classes = 5 / 5` (the Phase 3 chain still reproduces; that script is unmodified) |
| 4 | Phase 1 `check-inventory-reconciliation.ps1` | exit 0 - `OK(0): 103 entries; matched=86; green=50 yellow=8 unchecked=45; labels=5 deferred=3 assumptions=7` |
| 5 | Phase 1 `check-carryover.ps1` | exit 0 - `OK(0): carry-over covered entries = 5 (of 5 selected)` |
| 6 | Phase 1 `check-tranche-A.ps1` | exit 0 - `OK(0): tranche-A covered entries = 43 (of 43 selected)` |
| 7 | Phase 1 `check-tranche-B.ps1` | exit 0 - `OK(0): tranche-B covered entries = 20 (of 20 selected)` |
| 8 | Phase 1 `check-armofgianttree-charge.ps1` | exit 0 - `OK(0): ArmOfGiantTree charge is per-player + per-stack slot-keyed, synced, and server-authoritative` |
| 9 | Phase 2 `check-phase2.ps1` | exit 0 - `OK(0): phase2 implemented = 21 / 21 (full=9, shell=12)` |
| 10 | `dotnet test --filter "FullyQualifiedName~Yggdrasil"` | exit 0 - 3 passed, 0 failed, 0 skipped, 184 ms |
| 11 | AGENTS.md byte-level UTF-8 BOM check | anchor `git merge-base HEAD origin/master` = `f222f9316508c71658a331a1c309776fd862fa2d`; `UTF-8 BOM check passed (1440 files).` No BOM-prefixed file. Advisory and non-isolating (it measures the whole branch against a distant pre-phase ancestor); the binding, phase-scoped equivalent is invariant 13 inside the Phase 4 gate, which ran first over the phase own change set. |

Every link exited 0, so no exception had to be triaged. The rows above are generated mechanically from the nine `04-*-PLAN.md` files - a script counts their `<task>` elements and quotes each task `<automated>` and `<fails_when>` verbatim (only newlines, tabs and runs of spaces are collapsed to single spaces for the table, and a literal `|` inside a cell is escaped `\|`) - so the map cannot drift from the plans it describes. Counted task elements: **23** across 9 plans.

One row per `<task>` element across the nine plans, in plan-then-task order: **23** rows at the current plan revision (04-01 ×2, 04-02 ×2, 04-03 ×3, 04-04 ×3, 04-05 ×3, 04-06 ×3, 04-07 ×3, 04-08 ×2, 04-09 ×2). The plan set is the authority: if the counted row total and the counted `<task>` total disagree, the rows are regenerated from the plans.

---

## Sampling Rate

- **Per task commit:** `dotnet build /p:Configuration=Release /p:WarningLevel=0` for any C# change; the Phase 4 gate for matrix/gate changes
- **Per wave merge:** the Phase 4 gate + `03-.../scripts/check-biology.ps1` + `dotnet build` + the Phase 1/2 regression gates
- **Before `/gsd-verify-work`:** full suite green, plus the D-21 client/`!Main.dedServ` UAT bundle (`04-UAT.md`)
- **Max feedback latency:** ~150 s

---

## Wave 0 Requirements

- [x] `.planning/phases/04-remaining-ordinary-monsters/scripts/check-biology.ps1` — the Phase 4 gate (reads the shared `03-BIOLOGY.json`), authored before the classes so it runs red→green; leaves the Phase 3 script byte-identical
- [x] `.planning/phases/04-remaining-ordinary-monsters/04-DEVIATIONS.md` — D-47 per-creature dispositions, conservative spawn weights (D-54), drop denominators (D-59), blocker register
- [x] `.planning/phases/04-remaining-ordinary-monsters/04-UAT.md` — the D-21 client bundle (recorded, not executed)
- [x] 21 `ModNPC` class files (region subfolders recommended) + any self-contained attack projectiles
- [x] The reconciled 21 rows in `03-BIOLOGY.json` + `03-BIOLOGY.md`
- [x] Framework install: **none** — existing infrastructure (MSTest + PowerShell + build) covers the phase

---

## Manual-Only Verifications

| Behavior | Requirement | Why Manual | Test Instructions |
|----------|-------------|------------|-------------------|
| Mod loads with no missing-resource error; each new creature appears | BIO-01/02/03 | Runtime loader property; no offline script observes it (D-21) | Launch a client, enter the Kelp Curtain context, confirm clean load and creature presence per region |
| Subworld isolation — no creature spawns in the main world | BIO-06 | Runtime spawn behavior | Normal-world idle run + dedicated-server launch; confirm zero main-world spawns |
| Per-creature water/land behavior, hostility, status effects, drops | BIO-01/02/03 | Runtime gameplay property | Walk each documented context; verify 碧灵鮟鱇's two drops, 放射虫's shell, 枯木活化士兵 犬's staff |
| Dedicated-server safety (no graphics crash) | QUAL-03 | Runtime | Start a dedicated server; confirm no crash and no client-only work |

---

## Validation Sign-Off

**Completed by:** plan `04-09` Task 2, from its own close-out run — the boxes below are ticked against what that run observed, and `nyquist_compliant` flips to `true` there. The frontmatter `status` stays `draft` in this file's lifecycle until the `/gsd-validate-phase` §6 step promotes it to `validated`.

- [x] All tasks have `<automated>` verify or Wave 0 dependencies
- [x] Sampling continuity: no 3 consecutive tasks without automated verify
- [x] Wave 0 covers all MISSING references
- [x] No watch-mode flags
- [x] Feedback latency < 150s
- [x] `nyquist_compliant: true` set in frontmatter

**Approval:** 2026-09-16 - plan `04-09` Task 2. The complete offline chain is green in one run (11 links, every one exit 0) and the D-21 client bundle is recorded as not executed in `04-UAT.md`. Nyquist compliance is asserted for the offline half only: the runtime half of BIO-01/BIO-02/BIO-03 has no offline observer by construction (D-21), and the frontmatter `status` stays `draft` until the `/gsd-validate-phase` section 6 step promotes it.
