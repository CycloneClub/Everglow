---
phase: 01-item-inventory-completed-art-items
plan: 05
type: execute
wave: 5
depends_on: [01-03, 01-04]
files_modified:
  - .planning/phases/01-item-inventory-completed-art-items/01-INVENTORY.json
  - .planning/phases/01-item-inventory-completed-art-items/01-INVENTORY.md
  - .planning/phases/01-item-inventory-completed-art-items/01-DEVIATIONS.md
  - .planning/phases/01-item-inventory-completed-art-items/scripts/check-localization-coverage.ps1
  - Sources/Modules/Yggdrasil/KelpCurtain/Items/Tools/Developer/GenerateMazeRoom.cs
  - Sources/Modules/Yggdrasil/KelpCurtain/Items/Tools/Developer/ResetIsleOfBloom.cs
  - Sources/Modules/Yggdrasil/KelpCurtain/Items/Tools/Developer/ResetKelpCurtain.cs
  - Sources/Modules/Yggdrasil/KelpCurtain/Items/Tools/Developer/UnderWaterDungeon.cs
  - Sources/Everglow/Localization/en-US/Mods.Everglow.Items.*.hjson
  - Sources/Everglow/Localization/zh-Hans/Mods.Everglow.Items.*.hjson
autonomous: false
requirements: [QUAL-05]
estimate:
  tokens: 40000
  raw_tokens: 40000
  tasks: 3
  confidence: low

must_haves:
  truths:
    - "Every completed-art entry implemented in plans 03/04 with a non-empty `internal_name` and `localization.blocked != true` has a display key in both `Sources/Everglow/Localization/en-US/` and `Sources/Everglow/Localization/zh-Hans/`."
    - "The four `Tools/Developer` classes declare `LocalizationCategory` `Tools` before the exporter runs; no class in the tranche folders defaults to `Items`."
    - "A completed-art item still missing a key after the exporter is recorded with `localization.blocked: true` and a blocker, and `check-localization-coverage.ps1` excludes such entries instead of failing on them, so the gate's blocked-entry fallback is satisfiable."
    - "The 43 class-less entries routed to Phase 2 in plan 03 remain `phase == 2` and are not counted as Phase 1 localization or implementation misses."
    - "The `advances` ITEM requirement IDs (`ITEM-01`…`ITEM-04`, `ITEM-07`) recorded on every entry remain visible in `01-INVENTORY.json` for Phase 2 to consume."
    - "Localization keys are additive only: no existing key is deleted or renamed by the exporter run."
    - "`01-INVENTORY.json` records the Phase 1 actions and fixed deviations, and `01-DEVIATIONS.md` is the consolidated deviation ledger (QUAL-05)."
    - "`dotnet build /p:Configuration=Release /p:WarningLevel=0` passes after the tranche work."
    - "No placeholder art exists and no existing binary/art asset was modified in the phase."
    - statement: "A tModLoader client displays each completed-art tranche item's localized name and tooltip in both languages."
      verification: backstop
  artifacts:
    - ".planning/phases/01-item-inventory-completed-art-items/01-DEVIATIONS.md"
    - ".planning/phases/01-item-inventory-completed-art-items/scripts/check-localization-coverage.ps1"
    - "additive keys in `Sources/Everglow/Localization/en-US/Mods.Everglow.Items.*.hjson` and `Sources/Everglow/Localization/zh-Hans/Mods.Everglow.Items.*.hjson`"
    - "updated `.planning/phases/01-item-inventory-completed-art-items/01-INVENTORY.json` (`deviations`, `phase1_actions`)"
  key_links:
    - "LocalizationCategory on tranche items -> exporter category files -> both culture trees"
    - "plan 03/04 SUMMARies -> 01-DEVIATIONS.md -> 01-INVENTORY.json `deviations`"
    - "`phase == 2` class-less entries -> excluded from Phase 1 localization coverage -> Phase 2 worklist"
  prohibitions:
    - "Never delete or rename an existing localization key."
    - "Never hand-create or reclassify localization keys; the in-game exporter places them by `LocalizationCategory`."
    - "Never create placeholder art or modify existing binary/art assets."
    - "Never mutate the Feishu design documents in this phase."
  assumptions:
    - "QUAL-05 edge coverage is unresolved/unclassified by the spec-less probe; acceptance for localization is the both-culture coverage check plus a passing build."
    - "The in-game exporter is the only sanctioned key generator (AGENTS.md); it may additively update category files beyond the tranche, which is accepted under the additive-only rule."
---

# Phase 1 Plan 05 — Localization Parity, Deviation Ledger, and Final Phase Gate

<objective>
Complete the tranche's localization behaviour in both supported languages, consolidate the Phase 1 action/deviation record into the inventory, and run the final Phase 1 gates.

Purpose: ROADMAP Phase 1 success criterion 3 requires the completed-art items to work "with the documented recipe, value, effect, and localization behaviour", and QUAL-05 requires the planning artifacts to record known deviations and verification evidence without mutating the Feishu source. This plan also owns the four `Tools/Developer` `LocalizationCategory` fixes moved out of plan 04, so classification happens in the plan that runs the exporter, and it confirms that the class-less entries routed to Phase 2 by plan 03 are excluded from Phase 1 coverage rather than reported as misses.

Scope note: of the files in `files_modified`, the `Sources/Everglow/Localization/{en-US,zh-Hans}/Mods.Everglow.Items.*.hjson` paths are additive outputs written mechanically by the in-game exporter (`ExportHjson.ExportCategoryFiles`), not hand-edited context. The hand-edited surface of this plan is the `01-INVENTORY.*`/`01-DEVIATIONS.md` artifacts, `scripts/check-localization-coverage.ps1`, and the four developer-tool category fixes.

Output: additive localization keys in both culture trees, `01-DEVIATIONS.md`, updated `01-INVENTORY.json`, `scripts/check-localization-coverage.ps1`, `LocalizationCategory` overrides on the four `Tools/Developer` classes, and a green Release build.
</objective>

<execution_context>
@C:/Users/book/.config/opencode/gsd-core/workflows/execute-plan.md
@C:/Users/book/.config/opencode/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/phases/01-item-inventory-completed-art-items/01-CONTEXT.md
@.planning/phases/01-item-inventory-completed-art-items/01-INVENTORY.json
@.planning/phases/01-item-inventory-completed-art-items/01-03-SUMMARY.md
@.planning/phases/01-item-inventory-completed-art-items/01-04-SUMMARY.md
@AGENTS.md
</context>

## Artifacts This Plan Produces

- `.planning/phases/01-item-inventory-completed-art-items/01-DEVIATIONS.md` — consolidated Phase 1 deviation ledger.
- `.planning/phases/01-item-inventory-completed-art-items/scripts/check-localization-coverage.ps1` — both-culture key coverage gate (`-AllowMissing` baseline mode).
- `LocalizationCategory` `Tools` overrides on the four `Tools/Developer` classes (moved from plan 04).
- Additive keys in `Sources/Everglow/Localization/en-US/Mods.Everglow.Items.*.hjson` and `Sources/Everglow/Localization/zh-Hans/Mods.Everglow.Items.*.hjson` (exporter-generated).
- Updated `.planning/phases/01-item-inventory-completed-art-items/01-INVENTORY.json` `deviations[]` and `phase1_actions[]`.

<tasks>

<task type="auto">
  <name>Consolidate the deviation ledger and record current localization coverage</name>
  <files>.planning/phases/01-item-inventory-completed-art-items/01-INVENTORY.json, .planning/phases/01-item-inventory-completed-art-items/01-INVENTORY.md, .planning/phases/01-item-inventory-completed-art-items/01-DEVIATIONS.md, .planning/phases/01-item-inventory-completed-art-items/scripts/check-localization-coverage.ps1, Sources/Modules/Yggdrasil/KelpCurtain/Items/Tools/Developer/GenerateMazeRoom.cs, Sources/Modules/Yggdrasil/KelpCurtain/Items/Tools/Developer/ResetIsleOfBloom.cs, Sources/Modules/Yggdrasil/KelpCurtain/Items/Tools/Developer/ResetKelpCurtain.cs, Sources/Modules/Yggdrasil/KelpCurtain/Items/Tools/Developer/UnderWaterDungeon.cs</files>
  <read_first>
    - `.planning/phases/01-item-inventory-completed-art-items/01-03-SUMMARY.md` and `01-04-SUMMARY.md` (deviation ledgers and covered-entry counts).
    - `.planning/phases/01-item-inventory-completed-art-items/01-INVENTORY.json` (entries, blockers, `phase`, `deferred`, `advances`).
    - `Sources/Everglow.Function/Localization/OutputLocalizationHjsonItem.cs` and `Sources/Everglow.Function/Localization/ExportHjson.cs` (exporter behaviour, additive keys).
    - `AGENTS.md` — localization rules (additive keys, use the exporter, both cultures).
  </read_first>
  <action>
    First apply the four developer-tool `LocalizationCategory` overrides moved here from plan 04: `GenerateMazeRoom`, `ResetIsleOfBloom`, `ResetKelpCurtain`, and `UnderWaterDungeon` each gain `public override string LocalizationCategory => ...Categories.Tools;` using the nearest local spelling, then run `dotnet build /p:Configuration=Release /p:WarningLevel=0` to confirm they compile before the exporter runs.
    Read the plan 03 and plan 04 SUMMARY deviation ledgers and merge them into a new `01-DEVIATIONS.md` (columns: entry id, class file, field, before, after, source header/block id) plus a `deviations` array and a `phase1_actions` array in `01-INVENTORY.json`; record each tranche's `advances` ITEM IDs (`ITEM-01`…`ITEM-04`, `ITEM-07`) in the ledger so Phase 2 can consume them, and record the plan-03 Phase 2 routing (43 class-less entries) as a Phase 1 action. Regenerate the corresponding `01-INVENTORY.md` sections in place — do not re-run `scripts/parse-design-xml.ps1` — and re-run `scripts/check-inventory-reconciliation.ps1` to keep JSON/Markdown consistent. Author `scripts/check-localization-coverage.ps1` with a `-AllowMissing` switch: it selects every `01-INVENTORY.json` entry whose `phase` is `1`, `artwork_complete` is true, `internal_name` is non-empty, and `localization.blocked` is not true, derives the class short name, and checks that name appears as a key in one `Sources/Everglow/Localization/en-US/Mods.Everglow.Items.*.hjson` file and one `Sources/Everglow/Localization/zh-Hans/Mods.Everglow.Items.*.hjson` file. Excluding `localization.blocked` entries is deliberate: Task 3 marks an item whose key could not be produced with `localization.blocked: true` and a blocker, and the gate must honor that recorded blocker instead of failing on the entry. With `-AllowMissing` it prints the missing set and exits 0; without it, it exits non-zero listing any selected item missing a key in either culture. Run it with `-AllowMissing` and keep the printed baseline in the plan SUMMARY. Do not write any HJSON key by hand.
  </action>
  <verify>
    <automated>dotnet build /p:Configuration=Release /p:WarningLevel=0; if ($?) { powershell -NoProfile -ExecutionPolicy Bypass -File ".planning/phases/01-item-inventory-completed-art-items/scripts/check-localization-coverage.ps1" -AllowMissing }</automated>
    <fails_when>non-zero exit — the Release build failed (the four overrides did not compile), or the coverage script itself errored (a missing-key report is expected and does not fail this task)</fails_when>
  </verify>
  <acceptance_criteria>
    - `01-DEVIATIONS.md` exists and lists every deviation recorded by plans 03/04 (or states "none" with the covered-entry counts).
    - `01-INVENTORY.json` contains `deviations` and `phase1_actions` arrays, every entry's `advances` IDs are retained, and it still passes `check-inventory-reconciliation.ps1`.
    - The four `Tools/Developer` classes declare `LocalizationCategory` `Tools` and the Release build is green.
    - `check-localization-coverage.ps1 -AllowMissing` exits 0 and prints the pre-exporter missing-key set; its candidate set excludes entries with `localization.blocked == true` and entries with `phase != 1`.
    - `01-INVENTORY.md` still contains exactly 103 matrix rows.
  </acceptance_criteria>
  <done>The Phase 1 deviation ledger is consolidated into the inventory, and the pre-exporter localization gap set is captured.</done>
</task>

<task type="checkpoint:human-action" gate="blocking">
  <name>Run the in-game localization exporter to add missing keys</name>
  <action>Run the `OutputLocalizationHjsonItem` exporter in a tModLoader client to additively generate the missing `en-US` and `zh-Hans` item keys.</action>
  <read_first>
    - `Sources/Everglow.Function/Localization/OutputLocalizationHjsonItem.cs` — the item is fired to export; it calls `ExportHjson.ExportCategoryFiles` for both cultures and never overwrites existing keys.
    - `AGENTS.md` — localization rule requiring the exporter instead of hand-created keys.
  </read_first>
  <instructions>
    The agent has already built the mod and recorded which completed-art items are missing keys. Only the in-game exporter can create them, and it is reachable only from inside a running tModLoader client (it is a consumable ModItem whose `Shoot` writes the HJSON category files).
    1. Launch the tModLoader client with the mods built by this workspace and enable Everglow.
    2. Obtain the `OutputLocalizationHjsonItem` item (developer item browser or a cheat mod).
    3. Select it and fire it once; the console prints counts such as `ModItem: N√ / M×`.
    4. Confirm it reported zero unclassified items (or note the unclassified lines) and that the game did not crash.
    Do not hand-edit any HJSON file.
  </instructions>
  <verification>The agent runs `scripts/check-localization-coverage.ps1` (no switch) and every completed-art tranche item with `phase == 1` that is not already `localization.blocked` has a key in both cultures; `git diff --stat` shows no existing key was removed.</verification>
  <resume-signal>Type "done" after the exporter has run (or describe why it could not be run).</resume-signal>
  <acceptance_criteria>
    - The exporter ran and reported its item counts.
    - HJSON changes are additive only (no deleted or renamed keys).
    - The resumable state lets the agent re-run the coverage check.
  </acceptance_criteria>
</task>

<task type="auto">
  <name>Run the final Phase 1 gates and hand off the audit trail</name>
  <files>.planning/phases/01-item-inventory-completed-art-items/01-INVENTORY.json, .planning/phases/01-item-inventory-completed-art-items/01-INVENTORY.md</files>
  <precondition>A local tModLoader installation is resolvable so `dotnet build` can package, and the exporter step above has completed.</precondition>
  <read_first>
    - `.planning/phases/01-item-inventory-completed-art-items/01-VALIDATION.md` — §"Manual-Only Verifications" and §"Sampling Rate".
    - `AGENTS.md` — build, BOM-check, and localization rules.
    - `.planning/phases/01-item-inventory-completed-art-items/scripts/check-localization-coverage.ps1`, `scripts/check-inventory-reconciliation.ps1`, and `scripts/check-tranche-A.ps1`.
  </read_first>
  <action>
    Run the final Phase 1 gates in order: `scripts/check-inventory-reconciliation.ps1`; `scripts/check-localization-coverage.ps1` (no switch); `dotnet build /p:Configuration=Release /p:WarningLevel=0`; the AGENTS.md byte-level BOM check against `origin/master`; and a no-placeholder check confirming no `*.png`/binary file was added or modified under `Sources/Modules/Yggdrasil/KelpCurtain/` or `Sources/Everglow/Localization/`. For any completed-art item with `phase == 1` still missing a key after the exporter, set that entry's `localization.blocked` field to `true` and add a blocker naming the missing culture(s), leaving its `artwork_complete`/`code_complete`/`status` values untouched and never inventing a key; because `check-localization-coverage.ps1` excludes `localization.blocked` entries, a recorded blocker is honored instead of failing the gate. Confirm that every class-less entry remains `phase == 2` and is not reported as a Phase 1 miss. Update `01-INVENTORY.md` to match, re-run the reconciliation checker and `check-localization-coverage.ps1` (no switch), and finalize the SUMMARY with the covered-entry counts, fixed-deviation count, Phase 2-routed count, deferred/blocked counts, and the Phase 2 hand-off note (remaining art-incomplete and class-less entries already routed in plan 03).
  </action>
  <verify>
    <automated>powershell -NoProfile -ExecutionPolicy Bypass -File ".planning/phases/01-item-inventory-completed-art-items/scripts/check-localization-coverage.ps1"</automated>
    <fails_when>non-zero exit, or the error names a Phase 1 completed-art item lacking a key in `en-US` or `zh-Hans`</fails_when>
    <human-check>Open the inventory matrix and confirm: every Phase 1 completed-art item is either green or explicitly yellow/blocked with a reason; every blocked entry is real (no missing artwork hidden by a placeholder); the 43 class-less entries are routed to Phase 2 with reasons; and the Phase 2 hand-off list matches the remaining entries.</human-check>
  </verify>
  <acceptance_criteria>
    - `check-inventory-reconciliation.ps1` and `check-localization-coverage.ps1` both exit 0.
    - `dotnet build /p:Configuration=Release /p:WarningLevel=0` exits 0 with no `error CS` or `error MSB`.
    - BOM check passes; no `.png`/binary asset change under `Sources/Modules/Yggdrasil/KelpCurtain/` or `Sources/Everglow/Localization/`.
    - Every class-less entry has `phase == 2` and is not counted as a Phase 1 miss.
    - Any Phase 1 completed-art item lacking a key after the exporter is recorded with `localization.blocked: true` and a named blocker, and is excluded from the coverage gate rather than failing it.
    - The SUMMARY records covered-entry counts, deviations, Phase 2-routed/deferred/blocked counts, and the Phase 2 hand-off.
  </acceptance_criteria>
  <done>Phase 1 is gated green: inventory consistent, both-culture localization present for the Phase 1 tranche, Release build passing, no placeholders, Phase 2 routing recorded, and the audit trail committed.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Fetched Feishu rows -> localization keys | Keys are generated by the exporter from class metadata, never from document text. |
| Item classes -> localization HJSON | Category placement is derived from `LocalizationCategory`, not hand-classified. |
| Plan SUMMARies -> inventory ledger | Deviation records are consolidated into the machine inventory. |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-05-01 | Tampering | Hand-created/misclassified localization keys | high | mitigate | Only the in-game exporter creates keys; the prohibition block and AGENTS.md rule forbid hand-classification. |
| T-05-02 | Information Disclosure | Credentials committed during fetch/consolidation | high | mitigate | Only DocxXML and non-secret metadata exist in the phase directory; no token is written. |
| T-05-03 | Tampering | Placeholder art hiding missing approved artwork | high | mitigate | No-placeholder audit over the item and localization trees; missing keys/art remain blocked. |
| T-05-04 | Repudiation | Deviation ledger diverging from the inventory | medium | mitigate | Reconciliation and consistency checks re-run after consolidation. |
| T-05-05 | Tampering | Instruction-like design text reaching localization values | medium | mitigate | Keys/values come from class metadata; document text is never an input to the exporter. |
| T-05-06 | Repudiation | Phase 2-routed entries silently counted as Phase 1 misses | medium | mitigate | The coverage gate filters on `phase == 1`; the final task confirms the 43 entries remain `phase == 2`. |
| T-05-SC | Tampering | Package supply chain | low | accept | No packages are installed in this phase. |

</threat_model>

<verification>
- `check-inventory-reconciliation.ps1` exits 0.
- `check-localization-coverage.ps1` exits 0.
- `dotnet build /p:Configuration=Release /p:WarningLevel=0` exits 0.
- BOM check passes; `git status --porcelain` shows no binary/art change.
</verification>

<success_criteria>
- The Phase 1 completed-art tranche has both-culture localization and documented behaviour (ROADMAP success criterion 3).
- QUAL-05's audit trail (evidence, deviations, blockers, deferred scope, Phase 2 routing, Phase 2 hand-off) is complete and consistent.
- Every entry's `advances` ITEM IDs are preserved for Phase 2, and the four developer tools are classified as `Tools`.
- No Feishu mutation, no placeholder art, no deleted localization keys, and no Phase 2-routed entry reported as a Phase 1 miss.
</success_criteria>

<output>
Create `.planning/phases/01-item-inventory-completed-art-items/01-05-SUMMARY.md` when done.
</output>
