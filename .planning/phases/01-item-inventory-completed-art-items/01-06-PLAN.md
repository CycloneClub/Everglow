---
phase: 01-item-inventory-completed-art-items
plan: 06
type: execute
wave: 6
depends_on: [01-05]
files_modified:
  - .planning/phases/01-item-inventory-completed-art-items/01-INVENTORY.json
  - .planning/phases/01-item-inventory-completed-art-items/01-INVENTORY.md
  - .planning/phases/01-item-inventory-completed-art-items/01-DEVIATIONS.md
  - .planning/phases/01-item-inventory-completed-art-items/scripts/check-inventory-reconciliation.ps1
  - .planning/phases/01-item-inventory-completed-art-items/scripts/check-carryover.ps1
  - .planning/ROADMAP.md
  - Sources/Modules/Yggdrasil/KelpCurtain/Items/Weapons/UnderwaterTreasury/ArmOfGiantTree.cs
  - Sources/Modules/Yggdrasil/KelpCurtain/Items/Materials/ElftigernPowder.cs
  - Sources/Modules/Yggdrasil/KelpCurtain/Items/Misc/ForestBreath.cs
  - Sources/Modules/Yggdrasil/KelpCurtain/Items/Misc/WitheredMask.cs
  - Sources/Modules/Yggdrasil/KelpCurtain/Items/Weapons/QuetzalsWish.cs
autonomous: true
requirements: [QUAL-05]
estimate:
  tokens: 48000
  raw_tokens: 48000
  tasks: 3
  confidence: low

must_haves:
  truths:
    - "Allocation is by design-artwork state only (2026-09-12 ROADMAP correction): of the 18 `phase==2` completed-art class-less entries, the 5 non-boss item entries are `phase==1` and the 13 boss/special-encounter entries are `phase==7`."
    - "Every Phase 1 carry-over entry (Arm of Giant Tree, 森林之息, 厄佛提根的净化粉末, 枯萎面具, 魁札尔的愿望) either resolves its `internal_name` to a git-tracked `.cs` class under `Sources/Modules/Yggdrasil/KelpCurtain/Items/` or carries a recorded missing-artwork blocker; none is covered only by the generic design-code/no-repo markers."
    - "The 13 boss entries map to the correct roadmap requirement (9 Giant Winged Dragon to ITEM-06, 4 Klein Snake series to ITEM-05) and are not counted as Phase 1 or Phase 2 work."
    - "No placeholder art is created and no binary/art asset is modified: the two pre-existing orphan textures `Weapons/UnderwaterTreasury/ArmOfGiantTree.png` and `Materials/ElftigernPowder.png` are consumed by their classes, and the three entries with no repository texture carry a named artwork blocker."
    - "The matrix still has exactly 103 rows and the JSON/Markdown mirrors stay consistent (QUAL-05)."
    - "`dotnet build /p:Configuration=Release /p:WarningLevel=0` exits 0 after the new classes are added."
    - "Localization stays deferred under the user directive of 2026-09-12: the in-game exporter is not run, no HJSON key is hand-edited, and the new entries are recorded in the deferred-localization ledger with `check-localization-coverage.ps1 -AllowMissing` as advisory evidence."
    - "`scripts/parse-design-xml.ps1` is not re-run (CR-01 data-loss guard) and the Feishu design source is not mutated."
    - statement: "A tModLoader client can obtain the five carry-over items through their designed chest/trade sources and use each class in game once the Phase 6 loot tables exist."
      verification: backstop
  artifacts:
    - ".planning/phases/01-item-inventory-completed-art-items/scripts/check-carryover.ps1"
    - "updated `.planning/phases/01-item-inventory-completed-art-items/01-INVENTORY.json` (`phase`, `deferred`, `deferred_reason`, `internal_name`, `repo_asset`, `blockers`, `notes`, `phase1_actions`, `deviations`)"
    - "updated `.planning/phases/01-item-inventory-completed-art-items/01-INVENTORY.md` and `01-DEVIATIONS.md`"
    - "updated `scripts/check-inventory-reconciliation.ps1` (recorded-artwork-blocker exception)"
    - "`Sources/Modules/Yggdrasil/KelpCurtain/Items/Weapons/UnderwaterTreasury/ArmOfGiantTree.cs`"
    - "`Sources/Modules/Yggdrasil/KelpCurtain/Items/Materials/ElftigernPowder.cs`"
    - "`Sources/Modules/Yggdrasil/KelpCurtain/Items/Misc/ForestBreath.cs`"
    - "`Sources/Modules/Yggdrasil/KelpCurtain/Items/Misc/WitheredMask.cs`"
    - "`Sources/Modules/Yggdrasil/KelpCurtain/Items/Weapons/QuetzalsWish.cs`"
  key_links:
    - "18 class-less completed-art entries -> allocation by design section (non-boss item table vs Giant Winged Dragon / Klein Snake section) -> `phase` 1 or 7"
    - "phase==1 carry-over entries -> `check-carryover.ps1` -> tracked `.cs` class or recorded missing-art blocker"
    - "orphan textures `ArmOfGiantTree.png` / `ElftigernPowder.png` -> default `ModAsset` texture resolution beside the new `.cs`"
    - "9 Giant Winged Dragon rows -> ITEM-06; 4 Klein Snake rows -> ITEM-05 (ROADMAP Phase 7)"
  prohibitions:
    - "Never re-run `scripts/parse-design-xml.ps1`; it regenerates a base inventory with no reconciliation (CR-01) and would destroy the plans 02-05 record."
    - "Never create placeholder art or modify any `.png`/binary asset; reuse only art already in the repository."
    - "Never run the in-game localization exporter in this plan and never hand-create or hand-edit an HJSON key (user directive 2026-09-12; AGENTS.md)."
    - "Never mutate the Feishu design documents; Phase 1 is read-only against the design source."
  assumptions:
    - "Giant Winged Dragon mapping is inferred from the biology-design section `特殊：/巨翼龙` (`evidence/biology.xml`) and its tail-kill text (line 859: 破坏剑尾后必定掉落血肉聚合物、龙骸巨块大剑与整套狂战士护具) plus its `掉落物` / `可制作装备` tables; ITEM-06 owns that reward chain."
    - "Klein Snake mapping is inferred from the item-design H1 `克莱因蛇系列` (`evidence/item.xml` table `N4W9drIpio4C86xx8rDc04JJn3e`); ITEM-05 owns that series."
    - "Three carry-over entries (森林之息, 枯萎面具, 魁札尔的愿望) have no repository texture despite the Feishu artwork checkbox; they are implemented as code and recorded with a missing-artwork blocker rather than fabricating art (carry-over directive; D-06)."
    - "魁札尔的愿望's design row is Melee (`mel37`, 巨刃类武器) while the parser heuristic set its inventory `category` to `weapons.ranged`; the class is implemented as Melee per the design and the discrepancy is recorded, leaving the `id` and matrix grouping unchanged."
    - "`code_complete` continues to mirror the Feishu 代码 checkbox, so implemented carry-over entries keep `code_complete:false` / `status:unchecked` until Phase 8 writes the Feishu status."
---

# Phase 1 Plan 06 (Carry-Over) — Reallocate the 18 Class-Less Completed-Art Entries and Implement the Phase 1 Slice

<objective>
Correct the entry allocation for the 18 completed-art class-less entries that the superseded "class-less -> Phase 2" rule mis-routed, and implement the Phase 1 slice of them.

Purpose: the 2026-09-12 ROADMAP correction makes allocation depend on design-artwork state only. Two-step allocation applies: (1) design section -> phase (non-boss item tables stay in the item phases; the Giant Winged Dragon and Klein Snake sections belong to Phase 7 via ITEM-06/ITEM-05); (2) within the item phases, Feishu artwork state -> Phase 1 (complete) or Phase 2 (incomplete). Applying that rule, 5 entries are Phase 1 carry-over and 13 are Phase 7. This plan brings the 5 Phase 1 entries to implementation parity and corrects the 13 to their Phase 7 home, without re-running the destructive parser (CR-01) and without touching art or localization.

Output: corrected `01-INVENTORY.json`/`.md` and `01-DEVIATIONS.md`, the extended reconciliation gate, a new class-or-blocker carry-over gate (`scripts/check-carryover.ps1`), five implemented item classes, and a green Release build.
</objective>

<execution_context>
@C:/Users/book/.config/opencode/gsd-core/workflows/execute-plan.md
@C:/Users/book/.config/opencode/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/01-item-inventory-completed-art-items/01-CONTEXT.md
@.planning/phases/01-item-inventory-completed-art-items/01-PATTERNS.md
@.planning/phases/01-item-inventory-completed-art-items/01-INVENTORY.json
@.planning/phases/01-item-inventory-completed-art-items/01-DEVIATIONS.md
@.planning/phases/01-item-inventory-completed-art-items/evidence/biology.xml
@.planning/phases/01-item-inventory-completed-art-items/evidence/item.xml
@AGENTS.md
</context>

## Allocation Decision (source of truth for this plan)

**Phase 1 carry-over (5) — set `phase:1`, clear `deferred`; implement here:**

| Entry id | Name (zh) | Design section (evidence) | Advances | Class / texture |
| --- | --- | --- | --- | --- |
| `item-weapons.misc-arm-of-giant-tree` | 巨树之臂 | `item.xml` H2 水下宝库物品, table `doxcnnJSAMpWWmUqUwHGBjnoX3d` | ITEM-03 | `ArmOfGiantTree.cs` / repo texture exists |
| `item-weapons.misc-森林之息` | 森林之息 | `item.xml` H2 水下迷宫宝箱物品, table `doxcnlTJMIxnmaRlOltbxZDYzGc` | ITEM-03 | `ForestBreath.cs` / no repo texture |
| `item-weapons.misc-厄佛提根的净化粉末` | 厄佛提根的净化粉末 | `item.xml` H2 NPC交易物品, table `doxcnK1twRSRyYrOvXg4tyBBkxf` | ITEM-04 | `ElftigernPowder.cs` / repo texture exists |
| `item-weapons.misc-枯萎面具` | 枯萎面具 | `item.xml` H2 NPC交易物品, table `doxcnK1twRSRyYrOvXg4tyBBkxf` | ITEM-04 | `WitheredMask.cs` / no repo texture |
| `item-weapons.ranged-魁札尔的愿望` | 魁札尔的愿望 | `item.xml` H2 NPC交易物品, table `doxcnK1twRSRyYrOvXg4tyBBkxf` | ITEM-04 | `QuetzalsWish.cs` / no repo texture |

**Phase 7 correction (13) — set `phase:7`, clear `deferred`; no implementation in this phase:**

| Entry id | Name (zh) | Owner / mapping |
| --- | --- | --- |
| `biology_drop-weapons.melee-龙骸巨块大剑` | 龙骸巨块大剑 | Giant Winged Dragon tail-kill drop -> ITEM-06 |
| `biology_drop-weapons.misc-血肉聚合物` | 血肉聚合物 | Giant Winged Dragon tail-kill drop -> ITEM-06 |
| `biology_drop-weapons.melee-狂战士角盔` | 狂战士角盔 | Giant Winged Dragon tail-kill drop -> ITEM-06 |
| `biology_drop-weapons.melee-狂战士板甲` | 狂战士板甲 | Giant Winged Dragon tail-kill drop -> ITEM-06 |
| `biology_drop-weapons.melee-狂战士胫甲` | 狂战士胫甲 | Giant Winged Dragon tail-kill drop -> ITEM-06 |
| `biology_drop-weapons.misc-巨翼龙圣物-大师` | 巨翼龙圣物（大师） | Giant Winged Dragon relic -> ITEM-06 |
| `biology_drop-weapons.misc-巨翼龙纪念章` | 巨翼龙纪念章 | Giant Winged Dragon medal -> ITEM-06 |
| `biology_drop-weapons.magic-崩解阈限` | 崩解阈限 | Giant Winged Dragon craftable -> ITEM-06 |
| `biology_drop-weapons.melee-骇翼链剑` | 骇翼链剑 | Giant Winged Dragon craftable -> ITEM-06 |
| `item-weapons.melee-碧绿玉髓扇` | 碧绿玉髓扇 | Klein Snake series (`N4W9drIpio4C86xx8rDc04JJn3e`) -> ITEM-05 |
| `item-weapons.ranged-龙骨猎枪` | 龙骨猎枪 | Klein Snake series -> ITEM-05 |
| `item-weapons.melee-碧玉弯刀` | 碧玉弯刀 | Klein Snake series -> ITEM-05 |
| `item-weapons.summon-魂蛇手杖` | 魂蛇手杖 | Klein Snake series -> ITEM-05 |

Resulting phase counts: `{1: 65, 2: 25, 7: 13}` (was `{1: 60, 2: 43}`). The 25 remaining `phase:2` entries are the artwork-incomplete class-less entries.

## Artifacts This Plan Produces

- `.planning/phases/01-item-inventory-completed-art-items/scripts/check-carryover.ps1` — class-or-recorded-blocker gate for the Phase 1 carry-over entries.
- `scripts/check-inventory-reconciliation.ps1` extended to accept the D-06 recorded-artwork-blocker conflict.
- Corrected `01-INVENTORY.json` / `01-INVENTORY.md` and the `01-DEVIATIONS.md` allocation-correction record.
- Five item classes under `Sources/Modules/Yggdrasil/KelpCurtain/Items/`.
- Updated `.planning/ROADMAP.md` cross-cutting constraint recording the 5/13 split.

<tasks>

<task type="auto">
  <name>Task 1: Reconcile the 18-entry allocation in the inventory, ledger, and reconciliation gate</name>
  <files>.planning/phases/01-item-inventory-completed-art-items/01-INVENTORY.json, .planning/phases/01-item-inventory-completed-art-items/01-INVENTORY.md, .planning/phases/01-item-inventory-completed-art-items/01-DEVIATIONS.md, .planning/phases/01-item-inventory-completed-art-items/scripts/check-inventory-reconciliation.ps1, .planning/ROADMAP.md</files>
  <read_first>
    - `.planning/phases/01-item-inventory-completed-art-items/01-INVENTORY.json` (the 18 entries with `phase==2 && artwork_complete==true`).
    - `.planning/phases/01-item-inventory-completed-art-items/01-DEVIATIONS.md` and `01-CONTEXT.md` (D-05, D-06, D-07 semantics).
    - `.planning/phases/01-item-inventory-completed-art-items/evidence/biology.xml` (sections `特殊：/巨翼龙`, `苍带帘蛇/克莱因蛇`) and `evidence/item.xml` (H2 水下宝库物品, 水下迷宫宝箱物品, NPC交易物品, H1 克莱因蛇系列).
    - `.planning/ROADMAP.md` § Phase 1 cross-cutting constraints and § Phase 7.
    - `.planning/phases/01-item-inventory-completed-art-items/01-REVIEW.md` CR-01 (parser overwrite hazard).
    - `.planning/phases/01-item-inventory-completed-art-items/scripts/check-inventory-reconciliation.ps1`.
  </read_first>
  <action>
    Apply the Allocation Decision table above. For the five Phase 1 ids (`item-weapons.misc-arm-of-giant-tree`, `item-weapons.misc-森林之息`, `item-weapons.misc-厄佛提根的净化粉末`, `item-weapons.misc-枯萎面具`, `item-weapons.ranged-魁札尔的愿望`) set `phase` to 1, set `deferred` to false, set `deferred_reason` to empty, add a `carry_over` boolean true, and append a `notes` sentence: "2026-09-12 allocation: artwork complete, no repository class -> Phase 1 carry-over (design section {table})." Leave `artwork_complete`, `code_complete`, `status`, `tranche`, `advances`, `blockers` and `feishu` unchanged in this task.
    For the thirteen Phase 7 ids (the nine `biology_drop-*` Giant Winged Dragon rows and the four `item-weapons.*` Klein Snake rows) set `phase` to 7, `deferred` to false, `deferred_reason` to empty, append the owner requirement id to `advances` (`ITEM-06` for the nine Giant Winged Dragon rows, `ITEM-05` for the four Klein Snake rows) without removing existing ids, and append a `notes` sentence naming the design section and encounter. Leave the Feishu fields, checkboxes, `status` and `tranche` unchanged.
    Append a `phase1_actions` record (next free id, `P1A-12`) titled "Allocation correction (2026-09-12): completed-art class-less entries split 5 Phase 1 / 13 Phase 7", detailing the two-step rule and the resulting counts.
    Do not run `scripts/parse-design-xml.ps1` at any point (CR-01); edit the JSON in place with structured edits only.
    Update `01-INVENTORY.md`: add a short "Allocation correction (2026-09-12)" note under the header explaining the 5/13 split, refresh any Blockers cell that changed, keep exactly 103 matrix rows and keep the JSON/Markdown mirror aligned. Update `01-DEVIATIONS.md` with an "Allocation Correction" section that lists the 5 Phase 1 and 13 Phase 7 entries, the evidence section for each, and the superseded `P1A-03` routing; note that the 25 remaining `phase:2` entries are artwork-incomplete.
    Extend `scripts/check-inventory-reconciliation.ps1` so the `repo_asset` guard accepts the D-06 conflict: when `source_kind` is `item`, `internal_name` is non-empty, `repo_asset` is empty and `artwork_complete` is true, pass only if a blocker matches `texture|artwork`, otherwise keep failing. This preserves the no-silent-artwork-gap intent while honoring a recorded blocker.
    Update the `.planning/ROADMAP.md` Phase 1 cross-cutting constraint and Phase 7 note to record the corrected split (5 Phase 1 carry-over, 13 Phase 7 encounter rewards) and revise the sentence that currently treats all 18 as Phase 1 carry-over. Use a scoped edit, not a whole-file rewrite.
  </action>
  <verify>
    <automated>powershell -NoProfile -ExecutionPolicy Bypass -File ".planning/phases/01-item-inventory-completed-art-items/scripts/validate-inventory.ps1"; if ($?) { powershell -NoProfile -ExecutionPolicy Bypass -File ".planning/phases/01-item-inventory-completed-art-items/scripts/check-inventory-reconciliation.ps1" }</automated>
    <fails_when>non-zero exit from either script, or the `check-inventory-reconciliation.ps1` summary line does not report 103 entries with deferred count 3 (the `item-weapons.misc-a`, `-b` and `-c-名字要普通` placeholder rows; the D-11 tracked blockers RadialCarapace and VineRepairWand are not deferred).</fails_when>
  </verify>
  <acceptance_criteria>
    - Exactly five entries read `phase:1` with `carry_over:true`, `deferred:false` and empty `deferred_reason`.
    - Exactly thirteen entries read `phase:7` with `deferred:false`, the correct `ITEM-05`/`ITEM-06` id appended to `advances`, and a Phase 7 `notes` sentence.
    - Phase distribution is `{1: 65, 2: 25, 7: 13}` and total entries remain 103.
    - `scripts/parse-design-xml.ps1` file hash is unchanged from the committed version (not re-run).
    - `01-INVENTORY.md` still has 103 matrix rows and `01-DEVIATIONS.md` records the allocation correction.
    - `validate-inventory.ps1` and `check-inventory-reconciliation.ps1` both exit 0 after the `repo_asset` guard extension.
  </acceptance_criteria>
  <done>The 18 entries are correctly allocated (5 Phase 1, 13 Phase 7), the record is consistent, and the reconciliation gate accepts the recorded-artwork-blocker path.</done>
  <commit>docs(01-06): correct 18-entry completed-art allocation to 5 Phase 1 / 13 Phase 7</commit>
</task>

<task type="auto">
  <name>Task 2: Implement the five Phase 1 carry-over item classes and add the carry-over coverage gate</name>
  <files>Sources/Modules/Yggdrasil/KelpCurtain/Items/Weapons/UnderwaterTreasury/ArmOfGiantTree.cs, Sources/Modules/Yggdrasil/KelpCurtain/Items/Materials/ElftigernPowder.cs, Sources/Modules/Yggdrasil/KelpCurtain/Items/Misc/ForestBreath.cs, Sources/Modules/Yggdrasil/KelpCurtain/Items/Misc/WitheredMask.cs, Sources/Modules/Yggdrasil/KelpCurtain/Items/Weapons/QuetzalsWish.cs, .planning/phases/01-item-inventory-completed-art-items/scripts/check-carryover.ps1, .planning/phases/01-item-inventory-completed-art-items/01-INVENTORY.json, .planning/phases/01-item-inventory-completed-art-items/01-INVENTORY.md</files>
  <read_first>
    - `.planning/phases/01-item-inventory-completed-art-items/01-PATTERNS.md` (Weapons, Materials, Placeables, localization patterns).
    - `Sources/Modules/Yggdrasil/KelpCurtain/Items/Weapons/BladeOfGreenMoss.cs`, `.../Weapons/UnderwaterTreasury/ArcI.cs`, `.../Materials/CrimsonMoonSap.cs`, `.../Materials/DevilHeartIronBar_Item.cs` (nearest analogs).
    - `.planning/phases/01-item-inventory-completed-art-items/evidence/item.xml` rows: Arm of Giant Tree (`doxcnDtyyoVnv7A9vcZWjDND08c`), 森林之息 (`doxcnNLPLMZzujj3DNfLnTkgwfd`), 厄佛提根的净化粉末 (`doxcnMokilvUc63h6O7GgW67vPf`), 枯萎面具 (`doxcnEncDOzn1wTCbCub6Q4nnRH`), 魁札尔的愿望 (`doxcnD2XMx9mBf5DL0hUqX98vqb`).
    - `Sources/Everglow.Function/Utilities/LocalizationUtils.cs` (`Categories.MeleeWeapons`, `Categories.Materials`, `Categories.Vanity`, `Categories.Miscs`).
    - `AGENTS.md` and `Sources/Modules/Yggdrasil/AGENTS.md` (no placeholder art, tabs/LF/UTF-8-no-BOM, `LocalizationCategory` required).
  </read_first>
  <action>
    Implement five ordinary `ModItem` classes, each overriding `LocalizationCategory` and using tab indentation, file-scoped namespaces, Allman braces, LF, UTF-8 without BOM, and a trailing newline. Do not run the localization exporter.
    `ArmOfGiantTree` (namespace `Everglow.Yggdrasil.KelpCurtain.Items.Weapons.UnderwaterTreasury`, category MeleeWeapons): damage 55, `DamageClass.Melee`, knockback 7.25, use time/animation 45 for the smash with a 30-frame retract lockout, `ItemRarityID.Orange`, `Item.value` 2 gold, auto-reuse. Implement the documented charge behavior: holding the attack charges to a 2.5 s maximum for 200% damage plus a 100% shockwave, scaling linearly down to 75% below full charge, and a right-click normal swing. Reuse the sibling repository texture by keeping the class beside `ArmOfGiantTree.png`; do not add or edit any `.png`.
    `ElftigernPowder` (namespace `...Items.Materials`, category Materials): consumable, `ItemRarityID.Blue`, `Item.value` 25 silver, `Item.CommonMaxStack`. The designed effect is purifying a Wilted Zone; that system is GAME-03/Phase 6. Implement the item identity and any available hook, and record a precise dependency blocker "effect requires the Wilted Zone restoration system (GAME-03/Phase 6)". Reuse the sibling `ElftigernPowder.png`.
    `ForestBreath` (namespace `...Items.Misc`, category Miscs): the design row is a quest item (任务道具) with `ItemRarityID.Blue`, `Item.value` 50 silver and `Item.CommonMaxStack`; implement the item identity. Record a note that its procurement is the underwater-maze chest and its consumer is Phase 6 quest content. No repository texture exists, so record the artwork blocker described below.
    `WitheredMask` (namespace `...Items.Misc`, category Vanity): the design row is a vanity item (时装道具) with `ItemRarityID.Blue` and `Item.value` 15 silver; implement the vanity identity with no combat stats. No repository texture exists, so record the artwork blocker described below.
    `QuetzalsWish` (namespace `...Items.Weapons`, category MeleeWeapons): implement per the design row as a melee giant-blade weapon with damage 37, `DamageClass.Melee`, critical strike chance 8, `ItemRarityID.Pink` and `Item.value` 10 gold, and implement the documented four-stage left-click combo plus the charged right-click throw and explosion and the wound debuff where a projectile/buff asset already exists. Record a precise effect blocker for any stage that needs a new projectile, buff or VFX whose texture is absent; record the design-vs-parser category discrepancy (design Melee, inventory field `weapons.ranged`) in the deviation ledger. No repository texture exists, so record the artwork blocker described below.
    Never fabricate art. For `ForestBreath`, `WitheredMask` and `QuetzalsWish`, add the blocker text "approved texture (贴图) missing from repository despite Feishu artwork checkbox; class implemented, art pending" and set `repo_asset` to empty.
    Update `01-INVENTORY.json` for the five entries: set `internal_name` to the full class name for all five, set `repo_asset` to the sibling texture path for `ArmOfGiantTree` and `ElftigernPowder`, remove the "no repo implementation found" blocker where the class now exists, and add the artwork/effect blockers above. Keep `artwork_complete`, `code_complete`, `status`, `tranche`, `advances` and `feishu` unchanged (a Feishu status colour still needs both Feishu checkboxes complete). Refresh `01-INVENTORY.md` Blockers cells to match and keep 103 rows.
    Author `scripts/check-carryover.ps1`: select every entry with `phase == 1` and `carry_over == true`; for each, require either a non-empty `internal_name` whose class short name resolves to a git-tracked `.cs` under `Sources/Modules/Yggdrasil/KelpCurtain/Items`, or a recorded blocker matching `texture|artwork`; fail when an entry is covered only by the generic design-code or no-repo markers; include the same added/modified `.png` no-placeholder guard as `check-tranche-A.ps1`; exit 0 printing the covered count. Keep the script 100% ASCII and read the inventory through `[IO.File]::ReadAllText`.
    Build with `dotnet build /p:Configuration=Release /p:WarningLevel=0`.
  </action>
  <verify>
    <automated>dotnet build /p:Configuration=Release /p:WarningLevel=0; if ($?) { powershell -NoProfile -ExecutionPolicy Bypass -File ".planning/phases/01-item-inventory-completed-art-items/scripts/check-carryover.ps1"; if ($?) { powershell -NoProfile -ExecutionPolicy Bypass -File ".planning/phases/01-item-inventory-completed-art-items/scripts/check-tranche-A.ps1"; if ($?) { powershell -NoProfile -ExecutionPolicy Bypass -File ".planning/phases/01-item-inventory-completed-art-items/scripts/check-inventory-reconciliation.ps1" } } }</automated>
    <fails_when>non-zero exit: a compile error (`error CS`), a carry-over entry lacking both a tracked class and a recorded artwork blocker, a check-tranche-A failure, a reconciliation failure, or the no-placeholder guard detecting an added/modified `.png`.</fails_when>
  </verify>
  <acceptance_criteria>
    - Five new `.cs` files exist and compile in the Release build with no `error CS`.
    - Each of the five entries has a non-empty `internal_name` resolving to a tracked class; `ArmOfGiantTree` and `ElftigernPowder` also have a `repo_asset`; the other three carry the missing-texture blocker.
    - `check-carryover.ps1` exits 0 with 5 covered entries and its no-placeholder guard reports no added/modified `.png`.
    - `check-tranche-A.ps1` exits 0 and its covered count increased by the five new entries.
    - `check-inventory-reconciliation.ps1` exits 0 with 103 entries.
    - No `.png` or binary asset was added or modified (`git status --porcelain` shows only `.cs` and planning artifacts under the item tree).
  </acceptance_criteria>
  <done>The five carry-over classes are implemented, the inventory records their classes/assets/blockers, and both coverage gates plus the Release build pass.</done>
  <commit>feat(01-06): implement five Phase 1 carry-over items + carry-over coverage gate</commit>
</task>

<task type="auto">
  <name>Task 3: Record the localization deferral and run the final carry-over gates</name>
  <files>.planning/phases/01-item-inventory-completed-art-items/01-INVENTORY.json, .planning/phases/01-item-inventory-completed-art-items/01-INVENTORY.md, .planning/phases/01-item-inventory-completed-art-items/01-DEVIATIONS.md</files>
  <read_first>
    - `.planning/phases/01-item-inventory-completed-art-items/01-DEVIATIONS.md` § "Deferred Localization (User Directive)" and § "Phase 1 Actions".
    - `AGENTS.md` (localization rules, byte-level BOM check) and `Sources/Everglow.Function/Localization/OutputLocalizationHjsonItem.cs`.
    - `.planning/phases/01-item-inventory-completed-art-items/scripts/check-localization-coverage.ps1`, `check-inventory-reconciliation.ps1`, `check-tranche-A.ps1`, `check-carryover.ps1`.
  </read_first>
  <action>
    Do not run the in-game `OutputLocalizationHjsonItem` exporter and do not hand-edit any HJSON file (user directive 2026-09-12). Confirm each of the five new classes overrides `LocalizationCategory` so a future exporter run can classify them.
    Add the five new entries to the deferred-localization record in `01-DEVIATIONS.md` (status `deferred`, reason "localization deferred by user directive (2026-09-12)") and mirror them in `01-INVENTORY.json` `deviations[]` with the same `status`/`reason`, leaving `localization.blocked` false. Record a `phase1_actions` entry that extends P1A-11 to the five carry-over entries.
    Run the final gates in order and capture their output for the SUMMARY: `scripts/validate-inventory.ps1`; `scripts/check-inventory-reconciliation.ps1`; `scripts/check-tranche-A.ps1`; `scripts/check-tranche-B.ps1`; `scripts/check-carryover.ps1`; `scripts/check-localization-coverage.ps1 -AllowMissing`; `dotnet build /p:Configuration=Release /p:WarningLevel=0`; the AGENTS.md byte-level UTF-8 BOM check against `origin/master`; and a no-placeholder check confirming no `.png`/binary file changed under `Sources/Modules/Yggdrasil/KelpCurtain/`. The strict `check-localization-coverage.ps1` (no switch) is expected to stay red by directive; record its missing set as deferred evidence rather than failing.
    Add a closing "Carry-Over Gate" section to `01-DEVIATIONS.md` summarizing the 5 Phase 1 entries implemented, the 13 Phase 7 corrections, the 3 artwork blockers, the 25 remaining `phase:2` entries, and the localization deferral. Finalize the plan SUMMARY with these counts and the exact gate output.
  </action>
  <verify>
    <automated>powershell -NoProfile -ExecutionPolicy Bypass -File ".planning/phases/01-item-inventory-completed-art-items/scripts/check-carryover.ps1"; if ($?) { powershell -NoProfile -ExecutionPolicy Bypass -File ".planning/phases/01-item-inventory-completed-art-items/scripts/check-tranche-A.ps1"; if ($?) { powershell -NoProfile -ExecutionPolicy Bypass -File ".planning/phases/01-item-inventory-completed-art-items/scripts/check-inventory-reconciliation.ps1"; if ($?) { powershell -NoProfile -ExecutionPolicy Bypass -File ".planning/phases/01-item-inventory-completed-art-items/scripts/check-localization-coverage.ps1" -AllowMissing; if ($?) { dotnet build /p:Configuration=Release /p:WarningLevel=0 } } } }</automated>
    <fails_when>non-zero exit from any gate or the Release build; the strict coverage run (no switch) is not part of this command and its red result is expected and recorded, not a failure.</fails_when>
  </verify>
  <acceptance_criteria>
    - The five new entries appear in the deferred-localization ledger with `status: deferred` and no HJSON file was created or edited.
    - `check-carryover.ps1`, `check-tranche-A.ps1`, `check-tranche-B.ps1`, `check-inventory-reconciliation.ps1` and `validate-inventory.ps1` all exit 0.
    - `check-localization-coverage.ps1 -AllowMissing` exits 0 and its missing set includes the five carry-over entries as deferred evidence.
    - `dotnet build /p:Configuration=Release /p:WarningLevel=0` exits 0; BOM check passes; no `.png`/binary change under the item tree.
    - `01-DEVIATIONS.md` records the 5/13 split, the 3 artwork blockers, the 25 remaining Phase 2 entries and the localization deferral.
  </acceptance_criteria>
  <done>Phase 1 carry-over is gated: allocation corrected, the five items implemented with recorded artwork/effect blockers, localization deferral recorded, and all runnable gates plus the Release build green.</done>
  <commit>docs(01-06): record carry-over localization deferral and pass final Phase 1 gates</commit>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Fetched Feishu rows -> inventory routing | Design text is treated as data; phase assignment is derived from committed section evidence, never from executing document content. |
| Inventory -> coverage gate | The gate reads `internal_name`/`repo_asset`/`blockers` from the JSON and repo `git ls-files`. |
| Item classes -> asset pipeline | Textures resolve through source-generated `ModAsset` paths beside the `.cs`; no hand-written paths. |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-06-01 | Tampering | Re-running `parse-design-xml.ps1` destroys the reconciled inventory | high | mitigate | Prohibition block; edit the JSON in place; Task 1 acceptance checks the parser file is unchanged (CR-01). |
| T-06-02 | Tampering | Placeholder art hiding missing approved artwork | high | mitigate | Reuse only existing repo textures; three entries carry a missing-texture blocker; the carry-over gate and tranche-A gate both run a no-placeholder `.png` guard. |
| T-06-03 | Repudiation | Boss rewards silently re-routed into Phase 1/2 | medium | mitigate | The 13 Phase 7 entries are explicitly listed with their ITEM-05/ITEM-06 mapping; the ROADMAP constraint is updated and the allocation is mirrored in `phase1_actions`. |
| T-06-04 | Tampering | Hand-created localization keys | high | mitigate | The exporter is not run and no HJSON is edited; deferral is recorded as evidence (AGENTS.md rule). |
| T-06-05 | Repudiation | Inventory/Markdown mirror divergence | medium | mitigate | `check-inventory-reconciliation.ps1` re-checks 103 rows and field invariants after every task. |
| T-06-06 | Information Disclosure | Feishu credential/token committed | medium | mitigate | Only the committed XML snapshots and non-secret block ids are read; no token is written. |
| T-06-SC | Tampering | Package supply chain | low | accept | No package is installed in this plan. |
</threat_model>

<verification>
- `scripts/validate-inventory.ps1` exits 0.
- `scripts/check-inventory-reconciliation.ps1` exits 0 with 103 entries and phase counts `{1: 65, 2: 25, 7: 13}`.
- `scripts/check-carryover.ps1` exits 0 with 5 covered carry-over entries and no added/modified `.png`.
- `scripts/check-tranche-A.ps1` and `scripts/check-tranche-B.ps1` exit 0.
- `scripts/check-localization-coverage.ps1 -AllowMissing` exits 0 and lists the deferred entries.
- `dotnet build /p:Configuration=Release /p:WarningLevel=0` exits 0.
- AGENTS.md byte-level BOM check passes; `git status --porcelain` shows no binary/art change.
- `scripts/parse-design-xml.ps1` is unchanged and was not re-run.
</verification>

<success_criteria>
- The 2026-09-12 corrected allocation rule is applied: 5 completed-art class-less entries are Phase 1 and 13 boss/encounter entries are Phase 7 (ITEM-05/ITEM-06).
- Every Phase 1 carry-over entry is implemented as a tracked class or carries a recorded missing-artifact blocker; no placeholder art exists.
- The inventory mirror, deviation ledger and gates are consistent (QUAL-05), and the Release build is green.
- Localization remains deferred under the user directive with no hand-edited keys, and the design source is untouched.
</success_criteria>

<output>
Create `.planning/phases/01-item-inventory-completed-art-items/01-06-SUMMARY.md` when done.
</output>
