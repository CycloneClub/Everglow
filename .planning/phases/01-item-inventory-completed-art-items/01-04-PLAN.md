---
phase: 01-item-inventory-completed-art-items
plan: 04
type: execute
wave: 3
depends_on: [01-02]
files_modified:
  - Sources/Modules/Yggdrasil/KelpCurtain/Items/Placeables/GreenCourtBrick_Item.cs
  - Sources/Modules/Yggdrasil/KelpCurtain/Items/Placeables/AgedGreenCourtBrick_Item.cs
  - Sources/Modules/Yggdrasil/KelpCurtain/Items/Placeables/WaterErodedGreenBrick_Item.cs
  - Sources/Modules/Yggdrasil/KelpCurtain/Items/Placeables/ForestRainVineTile_Thick_Item.cs
  - Sources/Modules/Yggdrasil/KelpCurtain/Items/Placeables/ForestRainVineTile_Thin_Item.cs
  - Sources/Modules/Yggdrasil/KelpCurtain/Items/Placeables/Walls/AgedGreenCourtWall_Item.cs
  - Sources/Modules/Yggdrasil/KelpCurtain/Items/Placeables/DecayingWoodCourt/CrackedForestThrone_Item.cs
  - Sources/Modules/Yggdrasil/KelpCurtain/Items/Tools/Developer/GenerateMazeRoom.cs
  - Sources/Modules/Yggdrasil/KelpCurtain/Items/Tools/Developer/ResetIsleOfBloom.cs
  - Sources/Modules/Yggdrasil/KelpCurtain/Items/Tools/Developer/ResetKelpCurtain.cs
  - Sources/Modules/Yggdrasil/KelpCurtain/Items/Tools/Developer/UnderWaterDungeon.cs
  - .planning/phases/01-item-inventory-completed-art-items/scripts/check-tranche-B.ps1
autonomous: true
requirements: [QUAL-05]
estimate:
  tokens: 55000
  raw_tokens: 55000
  tasks: 3
  confidence: low

must_haves:
  truths:
    - "Every completed-art entry whose class belongs under `Items/{Armors,Placeables,Tools,Consumables,PermanentBoosters,Pets,Critters}` either has a compiling `ModItem` class or a recorded blocker."
    - "The seven unclassified placeable items and the four developer tools declare a `LocalizationCategory` (`Placeables` and `Tools` respectively); no class in these folders defaults to `Items`."
    - "Design deviations on completed-art entries are corrected to the Feishu design; art-incomplete deviations stay for Phase 2 (D-12)."
    - "`dotnet build /p:Configuration=Release /p:WarningLevel=0` passes."
    - "No `.png` or other binary asset is added or modified."
    - statement: "Each reconciled placeable/furniture/tool item places, uses, or equips correctly in a tModLoader client with the documented behavior."
      verification: backstop
  artifacts:
    - "new `ModItem` classes under `Sources/Modules/Yggdrasil/KelpCurtain/Items/{Armors,Placeables,Tools,Consumables,PermanentBoosters,Pets,Critters}/` (names derived from `01-INVENTORY.json`)"
    - "`LocalizationCategory` overrides on the seven placeable `_Item` classes and the four `Tools/Developer` classes listed in `files_modified`"
    - ".planning/phases/01-item-inventory-completed-art-items/scripts/check-tranche-B.ps1"
  key_links:
    - "01-INVENTORY.json artwork_complete=true entries -> item classes in the tranche-B folders"
    - "Placeable `_Item` key names -> `Sources/Everglow/Localization/{en-US,zh-Hans}/Mods.Everglow.Items.Placeables.hjson` (verified in plan 05)"
  prohibitions:
    - "Never create placeholder art or modify existing binary/art assets."
    - "Never hand-write an asset path string; use generated `ModAsset` members or `Commons.ModAsset`."
    - "Never delete or rename an existing localization key."
    - "Never bulk-rename placeable localization keys; resolve the `_Item` vs tile-name key per item (RESEARCH Open Question 3)."
    - "Never wholesale-rewrite an existing item class that already matches the design (D-10)."
  assumptions:
    - "QUAL-05 edge coverage is unresolved/unclassified by the spec-less probe; acceptance for the tranche is the completed-art coverage check plus a passing build. Flagged here, not silently dropped."
    - "RESEARCH A3 (the 70/131 missing-key count) is approximate; the plan fixes categories but does not assume a fixed number of missing keys."

---

# Phase 1 Plan 04 — Completed-Art Item Tranche B (Armors, Placeables, Tools, Consumables, Permanent Boosters, Pets, Critters)

<objective>
Bring the completed-art items in the armor/placeable/tool/consumable/booster/pet/critter group to design parity and category correctness, using the plan-02 inventory as the worklist.

Purpose: same tranche rules as plan 03 (D-10 trust existing, D-12 fix completed-art deviations, no placeholders), applied to the remaining category folders so the two tranches together cover every completed-art item folder with disjoint file ownership.

Output: reconciled and/or newly added `ModItem` classes in the tranche-B folders, `LocalizationCategory` overrides for the seven placeables and four developer tools, and `scripts/check-tranche-B.ps1`.
</objective>

<execution_context>
@C:/Users/book/.config/opencode/gsd-core/workflows/execute-plan.md
@C:/Users/book/.config/opencode/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/01-item-inventory-completed-art-items/01-CONTEXT.md
@.planning/phases/01-item-inventory-completed-art-items/01-INVENTORY.json
@.planning/phases/01-item-inventory-completed-art-items/01-PATTERNS.md
@AGENTS.md
</context>

## Artifacts This Plan Produces

- Reconciled/new `ModItem` classes under `Sources/Modules/Yggdrasil/KelpCurtain/Items/{Armors,Placeables,Tools,Consumables,PermanentBoosters,Pets,Critters}/` (names derived from `01-INVENTORY.json`).
- `LocalizationCategory` overrides on the seven placeable `_Item` classes and the four `Tools/Developer` classes listed in `files_modified`.
- `.planning/phases/01-item-inventory-completed-art-items/scripts/check-tranche-B.ps1` — completed-art coverage + no-placeholder gate.
- Deviation ledger entries for `01-04-SUMMARY.md` (merged into the inventory by plan 05).

<tasks>

<task type="auto">
  <name>Fix localization categories and completed-art design deviations (tranche B)</name>
  <reversibility rating="costly">D-12 fixes design deviations on completed-art entries inside Phase 1; reversing it would require re-opening accepted Phase 1 items and re-deciding the Phase 1/Phase 2 split.</reversibility>
  <files>Sources/Modules/Yggdrasil/KelpCurtain/Items/Placeables/GreenCourtBrick_Item.cs, Sources/Modules/Yggdrasil/KelpCurtain/Items/Placeables/AgedGreenCourtBrick_Item.cs, Sources/Modules/Yggdrasil/KelpCurtain/Items/Placeables/WaterErodedGreenBrick_Item.cs, Sources/Modules/Yggdrasil/KelpCurtain/Items/Placeables/ForestRainVineTile_Thick_Item.cs, Sources/Modules/Yggdrasil/KelpCurtain/Items/Placeables/ForestRainVineTile_Thin_Item.cs, Sources/Modules/Yggdrasil/KelpCurtain/Items/Placeables/Walls/AgedGreenCourtWall_Item.cs, Sources/Modules/Yggdrasil/KelpCurtain/Items/Placeables/DecayingWoodCourt/CrackedForestThrone_Item.cs, Sources/Modules/Yggdrasil/KelpCurtain/Items/Tools/Developer/GenerateMazeRoom.cs, Sources/Modules/Yggdrasil/KelpCurtain/Items/Tools/Developer/ResetIsleOfBloom.cs, Sources/Modules/Yggdrasil/KelpCurtain/Items/Tools/Developer/ResetKelpCurtain.cs, Sources/Modules/Yggdrasil/KelpCurtain/Items/Tools/Developer/UnderWaterDungeon.cs</files>
  <precondition>A local tModLoader installation is resolvable (a `tModLoader.targets` exists in this repo or an ancestor) so `dotnet build` can package.</precondition>
  <read_first>
    - `.planning/phases/01-item-inventory-completed-art-items/01-INVENTORY.json` — entries with `artwork_complete: true` in the tranche-B folders.
    - `.planning/phases/01-item-inventory-completed-art-items/01-PATTERNS.md` — §"Modifications: existing deviations to fix" and §"Placeables / Armors / Localization" analogs.
    - `Sources/Modules/Yggdrasil/KelpCurtain/Items/Placeables/OldMoss_Item.cs`, `Items/Placeables/Walls/GreenCourtWall_Item.cs`, `Items/Placeables/DecayingWoodCourt/WitherWoodBookcase.cs`.
    - `Sources/Everglow.Function/Utilities/LocalizationUtils.cs`.
    - `AGENTS.md` — formatting, no-placeholder, build rules.
  </read_first>
  <action>
    Add a `LocalizationCategory` override to each listed class: the seven placeable `_Item` classes get `LocalizationUtils.Categories.Placeables`, and `GenerateMazeRoom`, `ResetIsleOfBloom`, `ResetKelpCurtain`, `UnderWaterDungeon` get `LocalizationUtils.Categories.Tools`. Then, for every tranche-B completed-art entry (`artwork_complete: true`, class under `Items/{Armors,Placeables,Tools,Consumables,PermanentBoosters,Pets,Critters}`), compare the class against its Feishu design row and correct deviations in defense, effects, set bonuses, recipes, placeable-tile bindings, and values (D-12). Trust classes that already match (D-10); do not rewrite them. Do not modify art-incomplete entries. Use tab indentation, LF, UTF-8 without BOM, Allman braces, and no unrelated reformatting. Never create or edit a `.png`; never hand-write an asset path string.
  </action>
  <verify>
    <automated>dotnet build /p:Configuration=Release /p:WarningLevel=0</automated>
    <fails_when>non-zero exit, or build output contains "error CS" or "error MSB"</fails_when>
  </verify>
  <acceptance_criteria>
    - All eleven listed classes declare a non-default `LocalizationCategory`; no class in the tranche-B folders defaults to `Items`.
    - Every completed-art tranche-B entry's in-class values match its design row, or a blocker is recorded on the inventory entry.
    - Release build exits 0 with no `error CS` / `error MSB` lines.
    - No `.png` or other binary file is added or modified by this task.
  </acceptance_criteria>
  <done>Completed-art tranche-B classes match the design and carry correct localization categories; the Release build is green.</done>
</task>

<task type="auto">
  <name>Implement missing completed-art classes (tranche B)</name>
  <reversibility rating="costly">D-10 restricts this plan to adding only missing entries rather than re-implementing existing ones; reversing that stance would touch many item classes, localization keys, and compatibility-sensitive internal names.</reversibility>
  <files>Sources/Modules/Yggdrasil/KelpCurtain/Items/Armors/, Sources/Modules/Yggdrasil/KelpCurtain/Items/Placeables/, Sources/Modules/Yggdrasil/KelpCurtain/Items/Tools/, Sources/Modules/Yggdrasil/KelpCurtain/Items/Consumables/, Sources/Modules/Yggdrasil/KelpCurtain/Items/PermanentBoosters/, Sources/Modules/Yggdrasil/KelpCurtain/Items/Pets/, Sources/Modules/Yggdrasil/KelpCurtain/Items/Critters/</files>
  <read_first>
    - `.planning/phases/01-item-inventory-completed-art-items/01-INVENTORY.json` — completed-art entries with empty `internal_name` or a `no repo implementation found` blocker.
    - `.planning/phases/01-item-inventory-completed-art-items/01-PATTERNS.md` — the armor/placeable/furniture analogs.
    - `Sources/Modules/Yggdrasil/KelpCurtain/Items/Armors/DevilHeart/DevilHeartHelmet.cs`, `Items/Placeables/DecayingWoodCourt/WitherWoodBookcase.cs`.
    - `Sources/Everglow.Function/Templates/` — furniture templates (`BookcaseItem`, `ChairItem`, ...).
    - `AGENTS.md` — module placement, asset, resource-packing, and no-manual-`AddContent` rules.
  </read_first>
  <action>
    For every completed-art tranche-B entry with no repo class, add one `ModItem` class in the deterministic folder `Sources/Modules/Yggdrasil/KelpCurtain/Items/<Category>/`, namespace `Everglow.Yggdrasil.KelpCurtain.Items.<Category>`, internal name from the design row's English name. Prefer the `Everglow.Function/Templates` base for furniture instead of a bespoke `ModItem`. Set `LocalizationCategory` from `LocalizationUtils.Categories.*`, implement `SetDefaults`/placeable bindings/armor set behavior from the design row, and add recipes only when the design specifies them. Ordinary content must not use a manual `AddContent`. Do not create a `.png`; if approved artwork is absent, leave the entry blocked. Do not touch classes outside these seven folders.
  </action>
  <verify>
    <automated>dotnet build /p:Configuration=Release /p:WarningLevel=0</automated>
    <fails_when>non-zero exit, or build output contains "error CS" or "error MSB"</fails_when>
  </verify>
  <acceptance_criteria>
    - Every completed-art tranche-B entry either maps to an existing/new class or carries a blocker; no entry is unaccounted for.
    - New classes follow the folder/namespace/category conventions of their analogues and are auto-discovered.
    - Placeable/tile bindings resolve to existing tile classes; furniture uses the template base where one exists.
    - No `.png` is created; entries needing absent artwork remain blocked.
    - Release build exits 0.
  </acceptance_criteria>
  <done>All completed-art tranche-B items are represented by a compiling class or an explicit blocker, with no placeholder art.</done>
</task>

<task type="auto">
  <name>Gate the tranche with coverage, no-placeholder, and BOM checks</name>
  <files>.planning/phases/01-item-inventory-completed-art-items/scripts/check-tranche-B.ps1</files>
  <read_first>
    - `.planning/phases/01-item-inventory-completed-art-items/01-INVENTORY.json`.
    - `AGENTS.md` — BOM check script and build rules.
    - `.planning/phases/01-item-inventory-completed-art-items/01-VALIDATION.md` — §"Manual-Only Verifications".
  </read_first>
  <action>
    Author `scripts/check-tranche-B.ps1` (no parameters). It loads `01-INVENTORY.json`, selects entries whose `internal_name` contains one of `.Items.Armors.`, `.Items.Placeables.`, `.Items.Tools.`, `.Items.Consumables.`, `.Items.PermanentBoosters.`, `.Items.Pets.`, `.Items.Critters.` and whose `artwork_complete` is true, and fails if any such entry has neither a resolvable `.cs` file (from `internal_name`) nor a non-empty blocker. It also fails if `git status --porcelain` reports a newly added or modified `*.png` under `Sources/Modules/Yggdrasil/KelpCurtain/Items/`. On success print the count of covered entries. Run it with the AGENTS.md BOM check and the Release build, and record any fixed deviation for the plan SUMMARY.
  </action>
  <verify>
    <automated>powershell -NoProfile -ExecutionPolicy Bypass -File ".planning/phases/01-item-inventory-completed-art-items/scripts/check-tranche-B.ps1"</automated>
    <fails_when>non-zero exit — an artwork-complete tranche-B entry has no class and no blocker, or a `.png` was added/modified under `Sources/Modules/Yggdrasil/KelpCurtain/Items/`</fails_when>
  </verify>
  <acceptance_criteria>
    - `check-tranche-B.ps1` exits 0 and reports a non-zero covered-entry count.
    - No `*.png` under `Sources/Modules/Yggdrasil/KelpCurtain/Items/` is added or modified.
    - The BOM check from `AGENTS.md` passes for every file changed by this plan.
    - The Release build passes and deviations fixed are recorded for the SUMMARY.
  </acceptance_criteria>
  <done>The tranche-B tranche is gated by coverage, no-placeholder, and formatting checks, and any fixed deviations are recorded as QUAL-05 evidence.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Fetched design rows -> C# values | Design text is transcribed into code; it must remain data. |
| Placeable `_Item` class -> tile type | Mismatched placeable/tile bindings break placement behavior. |
| Item classes -> build/package pipeline | New/changed classes are compiled and packaged. |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-04-01 | Tampering | Placeholder art introduction | high | mitigate | No-placeholder prohibition plus `check-tranche-B.ps1`'s `.png` git guard; absent art stays blocked. |
| T-04-02 | Tampering/Elevation | Instruction-like design-row text driving code/paths | high | mitigate | Only numeric/name fields are transcribed; paths follow repo conventions. |
| T-04-03 | Tampering | Silent omission of a completed-art entry | medium | mitigate | Coverage checker requires class-or-blocker per completed-art tranche-B entry. |
| T-04-04 | Tampering | Placeable key rename breaking localization | medium | mitigate | The plan forbids bulk key renames and defers key resolution to plan 05's additive check. |
| T-04-SC | Tampering | Package supply chain | low | accept | No installs in this phase. |

</threat_model>

<verification>
- `dotnet build /p:Configuration=Release /p:WarningLevel=0` exits 0.
- `check-tranche-B.ps1` exits 0 with a non-zero covered-entry count.
- `git status --porcelain` shows no `.png`/binary change under `Sources/Modules/Yggdrasil/KelpCurtain/Items/`.
- BOM check from `AGENTS.md` passes.
</verification>

<success_criteria>
- Completed-art tranche-B entries are usable with documented values/effects (ROADMAP success criterion 3).
- Unclassified placeables/tools are fixed; completed-art design deviations are corrected (D-12), art-incomplete deviations deferred (Phase 2).
- No placeholder art, no binary asset edits, no manual `AddContent`.
</success_criteria>

<output>
Create `.planning/phases/01-item-inventory-completed-art-items/01-04-SUMMARY.md` when done, including the deviation ledger (file, field, before, after) and the covered-entry count.
</output>
